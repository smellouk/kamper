---
phase: 12-kotlin-gradle-first-monorepo-consolidation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STRUCTURE.md
autonomous: true
gap_closure: true
requirements: [BUILD-02]

must_haves:
  truths:
    - "STRUCTURE.md directory tree shows build-logic/ instead of buildSrc/"
    - "STRUCTURE.md directory tree includes demos/react-native/ with composite build note"
    - "STRUCTURE.md Directory Purposes section describes build-logic/ accurately (not buildSrc/)"
    - "STRUCTURE.md Special Directories section describes build-logic/ (not buildSrc/)"
    - "STRUCTURE.md includes a Gradle Build Infrastructure section covering CC, PREFER_SETTINGS, and composite builds"
  artifacts:
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Accurate post-Phase-12 build structure documentation"
      contains: "build-logic"
  key_links:
    - from: ".planning/codebase/STRUCTURE.md"
      to: "Actual repo build layout"
      via: "Directory tree and Special Directories sections"
      pattern: "build-logic|PREFER_SETTINGS|configuration-cache|includeBuild"
---

<objective>
Update `.planning/codebase/STRUCTURE.md` to accurately reflect the monorepo build structure after Phases 11 and 12: replace all three stale `buildSrc/` references with `build-logic/`, add `demos/react-native/` to the directory tree, and add a Gradle Build Infrastructure section documenting configuration cache, PREFER_SETTINGS, and composite builds.

Purpose: ROADMAP SC-3 requires "Build structure documented in STRUCTURE.md." The file was not updated during Phase 12 execution, leaving three stale `buildSrc/` references and no record of the composite build architecture or Gradle performance flags. This gap closure brings the documentation contract into compliance.

Output: Updated `.planning/codebase/STRUCTURE.md` with accurate post-Phase-12 content.
</objective>

<execution_context>
@/Users/smellouk/.claire/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/STRUCTURE.md
@.planning/phases/12-kotlin-gradle-first-monorepo-consolidation/12-VERIFICATION.md
@.planning/phases/12-kotlin-gradle-first-monorepo-consolidation/12-02-SUMMARY.md
@.planning/phases/12-kotlin-gradle-first-monorepo-consolidation/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix directory tree — replace buildSrc/, add demos/react-native/</name>
  <files>.planning/codebase/STRUCTURE.md</files>
  <read_first>.planning/codebase/STRUCTURE.md (lines 1-44, Directory Layout section)</read_first>
  <action>
In the `## Directory Layout` fenced code block (lines 7-44), make two changes:

**Change 1 — line 25:** Replace:
```
├── buildSrc/                           # Gradle convention plugins
```
With:
```
├── build-logic/                        # Gradle convention plugins (composite build)
```

**Change 2 — after the `demos/web/` line (currently line 32), add demos/react-native/:**

The demos/ block currently ends with:
```
│   └── web/                            # JS/WASM web demo
```

Change it to:
```
│   ├── web/                            # JS/WASM web demo
│   └── react-native/                   # React Native demo (Gradle includeBuild composite)
│       └── android/                    # Android Gradle root for the RN composite
```

No other changes in this task.
  </action>
  <verify>
    <automated>grep -n "build-logic\|react-native" /Users/smellouk/Developer/git/kamper/.planning/codebase/STRUCTURE.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildSrc/" .planning/codebase/STRUCTURE.md` returns 2 (the two remaining occurrences in Directory Purposes and Special Directories — fixed in Task 2)
    - `grep "build-logic/" .planning/codebase/STRUCTURE.md` returns at least 1 match (directory tree entry)
    - `grep "react-native" .planning/codebase/STRUCTURE.md` returns at least 1 match in the directory tree block
    - `grep "includeBuild composite\|Gradle includeBuild" .planning/codebase/STRUCTURE.md` returns a match
  </acceptance_criteria>
  <done>Directory tree shows build-logic/ and includes demos/react-native/ with composite build annotation. buildSrc/ no longer appears in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Directory Purposes, Special Directories, and add Gradle Build Infrastructure section</name>
  <files>.planning/codebase/STRUCTURE.md</files>
  <read_first>.planning/codebase/STRUCTURE.md (lines 158-307, Directory Purposes and Special Directories sections)</read_first>
  <action>
Make three edits to `.planning/codebase/STRUCTURE.md`:

**Edit 1 — Directory Purposes section, buildSrc/ entry (~line 180):**

Replace:
```
**`buildSrc/`:**
- Purpose: Shared Gradle convention plugins and dependency version management.
```
With:
```
**`build-logic/`:**
- Purpose: Shared Gradle convention plugins as a standalone composite build. Contains `KmpLibraryPlugin`, `AndroidConfigPlugin`, and `KamperPublishPlugin`. Has its own `settings.gradle.kts` with independent `repositories{}` using `FAIL_ON_PROJECT_REPOS`.
```

**Edit 2 — Special Directories section, buildSrc/ entry (~line 284):**

Replace:
```
**`buildSrc/`:**
- Purpose: Gradle plugin code compiled before the main build. Contains shared dependency versions and convention plugins.
- Generated: No (hand-authored)
- Committed: Yes
```
With:
```
**`build-logic/`:**
- Purpose: Gradle convention plugin composite build. Replaced `buildSrc/` in Phase 11. Contains hand-authored convention plugins applied by all Kamper library modules.
- Generated: No (hand-authored)
- Committed: Yes
```

**Edit 3 — Add new section before the `*Structure analysis:` footer line.**

Insert the following new section immediately before the final line (`*Structure analysis: 2026-04-26*`):

```
---

## Gradle Build Infrastructure

Established in Phases 11–12. Applies to the entire monorepo.

### Composite Builds

| Composite Root | Purpose | wired via |
|----------------|---------|-----------|
| `build-logic/` | Convention plugins (KmpLibraryPlugin, AndroidConfigPlugin, KamperPublishPlugin) | `includeBuild("build-logic")` in root `settings.gradle.kts` |
| `demos/react-native/android/` | React Native Android demo (framework-owned Groovy DSL preserved) | `includeBuild("demos/react-native/android")` in root `settings.gradle.kts` |

### Gradle Performance Flags (`gradle.properties`)

| Flag | Value | Effect |
|------|-------|--------|
| `org.gradle.configuration-cache` | `true` | Task graph serialized; reruns skip configuration phase |
| `org.gradle.parallel` | `true` | Modules compiled in parallel across daemon threads |
| `org.gradle.jvmargs` | `-Xmx4096m` | Daemon heap sized for 18 parallel KMP module compilations |

### Repository Management (`settings.gradle.kts`)

- `dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) }` — root settings declares `google()` + `mavenCentral()`; module-level `repositories {}` blocks are discouraged but not hard-blocked (PREFER_SETTINGS chosen over FAIL_ON_PROJECT_REPOS because KotlinJS toolchain requires adding ivy repositories at configuration time).
- `build-logic/settings.gradle.kts` uses `FAIL_ON_PROJECT_REPOS` independently — it is a separate Gradle build and does not inherit from the root.

```

No other changes.
  </action>
  <verify>
    <automated>grep -c "buildSrc" /Users/smellouk/Developer/git/kamper/.planning/codebase/STRUCTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildSrc" .planning/codebase/STRUCTURE.md` returns 0 (all three stale references replaced)
    - `grep -c "build-logic" .planning/codebase/STRUCTURE.md` returns 4 or more (tree + purposes + special + infrastructure)
    - `grep "PREFER_SETTINGS" .planning/codebase/STRUCTURE.md` returns a match
    - `grep "configuration-cache" .planning/codebase/STRUCTURE.md` returns a match
    - `grep "includeBuild" .planning/codebase/STRUCTURE.md` returns at least 2 matches (one per composite)
    - `grep "react-native" .planning/codebase/STRUCTURE.md` returns at least 2 matches (tree + infrastructure table)
  </acceptance_criteria>
  <done>Zero buildSrc/ references remain. build-logic/ described accurately in Directory Purposes and Special Directories. New Gradle Build Infrastructure section documents composite builds, performance flags, and repository management. STRUCTURE.md now satisfies ROADMAP SC-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → planning doc | Documentation-only change; no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | STRUCTURE.md edit | accept | Documentation file; no secrets, no executable content; version-controlled |
</threat_model>

<verification>
After completing both tasks:

```bash
grep -c "buildSrc" .planning/codebase/STRUCTURE.md
grep -c "build-logic" .planning/codebase/STRUCTURE.md
grep "PREFER_SETTINGS" .planning/codebase/STRUCTURE.md
grep "configuration-cache" .planning/codebase/STRUCTURE.md
grep "react-native" .planning/codebase/STRUCTURE.md
grep "includeBuild" .planning/codebase/STRUCTURE.md
```

Expected results:
- `buildSrc` count: 0
- `build-logic` count: 4 or more
- `PREFER_SETTINGS`: 1 match
- `configuration-cache`: 1 match
- `react-native`: 2 or more matches
- `includeBuild`: 2 or more matches
</verification>

<success_criteria>
- All three stale `buildSrc/` references replaced with `build-logic/`
- `demos/react-native/` appears in the directory tree with composite build annotation
- `build-logic/` description in Directory Purposes section reflects composite build reality
- `build-logic/` description in Special Directories section notes Phase 11 replacement of buildSrc
- New "Gradle Build Infrastructure" section documents CC, parallel, PREFER_SETTINGS, and both composite builds
- ROADMAP SC-3 satisfied: build structure documented accurately in STRUCTURE.md
</success_criteria>

<output>
After completion, create `.planning/phases/12-kotlin-gradle-first-monorepo-consolidation/12-05-SUMMARY.md`
</output>

---
phase: 20-repo-clean-up-for-open-source-projects-contribution-guidelin
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - SECURITY.md
autonomous: true
requirements:
  - OSS-02
tags:
  - documentation
  - readme
  - oss-readiness
  - security-md-removal

must_haves:
  truths:
    - "An external user can copy the README install block, paste it into a fresh Gradle project, and the dependencies resolve from Maven Central without authentication"
    - "README contains no `maven.pkg.github.com` references"
    - "README contains no `Security Considerations` section duplicating SECURITY.md content"
    - "README contains a Versioning section explaining semver in Kamper context (D-09)"
    - "SECURITY.md no longer exists in the repository (D-05)"
    - "Internal Phase 14 audit content (threat IDs, ASVS levels) does not leak into any public-facing file"
  artifacts:
    - path: "README.md"
      provides: "Public-facing project introduction, install instructions (Maven Central), modules table, integrations, versioning"
      contains: "com.smellouk.kamper:engine"
      min_lines: 400
  key_links:
    - from: "README.md installation block"
      to: "Maven Central (mavenCentral())"
      via: "Gradle dependency declarations"
      pattern: "com.smellouk.kamper:engine"
    - from: "README.md Versioning section"
      to: "GitHub Releases + CHANGELOG.md"
      via: "links"
      pattern: "github.com/smellouk/kamper/releases"
---

<objective>
Refresh `README.md` to be the public face of v1.0 and delete `SECURITY.md` per D-05. Two simultaneous changes:

1. **README.md surgical replacement** (D-07, D-08, D-09): preserve the existing structure and badge strip, but (a) replace the installation block (currently uses `maven.pkg.github.com` which requires auth) with Maven Central coordinates, (b) remove the "Security Considerations" section that duplicates SECURITY.md, and (c) add a Versioning section that points to GitHub Releases and CHANGELOG.md.

2. **SECURITY.md deletion** (D-05): the file (162 lines) is deleted entirely. Lines 102-162 contain internal Phase 14 audit content (threat IDs, ASVS levels, accepted risks) that must not appear in any public-facing file. Deletion eliminates the leak path automatically.

Purpose: Implement the README + SECURITY.md half of OSS-02 (open-source readiness for the public-facing files). Leave audit-trail-clean — no leftover stale URLs, no leftover dup content.

Output: A refreshed README.md (≥400 lines, all required sections present, no stale references) and a removed SECURITY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-CONTEXT.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md
@README.md
@SECURITY.md

<interfaces>
<!-- Content contracts derived from RESEARCH.md (verified) and the verified Maven Central artifact map. -->

VERIFIED Maven Central artifact coordinates (RESEARCH.md lines 106-121, derived from build-logic/src/main/kotlin/KamperPublishPlugin.kt):

  Group: `com.smellouk.kamper`
  Version: `1.0.0` (from gradle.properties)

  Artifacts:
    com.smellouk.kamper:engine:1.0.0
    com.smellouk.kamper:bom:1.0.0
    com.smellouk.kamper:cpu-module:1.0.0
    com.smellouk.kamper:fps-module:1.0.0
    com.smellouk.kamper:memory-module:1.0.0
    com.smellouk.kamper:network-module:1.0.0
    com.smellouk.kamper:jank-module:1.0.0
    com.smellouk.kamper:gc-module:1.0.0
    com.smellouk.kamper:thermal-module:1.0.0
    com.smellouk.kamper:issues-module:1.0.0
    com.smellouk.kamper:sentry-integration:1.0.0
    com.smellouk.kamper:firebase-integration:1.0.0
    com.smellouk.kamper:opentelemetry-integration:1.0.0
    com.smellouk.kamper:ui-android:1.0.0

VERIFIED line ranges in current README.md (do NOT alter):
  - Lines 1-6: badge strip (license, release, issues, stars) — KEEP
  - Lines 7-88: tagline, Kamper UI section, platform support matrix — KEEP
  - Lines 122-299: Quick start, Modules section — KEEP (apart from any embedded GPR refs)
  - Lines 300-492: Kamper UI detailed section, Demos, Lifecycle, Service Integrations — KEEP
  - Lines 537-586: How-tos, Contributing, Acknowledgements, License — KEEP

VERIFIED line ranges in current README.md to MODIFY:
  - Lines 91-120: Installation block (contains maven.pkg.github.com — replace with Maven Central block)
  - Lines 494-535: "Security Considerations" section (delete entirely — content duplicates SECURITY.md and references KamperUiInitProvider internals)
  - Insert near line 557 (before `## Contributing`): new `## Versioning` section

VERIFIED structure of current SECURITY.md (162 lines):
  - Lines 1-101: Public-facing security policy (vulnerability reporting, contact email)
  - Lines 102-162: INTERNAL Phase 14 audit (threat IDs T-12-XX, ASVS Level annotations, "Phase 14" mentions)
  Action: delete entire file — D-05 explicit, do not preserve any content.

Pitfalls (from RESEARCH.md):
- Pitfall 2: removing SECURITY.md without removing README "Security Considerations" leaves duplicate content
- Pitfall 3: SECURITY.md has internal audit appended; deletion resolves automatically (do NOT copy any content out before deleting)
- Pitfall 4: stale `maven.pkg.github.com` URL in README causes 401 errors for external contributors
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README.md installation block and add Versioning section (D-07, D-08, D-09)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file, 585 lines — to identify exact boundaries of sections being replaced/removed/inserted)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md (lines 309-358 — installation block + Versioning section verbatim)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md (lines 259-330 — surgical change map)
    - build-logic/src/main/kotlin/KamperPublishPlugin.kt (verify artifact ID derivation if uncertain)
  </read_first>
  <action>
    Apply THREE surgical changes to README.md. Do not rewrite the entire file — preserve all sections listed as KEEP in the interfaces block above.

    **Change 1: Replace the Installation block (current lines ~91-120, identified by the `maven.pkg.github.com` URL).**

    Locate and DELETE:
    - The paragraph that begins "Add the GitHub Packages repository, then pull..."
    - The `repositories { maven("https://maven.pkg.github.com/smellouk/kamper") ... }` block
    - The associated `dependencies { ... }` block that uses GitHub Packages

    Replace with EXACTLY:

    ````markdown
    ## Installation

    Kamper artifacts are published to **Maven Central**. Add the engine and whichever modules you need:

    ```kotlin
    dependencies {
        val kamperVersion = "1.0.0"

        implementation("com.smellouk.kamper:engine:$kamperVersion")

        // Core metrics
        implementation("com.smellouk.kamper:cpu-module:$kamperVersion")
        implementation("com.smellouk.kamper:fps-module:$kamperVersion")
        implementation("com.smellouk.kamper:memory-module:$kamperVersion")
        implementation("com.smellouk.kamper:network-module:$kamperVersion")

        // Advanced metrics
        implementation("com.smellouk.kamper:jank-module:$kamperVersion")
        implementation("com.smellouk.kamper:gc-module:$kamperVersion")
        implementation("com.smellouk.kamper:thermal-module:$kamperVersion")
        implementation("com.smellouk.kamper:issues-module:$kamperVersion")

        // Android debug overlay (auto-init, debug builds only)
        debugImplementation("com.smellouk.kamper:ui-android:$kamperVersion")
    }
    ```

    > If `mavenCentral()` is not already present in your `repositories {}` block, add it.
    ````

    **Change 2: Delete the "Security Considerations" section (current lines ~494-535).**

    Locate the section beginning with `## Security Considerations` (or a near-identical heading) and ending immediately before the next H2 heading (typically `## How-tos` per PATTERNS.md line 314). Delete every line from the `## Security Considerations` heading through and including the closing `---` (if any) before the next H2 heading. Do not preserve any sentences, even if they appear factually correct — the entire section was content-duplication of SECURITY.md and references implementation internals (`KamperUiInitProvider`) inappropriate for a top-level README.

    **Change 3: Insert a new Versioning section before `## Contributing` (current ~line 557).**

    Insert this new section verbatim immediately before the `## Contributing` heading:

    ```markdown
    ## Versioning

    Kamper follows [semantic versioning](https://semver.org/):

    - **Patch** (`1.0.x`) — bug fixes; no API changes
    - **Minor** (`1.x.0`) — new modules or features; backward compatible
    - **Major** (`x.0.0`) — breaking API changes; frozen for all v1.x releases

    The latest release is always available on [GitHub Releases](https://github.com/smellouk/kamper/releases).
    Changes are listed in [CHANGELOG.md](CHANGELOG.md).
    ```

    **Discretionary items (Claude's Discretion per CONTEXT.md):**
    - DO NOT add a CI badge — verified that `pull-request.yml` targets the `develop` branch (.github/workflows/pull-request.yml line 8) and no push-to-main workflow exists; a badge would render misleading or empty (Pitfall 1, RESEARCH.md).
    - DO NOT add CODE_OF_CONDUCT.md in this task (it is a separate file; if added, it would be in a separate plan/follow-up). README may include a brief footer note, but no new file is required.
    - License attribution at footer: keep whatever is currently there.

    **Stale-reference sweep (final pass after the three changes):**
    - Confirm `grep -c "maven.pkg.github.com" README.md` returns 0
    - Confirm `grep -c "GitHub Packages" README.md` returns 0
    - Confirm no link to `blob/develop/...` remains in README
  </action>
  <verify>
    <automated>test -f README.md && grep -c "maven.pkg.github.com" README.md | grep -q "^0$" && grep -c "GitHub Packages" README.md | grep -q "^0$" && grep -c "com.smellouk.kamper:engine" README.md | grep -qE "^[1-9]" && grep -c "com.smellouk.kamper:cpu-module" README.md | grep -qE "^[1-9]" && grep -c "## Installation" README.md | grep -q "^1$" && grep -c "## Versioning" README.md | grep -q "^1$" && grep -c "## Security Considerations" README.md | grep -q "^0$" && grep -c "github.com/smellouk/kamper/releases" README.md | grep -qE "^[1-9]" && grep -c "semantic versioning\|semver.org" README.md | grep -qE "^[1-9]" && ! grep -q "blob/develop/" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `wc -l README.md` reports >= 400 lines (current 585; expected after edits ~570 ± 30, never below 400)
    - `grep -c "maven.pkg.github.com" README.md` returns 0
    - `grep -c "GitHub Packages" README.md` returns 0
    - `grep -c "com.smellouk.kamper:engine" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:cpu-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:fps-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:memory-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:network-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:jank-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:gc-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:thermal-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:issues-module" README.md` returns >= 1
    - `grep -c "com.smellouk.kamper:ui-android" README.md` returns >= 1
    - `grep -c "## Installation" README.md` returns 1 (exactly one Installation H2)
    - `grep -c "## Versioning" README.md` returns 1
    - `grep -c "## Security Considerations" README.md` returns 0 (section deleted)
    - `grep -c "KamperUiInitProvider" README.md` returns 0 (Pitfall 2 — implementation internals removed)
    - `grep -c "github.com/smellouk/kamper/releases" README.md` returns >= 1
    - `grep -c "CHANGELOG.md" README.md` returns >= 1
    - `grep -c "semantic versioning\|semver.org" README.md` returns >= 1
    - `! grep -q "blob/develop/" README.md` (no stale develop links)
    - Badge strip preserved: `head -6 README.md | grep -c "shields.io"` returns >= 3
    - Existing platform matrix preserved: `grep -c "Android\|iOS\|JVM\|macOS\|tvOS" README.md` >= 5
    - Existing Service Integrations section preserved: `grep -cE "^## Service Integrations\|^## Integrations" README.md` >= 1 OR `grep -c "Sentry\|Firebase\|OpenTelemetry" README.md` >= 3
  </acceptance_criteria>
  <done>README.md has exactly one Installation block (Maven Central, no GitHub Packages), exactly one Versioning section, no Security Considerations section, no `maven.pkg.github.com` references, all 9 module artifact coordinates present, and the unmodified KEEP regions (badges, tagline, modules table, integrations) are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete SECURITY.md (D-05)</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (current 162-line file — to confirm what is being deleted; lines 102-162 contain internal Phase 14 audit data per RESEARCH.md Pitfall 3)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-CONTEXT.md (D-05: "Delete SECURITY.md entirely — it is not needed")
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md (lines 580-589 — security domain note: deletion eliminates the leak path automatically)
  </read_first>
  <action>
    Delete `SECURITY.md` from the working tree using `git rm SECURITY.md`. Do not copy or archive any content — D-05 is explicit ("Delete SECURITY.md entirely") and Pitfall 3 warns that lines 102-162 contain internal Phase 14 audit (threat IDs, ASVS levels) that must not appear in any other file.

    Concrete commands:
    ```bash
    git rm SECURITY.md
    ```

    After removal, confirm:
    ```bash
    test ! -f SECURITY.md && echo "PASS: SECURITY.md deleted"
    ```

    Do NOT:
    - Move or rename the file
    - Copy any content into README.md, CONTRIBUTING.md, or any other public-facing file
    - Reference SECURITY.md from README.md (D-07 makes Security an absent section by design — see Open Question 2 in RESEARCH.md)
    - Re-create SECURITY.md as a stub or shorter version
  </action>
  <verify>
    <automated>test ! -f SECURITY.md && ! grep -rE "T-12-[0-9]+|ASVS Level [12]|Phase 14 (security audit|audit)" README.md CONTRIBUTING.md .github/ 2>/dev/null && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f SECURITY.md` exits 0 (file no longer exists)
    - `git ls-files SECURITY.md` returns empty (file removed from index)
    - `grep -rE "T-12-[0-9]+" README.md CONTRIBUTING.md .github/` returns nothing (no internal threat IDs leaked)
    - `grep -rE "ASVS Level [12]" README.md CONTRIBUTING.md .github/` returns nothing (no internal ASVS annotations leaked)
    - `grep -rE "Phase 14" README.md CONTRIBUTING.md .github/` returns nothing (no internal phase references leaked)
    - No new file `SECURITY-old.md` or similar archive exists in the repository: `! ls SECURITY*.md 2>/dev/null` (no SECURITY*.md files at all)
  </acceptance_criteria>
  <done>SECURITY.md no longer exists in the working tree or git index. No SECURITY.md content (including the internal Phase 14 audit) appears in any other public-facing file.</done>
</task>

</tasks>

<verification>
After both tasks complete, run from the repo root the OSS-02g verification script (RESEARCH.md lines 542-549):

```bash
echo "=== Stale references check ===" && \
( ! grep -rEn "(blob/develop|develop branch|on develop|branches: \[develop\])" README.md && echo "PASS: no develop branch refs in README" ) && \
( ! grep -rn "maven.pkg.github.com" README.md && echo "PASS: no GPR URL in README" ) && \
( ! grep -rEn "TODO:|FIXME:" README.md .github/ 2>/dev/null && echo "PASS: no TODO/FIXME in public files" ) && \
( test ! -f SECURITY.md && echo "PASS: SECURITY.md deleted" ) && \
( ! grep -rE "T-12-[0-9]+|ASVS Level [12]|Phase 14" README.md CONTRIBUTING.md .github/ 2>/dev/null && echo "PASS: no internal audit content leaked" )
```

All five PASS lines must print.
</verification>

<success_criteria>
- README.md installation block uses Maven Central with all 9 module artifact IDs and the `ui-android` debug overlay
- README.md "Security Considerations" section removed
- README.md has a new Versioning section linking to GitHub Releases and CHANGELOG.md
- README.md preserves badges, tagline, modules table, Service Integrations, How-tos, Contributing, License
- SECURITY.md deleted; no internal Phase 14 audit content leaks into any other public file
- No `maven.pkg.github.com` references remain anywhere in README.md
</success_criteria>

<output>
After completion, create `.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-03-SUMMARY.md` describing:
- README.md surgical changes (installation block replaced, Security Considerations removed, Versioning section added) with line-count delta
- SECURITY.md deletion (162 lines removed)
- Verification script results (all 5 PASS lines)
- Confirmation that internal Phase 14 audit content does not appear anywhere
</output>

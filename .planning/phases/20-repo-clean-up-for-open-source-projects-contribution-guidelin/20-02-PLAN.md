---
phase: 20-repo-clean-up-for-open-source-projects-contribution-guidelin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements:
  - OSS-02
tags:
  - documentation
  - contributing
  - oss-readiness

must_haves:
  truths:
    - "A new contributor can clone the repo, install JDK 17, run jvmTest, and submit a conformant PR using only CONTRIBUTING.md (without reading CLAUDE.md)"
    - "CONTRIBUTING.md commit format rules match CLAUDE.md exactly: same types, same scopes, no emojis, no `resolves #N`, imperative mood, lowercase, no trailing period"
    - "CONTRIBUTING.md PR checklist matches the four items from CLAUDE.md PR checklist plus additive-only API"
    - "CONTRIBUTING.md warns that `connectedAndroidTest` requires a connected device"
    - "CONTRIBUTING.md states `main` is the integration branch and there is no `develop` branch"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Standalone human contributor guide aligned with CLAUDE.md and Conventional Commits"
      contains: "JDK 17"
      min_lines: 100
  key_links:
    - from: "CONTRIBUTING.md"
      to: "CLAUDE.md"
      via: "rules agreement (independent docs that must agree)"
      pattern: "Conventional Commits"
    - from: "CONTRIBUTING.md PR checklist section"
      to: ".github/PULL_REQUEST_TEMPLATE.md"
      via: "identical checklist items"
      pattern: "gradlew detekt"
---

<objective>
Replace `CONTRIBUTING.md` (currently 63 lines of outdated git-flow + emoji-commit content) with a standalone human contributor guide that mirrors every rule in CLAUDE.md (the Claude-agent-facing source of truth from Phase 19) without cross-referencing it.

Per D-01 and D-02: the new CONTRIBUTING.md must be self-contained — a contributor must be able to follow it from setup to merged PR without ever opening CLAUDE.md. Per D-02, the two documents are independent but every rule must agree exactly. Pitfall 6 in RESEARCH.md flags divergence as the highest-likelihood failure mode for this plan.

Purpose: Implement D-01 + D-02 — the third pillar of OSS-02 (CONTRIBUTING.md covers setup, coding standards, PR process). Delete and rewrite, do not patch.

Output: A new CONTRIBUTING.md covering 9 required sections (Prerequisites, Clone & build, Development workflow, Device-requiring tests, Module pattern overview, Commit format, Branch strategy, PR process, Code review).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-CONTEXT.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md
@CLAUDE.md
@CONTRIBUTING.md

<interfaces>
<!-- Content contracts. CLAUDE.md is the authoritative source; every rule in CONTRIBUTING.md MUST agree. -->

CLAUDE.md → CONTRIBUTING.md verbatim mirrors:

(1) Prerequisites (CLAUDE.md lines 7-12):
   - JDK 17 required
   - No Android device needed for default workflow
   - Smoke test: `./gradlew :kamper:api:test :kamper:engine:test`

(2) Build & test commands (CLAUDE.md lines 18-30) — mirror this 7-row table:
   - `./gradlew :kamper:modules:<name>:jvmTest` — single module JVM only — primary fast path
   - `./gradlew :kamper:modules:<name>:test` — single module all unit tests — includes androidUnitTest
   - `./gradlew :kamper:api:test` — API contracts
   - `./gradlew :kamper:engine:test` — Engine
   - `./gradlew test` — all modules all unit tests — full pre-commit sweep
   - `./gradlew detekt` — static analysis (zero-tolerance, maxIssues: 0)
   - `./gradlew connectedAndroidTest` — REQUIRES DEVICE — never run autonomously

(3) Conventional Commits format (CLAUDE.md lines 95-130):
   Allowed types: feat, fix, chore, docs, test, refactor
   Allowed scopes: cpu, fps, memory, network, issues, jank, gc, thermal, engine, api, ui, build, deps, phase<N>
   Rules: imperative, lowercase, no trailing period, no emojis, no resolves #N / fixes #N

(4) Branch strategy (CLAUDE.md line 131-134):
   - `main` is integration branch — no `develop`
   - Squash merge for PRs

(5) PR checklist (CLAUDE.md lines 137-141) — must equal the 4 items in PULL_REQUEST_TEMPLATE.md:
   - `./gradlew detekt` passes (zero issues)
   - `./gradlew :kamper:modules:<name>:jvmTest` passes for every touched module
   - No `TODO:` or `FIXME:` in changed files
   - Public API changes are additive only (no removals or signature breaks) [from D-04]

(6) Module pattern (CLAUDE.md lines 41-92) — summarize, do not duplicate verbatim:
   - 4-class structure: {Name}Info, {Name}Config, {Name}Watcher, {Name}Performance
   - INVALID sentinel on every Info subclass
   - Builder/DEFAULT pattern with @KamperDslMarker
   - Canonical reference module: kamper/modules/cpu/

Anti-patterns from current CONTRIBUTING.md (lines 1-63) — must NOT carry over:
- git-flow workflow (develop branch as integration target)
- Emoji-prefixed commits (feat: 🎉, fix: 🐛, etc.)
- "#ISSUE_ID" prefix on commits
- "git rebase -t" instruction
- "resolves #N" footer template
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CONTRIBUTING.md with standalone, CLAUDE.md-aligned contributor guide (D-01, D-02)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (current 63 lines — to confirm what is being entirely replaced)
    - CLAUDE.md (lines 1-226 — authoritative rules to mirror)
    - .planning/codebase/CONVENTIONS.md (naming, Detekt rules — supplementary context)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-CONTEXT.md (decisions D-01, D-02)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md (lines 295-308 — required sections list)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md (lines 24-110 — section templates)
  </read_first>
  <action>
    Overwrite `CONTRIBUTING.md` entirely with a new document. Per D-02, the new document must be standalone — a contributor follows it without ever opening CLAUDE.md. Per D-01, all 9 sections below are mandatory and must appear in this order.

    Required sections, with exact heading text and required content:

    **1. `# Contributing to Kamper`** — H1 title.

    **2. `## Prerequisites`**
    Must include:
    - JDK 17 required (state version explicitly)
    - No Android device required for default development workflow
    - Smoke test command on a fresh clone: `./gradlew :kamper:api:test :kamper:engine:test`

    **3. `## Clone and build`**
    Must include:
    - `git clone https://github.com/smellouk/kamper.git`
    - `cd kamper`
    - First-build smoke test (same as Prerequisites)

    **4. `## Development workflow`**
    Must include this exact table (or equivalent prose listing all 6 commands), mirrored from CLAUDE.md lines 18-30:

    | Command | Scope | When to use |
    |---------|-------|-------------|
    | `./gradlew :kamper:modules:<name>:jvmTest` | Single module, JVM only | Fast feedback (primary path) |
    | `./gradlew :kamper:modules:<name>:test` | Single module, all unit tests | Includes androidUnitTest |
    | `./gradlew :kamper:api:test` | API contracts | After modifying api layer |
    | `./gradlew :kamper:engine:test` | Engine | After modifying engine layer |
    | `./gradlew test` | All modules, all unit tests | Full pre-commit sweep |
    | `./gradlew detekt` | Static analysis (zero-tolerance) | Before every commit |

    State explicitly: detekt has `maxIssues: 0` — any new issue fails the build.

    **5. `## Device-requiring tests`**
    Must include the literal warning paragraph (mirror CLAUDE.md lines 32-34):

    > **`./gradlew connectedAndroidTest` requires a connected Android device or running emulator.**
    > It will fail in any environment without one. Confirm a device is attached before running.

    **6. `## Module pattern`**
    Must include:
    - The 4-class structure listing: `{Name}Info`, `{Name}Config`, `{Name}Watcher`, `{Name}Performance` with their roles and visibility (public/public/internal/internal)
    - Mention that every `Info` subclass has a companion `INVALID` constant
    - Mention `@KamperDslMarker` Builder + `DEFAULT` companion val pattern
    - Reference `kamper/modules/cpu/` as the canonical example

    **7. `## Commit format`**
    Must include EXACTLY these strings (no emoji, no resolves #N):

    ```
    <type>(<scope>): <short description>
    ```

    Allowed types: `feat`, `fix`, `chore`, `docs`, `test`, `refactor`

    Allowed scopes: `cpu`, `fps`, `memory`, `network`, `issues`, `jank`, `gc`, `thermal`, `engine`, `api`, `ui`, `build`, `deps`, `phase<N>`

    Rules:
    - Imperative mood, lowercase after the colon, no trailing period
    - No emojis anywhere in commit messages
    - No `resolves #N` or `fixes #N` footers
    - No `#ISSUE_ID` prefix

    Include 3-6 example commits using real Kamper scopes:

    ```
    feat(cpu): add thermal throttle detection
    fix(engine): guard against duplicate module install
    chore(deps): bump coroutines to 1.10.1
    docs(api): update Watcher KDoc
    test(fps): add FpsWatcher interval test
    refactor(memory): extract MemoryInfoMapper
    ```

    **8. `## Branch strategy`**
    Must include:
    - `main` is the integration branch
    - There is no `develop` branch
    - PRs are squash-merged

    **9. `## Pull request process`**
    Must include the EXACT same 4 checklist items as `.github/PULL_REQUEST_TEMPLATE.md`:

    - [ ] `./gradlew detekt` passes (zero issues)
    - [ ] `./gradlew :kamper:modules:<name>:jvmTest` passes for every touched module
    - [ ] No `TODO:` or `FIXME:` comments in changed files
    - [ ] Public API changes are additive only (no removals or signature breaks)

    **10. `## Code review`**
    Brief section noting:
    - Single maintainer; please be patient
    - Reviewers check the PR checklist above plus module-pattern adherence
    - Use the `kamper-module-review` Claude skill (mention is optional) or self-review against `kamper/modules/cpu/`

    Forbidden content (must not appear anywhere):
    - The word `develop` in the context of a branch name (other than the verb "to develop")
    - Any emoji character
    - The string `resolves #` or `fixes #`
    - The string `git-flow` or `gitflow`
    - The string `git rebase -t` (an old instruction)
    - The string `#ISSUE_ID`
    - Any reference to `maven.pkg.github.com`
    - The string `TODO:` or `FIXME:` outside of the PR-checklist context (i.e. it must appear inside backticks ``TODO:`` only as a forbidden-content example)

    Length target: 100-200 lines. The current file (63 lines) is too sparse; the goal is a self-contained doc.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q "JDK 17" CONTRIBUTING.md && grep -q "main" CONTRIBUTING.md && grep -qE "feat\(cpu\)" CONTRIBUTING.md && grep -q "gradlew detekt" CONTRIBUTING.md && grep -q "additive only" CONTRIBUTING.md && grep -q "INVALID" CONTRIBUTING.md && grep -q "Conventional Commits\|conventional commits\|<type>(<scope>)" CONTRIBUTING.md && grep -cE "🎉|🐛|💚|✅|🚀|📄|🧪|📷" CONTRIBUTING.md | grep -q "^0$" && ! grep -E "(blob/develop|branches?[^.]*develop|develop branch|on develop)" CONTRIBUTING.md && ! grep -q "resolves #" CONTRIBUTING.md && ! grep -q "fixes #" CONTRIBUTING.md && ! grep -q "git rebase -t" CONTRIBUTING.md && ! grep -q "#ISSUE_ID" CONTRIBUTING.md && ! grep -q "maven.pkg.github.com" CONTRIBUTING.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CONTRIBUTING.md` exits 0
    - `wc -l CONTRIBUTING.md` reports >= 100 lines
    - `grep -c "^# Contributing to Kamper" CONTRIBUTING.md` returns 1
    - `grep -c "^## Prerequisites" CONTRIBUTING.md` returns 1
    - `grep -c "^## Clone and build" CONTRIBUTING.md` returns 1
    - `grep -c "^## Development workflow" CONTRIBUTING.md` returns 1
    - `grep -c "^## Device-requiring tests" CONTRIBUTING.md` returns 1
    - `grep -c "^## Module pattern" CONTRIBUTING.md` returns 1
    - `grep -c "^## Commit format" CONTRIBUTING.md` returns 1
    - `grep -c "^## Branch strategy" CONTRIBUTING.md` returns 1
    - `grep -c "^## Pull request process" CONTRIBUTING.md` returns 1
    - `grep -c "^## Code review" CONTRIBUTING.md` returns 1
    - `grep -c "JDK 17" CONTRIBUTING.md` >= 1
    - `grep -c "gradlew :kamper:api:test" CONTRIBUTING.md` >= 1
    - `grep -c "gradlew :kamper:modules:<name>:jvmTest" CONTRIBUTING.md` >= 1
    - `grep -c "gradlew detekt" CONTRIBUTING.md` >= 1
    - `grep -c "connectedAndroidTest" CONTRIBUTING.md` >= 1
    - `grep -c "INVALID" CONTRIBUTING.md` >= 1
    - `grep -c "@KamperDslMarker\|Builder\|DEFAULT" CONTRIBUTING.md` >= 1
    - `grep -c "kamper/modules/cpu" CONTRIBUTING.md` >= 1
    - All 6 allowed commit types are present: `grep -cE "(feat|fix|chore|docs|test|refactor)" CONTRIBUTING.md` >= 6
    - At least 4 of the 14 allowed scopes are present in real example commits: `grep -cE "(cpu|fps|memory|engine|api|deps)" CONTRIBUTING.md` >= 4
    - `grep -c "additive only" CONTRIBUTING.md` >= 1
    - `grep -c "squash" CONTRIBUTING.md` >= 1 (squash-merge branch strategy)
    - `grep -cE "🎉|🐛|💚|✅|🚀|📄|🧪|📷|❤" CONTRIBUTING.md` returns 0 (no emoji)
    - `! grep -q "resolves #" CONTRIBUTING.md` (true)
    - `! grep -q "fixes #" CONTRIBUTING.md` (true)
    - `! grep -q "git rebase -t" CONTRIBUTING.md` (true)
    - `! grep -q "#ISSUE_ID" CONTRIBUTING.md` (true)
    - `! grep -q "maven.pkg.github.com" CONTRIBUTING.md` (true)
    - `! grep -qE "(blob/develop|on develop|develop branch|branches: \[develop\])" CONTRIBUTING.md` (true) — "develop" must not appear as a branch name (the verb form may appear, e.g. "developers")
    - The 4 PR checklist items are byte-identical to those in `.github/PULL_REQUEST_TEMPLATE.md` (verifiable by: `for item in "gradlew detekt" "gradlew :kamper:modules:<name>:jvmTest" "TODO:" "additive only"; do grep -q "$item" CONTRIBUTING.md && grep -q "$item" .github/PULL_REQUEST_TEMPLATE.md || echo "MISMATCH on: $item"; done` — produces no MISMATCH lines)
  </acceptance_criteria>
  <done>CONTRIBUTING.md is replaced with a standalone, 100+ line guide containing all 9 D-01 sections in order, with rules byte-equivalent to CLAUDE.md and PR checklist items byte-equivalent to `.github/PULL_REQUEST_TEMPLATE.md`. No emojis, no `develop` branch references, no `resolves #N` template.</done>
</task>

</tasks>

<verification>
After the task completes, run from the repo root:

```bash
# All 9 sections present in order
for section in "# Contributing to Kamper" "## Prerequisites" "## Clone and build" "## Development workflow" "## Device-requiring tests" "## Module pattern" "## Commit format" "## Branch strategy" "## Pull request process" "## Code review"; do
  grep -c "^$section" CONTRIBUTING.md | xargs -I{} test {} = 1 || echo "MISSING: $section"
done

# Cross-doc agreement with CLAUDE.md (sample of high-risk items)
diff <(grep -oE "feat|fix|chore|docs|test|refactor" CONTRIBUTING.md | sort -u) <(echo -e "chore\ndocs\nfeat\nfix\nrefactor\ntest")
# (must produce no diff output)

# Cross-doc agreement with PULL_REQUEST_TEMPLATE.md
for item in "gradlew detekt" "gradlew :kamper:modules:<name>:jvmTest" "TODO:" "additive only"; do
  grep -q "$item" CONTRIBUTING.md && grep -q "$item" .github/PULL_REQUEST_TEMPLATE.md && echo "MATCH: $item" || echo "MISMATCH: $item"
done

# Stale-reference sweep
! grep -E "🎉|🐛|💚|✅|🚀|📄|🧪|📷|❤|resolves #|fixes #|git rebase -t|#ISSUE_ID|maven.pkg.github.com|blob/develop|develop branch" CONTRIBUTING.md && echo "PASS: no stale content"
```
</verification>

<success_criteria>
- CONTRIBUTING.md fully replaced (current 63 lines → ≥100 lines covering 9 mandatory sections)
- Every commit type and rule in CONTRIBUTING.md matches CLAUDE.md exactly
- PR checklist in CONTRIBUTING.md matches `.github/PULL_REQUEST_TEMPLATE.md` byte-for-byte
- Zero emojis, zero `develop` branch references, zero outdated git-flow content
- Self-contained: a new contributor can follow it from setup to PR without consulting any other doc
</success_criteria>

<output>
After completion, create `.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-02-SUMMARY.md` describing:
- File replaced (line count before → after)
- Cross-doc verification result (CLAUDE.md and PULL_REQUEST_TEMPLATE.md agreement)
- Any sections expanded beyond the minimum requirements (note Claude's discretion areas: optional CODE_OF_CONDUCT.md mention, prose ordering)
</output>

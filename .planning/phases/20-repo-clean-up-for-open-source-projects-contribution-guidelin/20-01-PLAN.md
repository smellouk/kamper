---
phase: 20-repo-clean-up-for-open-source-projects-contribution-guidelin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug-report.yml
  - .github/ISSUE_TEMPLATE/feature-request.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - OSS-02
tags:
  - documentation
  - github-templates
  - oss-readiness

must_haves:
  truths:
    - "Bug reports follow a structured GitHub Forms layout that requires platform, version, module, reproduction steps, expected and actual behavior"
    - "Feature requests follow a structured GitHub Forms layout requiring use case"
    - "PR template uses no emojis, no `develop` branch references, and matches the CLAUDE.md PR checklist exactly (detekt, jvmTest, no TODO/FIXME, additive-only API)"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug-report.yml"
      provides: "Structured GitHub Forms bug-report template"
      contains: "name: Bug Report"
    - path: ".github/ISSUE_TEMPLATE/feature-request.yml"
      provides: "Structured GitHub Forms feature-request template"
      contains: "name: Feature Request"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Conventions-aligned PR template (replacement)"
      contains: "Public API changes are additive only"
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "CLAUDE.md PR checklist (lines 135-143)"
      via: "verbatim checklist items"
      pattern: "gradlew detekt"
    - from: ".github/ISSUE_TEMPLATE/bug-report.yml"
      to: "Kamper supported platforms"
      via: "platform dropdown options"
      pattern: "Android|iOS|JVM|macOS|tvOS|JS|WasmJS"
---

<objective>
Create the three GitHub-side template files that GitHub renders when contributors open issues or pull requests against Kamper. These three files are the contributor-facing entry points to the repository — they encode our conventions (Conventional Commits, detekt zero-tolerance, additive-only API, no `develop` branch) into structured input forms.

Purpose: Implement D-03 (issue templates) and D-04 (PR template replacement) — open-source readiness requirement OSS-02 cannot be met without them.

Output: Two new GitHub Forms YAML issue templates and a complete replacement of the existing emoji-laden, develop-referencing PR template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-CONTEXT.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md
@.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md
@CLAUDE.md
@.github/PULL_REQUEST_TEMPLATE.md

<interfaces>
<!-- These are not code interfaces but content contracts. Executors MUST treat them as load-bearing strings. -->

CLAUDE.md PR checklist (lines 135-143) — VERBATIM strings to mirror in PULL_REQUEST_TEMPLATE.md:
- "`./gradlew detekt` passes (zero issues)"
- "`./gradlew :kamper:modules:<name>:jvmTest` passes for every touched module"
- "No `TODO:` or `FIXME:` comments in changed files"
- (D-04 adds) "Public API changes are additive only (no removals or signature breaks)"

Kamper supported platforms (from CLAUDE.md line 3) — dropdown options for bug-report.yml platform field:
Android, iOS, JVM, macOS, tvOS, JS, WasmJS

Kamper version (canonical, from gradle.properties + .release-please-manifest.json): 1.0.0 (use as placeholder only)

Existing PR template anti-patterns to NOT preserve (.github/PULL_REQUEST_TEMPLATE.md current content):
- Emoji headers: `🚀`, `🧪`, `✅`, `📷`, `📄`
- `blob/develop/CONTRIBUTING.md` link (line 19)
- `blob/develop/README.md` link (line 23)
- "I have used and tested this on my device(s)" (irrelevant to Kamper PR process)
- "I have added unit tests to cover the changes." (replaced by additive-only API rule)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bug-report and feature-request GitHub Forms templates (D-03)</name>
  <files>.github/ISSUE_TEMPLATE/bug-report.yml, .github/ISSUE_TEMPLATE/feature-request.yml</files>
  <read_first>
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md (lines 173-278 — full template patterns)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md (lines 113-231 — verbatim YAML to use)
    - CLAUDE.md (line 3 — confirms Android, iOS, JVM, macOS, tvOS, JS, WasmJS as supported platforms)
  </read_first>
  <action>
    Create the directory `.github/ISSUE_TEMPLATE/` if it does not already exist (it currently does not — `.github/` only contains `PULL_REQUEST_TEMPLATE.md` and `workflows/`).

    Then create TWO files. Use these exact contents (per D-03, RESEARCH.md schema, and PATTERNS.md):

    **File A: `.github/ISSUE_TEMPLATE/bug-report.yml`** (verbatim — note hyphenated filename per VALIDATION.md sampling map):

    ```yaml
    name: Bug Report
    description: Report a reproducible bug in Kamper
    title: "[Bug]: "
    labels: ["bug", "triage"]
    body:
      - type: markdown
        attributes:
          value: |
            Thanks for taking the time to report a bug.
            Before opening, please search existing issues.

      - type: textarea
        id: description
        attributes:
          label: Description
          description: A clear description of what the bug is.
        validations:
          required: true

      - type: textarea
        id: reproduction
        attributes:
          label: Reproduction steps
          description: Minimal code or steps to reproduce the issue.
          placeholder: |
            1. Install CpuModule
            2. Call Kamper.start()
            3. ...
        validations:
          required: true

      - type: textarea
        id: expected
        attributes:
          label: Expected behavior
        validations:
          required: true

      - type: textarea
        id: actual
        attributes:
          label: Actual behavior
        validations:
          required: true

      - type: dropdown
        id: platform
        attributes:
          label: Platform
          options:
            - Android
            - iOS
            - JVM
            - macOS
            - tvOS
            - JS
            - WasmJS
        validations:
          required: true

      - type: input
        id: kamper_version
        attributes:
          label: Kamper version
          placeholder: "1.0.0"
        validations:
          required: true

      - type: input
        id: module
        attributes:
          label: Module(s) affected
          placeholder: "cpu, fps, memory, ..."
    ```

    **File B: `.github/ISSUE_TEMPLATE/feature-request.yml`** (verbatim):

    ```yaml
    name: Feature Request
    description: Propose a new feature or behavior change
    title: "[Feature]: "
    labels: ["enhancement"]
    body:
      - type: textarea
        id: use_case
        attributes:
          label: Use case
          description: What problem does this solve?
        validations:
          required: true

      - type: textarea
        id: proposed_api
        attributes:
          label: Proposed API or behavior
          description: How would a developer use this feature?
        validations:
          required: false

      - type: textarea
        id: alternatives
        attributes:
          label: Alternatives considered
        validations:
          required: false
    ```

    Do not introduce any other fields. Do not add a `config.yml`. Do not add `assignees`.
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug-report.yml && test -f .github/ISSUE_TEMPLATE/feature-request.yml && grep -q "name: Bug Report" .github/ISSUE_TEMPLATE/bug-report.yml && grep -q "name: Feature Request" .github/ISSUE_TEMPLATE/feature-request.yml && grep -q "WasmJS" .github/ISSUE_TEMPLATE/bug-report.yml && grep -q "label: Use case" .github/ISSUE_TEMPLATE/feature-request.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/ISSUE_TEMPLATE/bug-report.yml` exits 0
    - `test -f .github/ISSUE_TEMPLATE/feature-request.yml` exits 0
    - `grep -c "name: Bug Report" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "name: Feature Request" .github/ISSUE_TEMPLATE/feature-request.yml` returns 1
    - `grep -c "labels: \[\"bug\", \"triage\"\]" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "labels: \[\"enhancement\"\]" .github/ISSUE_TEMPLATE/feature-request.yml` returns 1
    - `grep -c "Android" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1 AND `grep -c "WasmJS" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1 (all 7 KMP targets present)
    - `grep -c "label: Description" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Reproduction steps" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Expected behavior" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Actual behavior" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Kamper version" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Module(s) affected" .github/ISSUE_TEMPLATE/bug-report.yml` returns 1
    - `grep -c "label: Use case" .github/ISSUE_TEMPLATE/feature-request.yml` returns 1
    - `grep -c "label: Proposed API or behavior" .github/ISSUE_TEMPLATE/feature-request.yml` returns 1
    - `grep -c "label: Alternatives considered" .github/ISSUE_TEMPLATE/feature-request.yml` returns 1
    - Neither file contains a `config.yml` reference, an `assignees:` field, or any emoji character
  </acceptance_criteria>
  <done>Both `.yml` files exist under `.github/ISSUE_TEMPLATE/`, conform to GitHub Forms schema, cover all required fields per D-03, and use only ASCII (no emoji).</done>
</task>

<task type="auto">
  <name>Task 2: Replace .github/PULL_REQUEST_TEMPLATE.md (D-04)</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE.md (current file — to confirm what is being entirely replaced)
    - CLAUDE.md (lines 135-143 — authoritative PR checklist source)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-PATTERNS.md (lines 235-256 — full replacement pattern)
    - .planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-RESEARCH.md (lines 280-293 — verbatim PR template)
  </read_first>
  <action>
    Overwrite `.github/PULL_REQUEST_TEMPLATE.md` with the EXACT following content (no other content preserved from the current file). Per D-04, this is a full replacement, not a patch.

    **Exact file content:**

    ```markdown
    ## What changed and why
    <!-- Describe your change and the motivation behind it. -->

    ## Checklist
    - [ ] `./gradlew detekt` passes (zero issues)
    - [ ] `./gradlew :kamper:modules:<name>:jvmTest` passes for every touched module
    - [ ] No `TODO:` or `FIXME:` in changed files
    - [ ] Public API changes are additive only (no removals or signature breaks)
    ```

    Constraints (must be true after writing):
    - File contains zero emojis. The current file has 5 emojis (🚀, 📄, 🧪, ✅, 📷) — none must remain.
    - File contains zero references to the `develop` branch. The current file has two `blob/develop/` URLs — both must be gone.
    - File contains zero references to "device" or "tested this on my device(s)" — that line is removed.
    - File contains zero links to `CONTRIBUTING.md` or `README.md` — the new template is self-contained.
    - File contains exactly two `##` section headings: `## What changed and why` and `## Checklist`.
    - File contains exactly four checklist items, each starting with `- [ ]`.
  </action>
  <verify>
    <automated>grep -cE "🚀|📄|🧪|✅|📷|❤" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^0$" && grep -c "develop" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^0$" && grep -c "## What changed and why" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -c "## Checklist" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -c "gradlew detekt" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -c "gradlew :kamper:modules:<name>:jvmTest" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -c "TODO:" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -c "additive only" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^1$" && grep -cE "^- \[ \]" .github/PULL_REQUEST_TEMPLATE.md | grep -q "^4$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "🚀|📄|🧪|✅|📷|❤" .github/PULL_REQUEST_TEMPLATE.md` returns 0 (no emojis)
    - `grep -c "develop" .github/PULL_REQUEST_TEMPLATE.md` returns 0 (no develop branch references)
    - `grep -c "device" .github/PULL_REQUEST_TEMPLATE.md` returns 0 (no device-test boilerplate)
    - `grep -c "## What changed and why" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -c "## Checklist" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -cE "^- \[ \]" .github/PULL_REQUEST_TEMPLATE.md` returns 4 (exactly four checklist items)
    - `grep -c "gradlew detekt" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -c "gradlew :kamper:modules:<name>:jvmTest" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -c "TODO:" .github/PULL_REQUEST_TEMPLATE.md` returns 1 (in checklist context only)
    - `grep -c "additive only" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `wc -l .github/PULL_REQUEST_TEMPLATE.md` reports between 6 and 10 lines (the new template is short)
  </acceptance_criteria>
  <done>The PR template contains exactly the four checklist items mirrored from CLAUDE.md PR checklist, has the "What changed and why" heading, and contains zero emojis, zero `develop` references, and zero leftover content from the previous file.</done>
</task>

</tasks>

<verification>
After all tasks complete, run from the repo root:

```bash
# All three files exist
test -f .github/ISSUE_TEMPLATE/bug-report.yml && \
test -f .github/ISSUE_TEMPLATE/feature-request.yml && \
test -f .github/PULL_REQUEST_TEMPLATE.md && echo "PASS: files exist"

# Stale-reference sweep across all created files
! grep -rE "🚀|📄|🧪|✅|📷|❤|develop" .github/ISSUE_TEMPLATE/ .github/PULL_REQUEST_TEMPLATE.md && \
echo "PASS: no stale references"

# PR template checklist mirrors CLAUDE.md
grep -c "gradlew detekt" .github/PULL_REQUEST_TEMPLATE.md
grep -c "gradlew :kamper:modules:<name>:jvmTest" .github/PULL_REQUEST_TEMPLATE.md
grep -c "TODO:" .github/PULL_REQUEST_TEMPLATE.md
grep -c "additive only" .github/PULL_REQUEST_TEMPLATE.md
# All four greps must return 1.
```
</verification>

<success_criteria>
- `.github/ISSUE_TEMPLATE/bug-report.yml` exists with all D-03 required fields and 7 KMP platforms in dropdown
- `.github/ISSUE_TEMPLATE/feature-request.yml` exists with use_case, proposed_api, alternatives sections
- `.github/PULL_REQUEST_TEMPLATE.md` is fully replaced with the conventions-aligned 4-item checklist
- Zero emojis remain in any of the three files
- Zero references to the `develop` branch remain in any of the three files
- Checklist items mirror CLAUDE.md PR checklist (lines 135-143) verbatim plus the additive-only API item from D-04
</success_criteria>

<output>
After completion, create `.planning/phases/20-repo-clean-up-for-open-source-projects-contribution-guidelin/20-01-SUMMARY.md` describing:
- Three files created/replaced
- Verification commands and their PASS results
- Any deviations from the plan (expected: none)
</output>

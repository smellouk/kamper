---
phase: 19-make-repo-claude-friend-and-research-which-skill-we-could-of
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - .claude/skills/kamper-check/SKILL.md
  - .claude/skills/kamper-module-review/SKILL.md
  - .claude/settings.json
autonomous: true
requirements:
  - OSS-01
tags:
  - documentation
  - claude
  - skills
  - oss
  - settings

must_haves:
  truths:
    - ".claude/skills/kamper-check/SKILL.md exists with the 4 standard project-skill sections"
    - "kamper-check skill distinguishes fast (jvmTest, no device) from slow (connectedAndroidTest, requires device) paths"
    - "kamper-check skill explicitly tells the agent NOT to run connectedAndroidTest in autonomous Claude sessions"
    - ".claude/skills/kamper-module-review/SKILL.md exists with the 4 standard project-skill sections"
    - "kamper-module-review checklist covers naming, expect/actual completeness across all 8 platforms, INVALID sentinel, Builder/DEFAULT pattern, test coverage, safety try/catch rule"
    - ".claude/settings.json allow list extended with safe Gradle commands (jvmTest, test, detekt) but NOT connectedAndroidTest"
    - ".claude/settings.json remains valid JSON after modification"
  artifacts:
    - path: ".claude/skills/kamper-check/SKILL.md"
      provides: "Slash-command skill that runs the right Gradle check command per scope"
      min_lines: 80
      contains:
        - "## Purpose"
        - "## When to Use"
        - "## Steps"
        - "## Reference"
        - "jvmTest"
        - "connectedAndroidTest"
        - "detekt"
        - "device"
    - path: ".claude/skills/kamper-module-review/SKILL.md"
      provides: "Slash-command skill that reviews a Kamper module against conventions"
      min_lines: 100
      contains:
        - "## Purpose"
        - "## When to Use"
        - "## Steps"
        - "## Reference"
        - "INVALID"
        - "Builder"
        - "DEFAULT"
        - "expect"
        - "actual"
        - "try/catch"
        - "androidMain"
        - "iosMain"
        - "jvmMain"
        - "macosMain"
        - "tvosMain"
        - "jsMain"
        - "wasmJsMain"
    - path: ".claude/settings.json"
      provides: "Extended allowlist for safe Gradle test/detekt commands"
      contains:
        - "jvmTest"
        - "detekt"
  key_links:
    - from: ".claude/skills/kamper-check/SKILL.md"
      to: ".claude/settings.json"
      via: "documents the Gradle commands now allowed"
      pattern: ".claude/settings.json"
    - from: ".claude/skills/kamper-module-review/SKILL.md"
      to: ".planning/codebase/CONVENTIONS.md"
      via: "review checklist mirrors conventions"
      pattern: "CONVENTIONS"
    - from: ".claude/settings.json"
      to: "Gradle"
      via: "Bash allowlist entries"
      pattern: "gradlew"
---

<objective>
Create the remaining two project skills (`kamper-check` and `kamper-module-review`) and extend the existing `.claude/settings.json` allowlist with safe Gradle commands so future Claude sessions can run JVM unit tests and Detekt without permission prompts. `connectedAndroidTest` and other device-requiring tasks are deliberately NOT added.

Purpose:
- `kamper-check` gives the agent a single command-discovery skill so it never guesses Gradle task paths or accidentally tries `connectedAndroidTest`.
- `kamper-module-review` codifies the convention checklist so any module can be reviewed mechanically against the same standards.
- The settings.json extension closes the gap identified in RESEARCH.md (open question 1) so the skills can be invoked end-to-end without permission interruptions for the common cases.

Output: 3 files modified — 2 new SKILL.md files, 1 settings.json edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-CONTEXT.md
@.planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@CLAUDE.md
@.claude/settings.json

<interfaces>
<!-- Verbatim Gradle command reference from RESEARCH.md, verified against TESTING.md + .claude/settings.json -->

Gradle command table:
  ./gradlew :kamper:modules:<name>:jvmTest    Single module JVM         No device   Fast feedback (primary path for Claude)
  ./gradlew :kamper:modules:<name>:test       Single module all unit    No device   Includes androidUnitTest (MockK, JVM)
  ./gradlew :kamper:api:test                  API contracts             No device   After modifying api layer
  ./gradlew :kamper:engine:test               Engine                    No device   After modifying engine layer
  ./gradlew test                              All modules unit tests    No device   Full pre-commit sweep
  ./gradlew detekt                            Static analysis           No device   Before every commit (zero-tolerance)
  ./gradlew connectedAndroidTest              Instrumented              YES device  NEVER in CI without device; SKIP in Claude sessions
  ./gradlew assembleXCFramework               iOS/macOS XCFramework     No device   Only when publishing iOS artifacts

Convention checklist for kamper-module-review (verbatim from RESEARCH.md):

### Naming
- [ ] Module directory: kamper/modules/{name}/
- [ ] Package: com.smellouk.kamper.{name}
- [ ] Info class: {Name}Info (data class, implements Info)
- [ ] Config class: {Name}Config (data class, implements Config)
- [ ] Watcher class: {Name}Watcher (internal, extends Watcher<{Name}Info>)
- [ ] Performance class: {Name}Performance (internal, extends Performance<...>)

### Completeness
- [ ] {Name}Info.companion.INVALID exists
- [ ] {Name}Config.Builder object with build() function
- [ ] {Name}Config.DEFAULT companion val
- [ ] expect val {Name}Module in commonMain/Module.kt
- [ ] actual val {Name}Module in ALL 7 platform actual source sets: androidMain, iosMain, jvmMain, macosMain, tvosMain, jsMain, wasmJsMain
- [ ] Registered in settings.gradle.kts

### Safety
- [ ] All platform calls in XxxInfoSource wrapped in try/catch
- [ ] Watcher loop uses while (isActive), not while (true)
- [ ] No println() in production code — uses logger.log()
- [ ] No TODO: or FIXME: comments (Detekt will fail)

### Testing
- [ ] {Name}ConfigBuilderTest in commonTest
- [ ] {Name}InfoMapperTest in commonTest
- [ ] Platform source test in androidTest or jvmTest

Existing .claude/settings.json (read in context):
{
  "permissions": {
    "allow": [
      "Bash(date:*)", "Bash(echo:*)", "Bash(cat:*)", "Bash(ls:*)", "Bash(mkdir:*)",
      "Bash(wc:*)", "Bash(head:*)", "Bash(tail:*)", "Bash(sort:*)", "Bash(grep:*)",
      "Bash(tr:*)", "Bash(git add:*)", "Bash(git commit:*)", "Bash(git status:*)",
      "Bash(git log:*)", "Bash(git diff:*)", "Bash(git tag:*)"
    ]
  }
}

New entries to ADD to allow list (per RESEARCH.md open question 1 recommendation):
  Bash(./gradlew *:jvmTest:*)
  Bash(./gradlew *:test:*)
  Bash(./gradlew :kamper:api:test:*)
  Bash(./gradlew :kamper:engine:test:*)
  Bash(./gradlew test:*)
  Bash(./gradlew detekt:*)
  Bash(test:*)
  Bash(find:*)

Entries to NOT ADD (deliberate exclusions — require device or are out of scope):
  - connectedAndroidTest
  - assembleXCFramework
  - publish
  - any deploy/release tasks
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create kamper-check skill SKILL.md</name>
  <files>.claude/skills/kamper-check/SKILL.md</files>
  <read_first>
    - .planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-RESEARCH.md
    - .planning/codebase/TESTING.md
    - .claude/settings.json
    - CLAUDE.md
  </read_first>
  <action>
First, create the directory: `mkdir -p .claude/skills/kamper-check`.

Then create `.claude/skills/kamper-check/SKILL.md` with the 4 standard project-skill sections. The skill must distinguish fast (no-device) commands from slow (device-required) commands and embed the verbatim 8-row Gradle command table from `<interfaces>`.

Required structure (heading text MUST match these strings so grep gates pass):

```
# kamper-check

## Purpose
One sentence: documents the right Gradle command for any check scope (single module fast,
single module full, all modules, detekt) and explicitly flags `connectedAndroidTest` as
device-required so Claude never invokes it autonomously.

## When to Use
- After modifying any production code, before committing.
- When the user asks "run the tests" or "run check" — pick the narrowest scope first.
- When the agent is unsure which Gradle task path is correct.
- Pick the scope by what was modified:
  - Single module file then `./gradlew :kamper:modules:<name>:jvmTest`
  - api/engine then `./gradlew :kamper:api:test` or `./gradlew :kamper:engine:test`
  - Multiple modules then `./gradlew test`
  - Always followed by `./gradlew detekt` before committing

## Steps
1. **Identify the scope** — single module / api / engine / multi-module.
2. **Pick the fast path first** — `:kamper:modules:<name>:jvmTest` for a single module.
   This runs ONLY the JVM unit tests (typically 1-3 seconds per module). Prefer this for
   fast feedback during iterative development.
3. **Run the chosen command** from the project root: `./gradlew <task-path>`.
4. **If green and the change is non-trivial**, run the full unit test sweep: `./gradlew test`.
5. **Always run detekt before committing**: `./gradlew detekt`. Zero-tolerance, build fails on any issue.
6. **DO NOT run `./gradlew connectedAndroidTest`** — this requires a connected device or
   running emulator. It will fail in autonomous Claude sessions. If the user explicitly
   asks for instrumented tests, ask them to confirm a device is attached first.
7. **Report results** — include the test count, duration, and any failures.

## Reference

### Command table (verified against TESTING.md and .claude/settings.json)
| Command | Scope | Device needed? | When to use |
|---------|-------|----------------|-------------|
| `./gradlew :kamper:modules:<name>:jvmTest` | Single module JVM | No | Fast feedback - primary path for Claude |
| `./gradlew :kamper:modules:<name>:test` | Single module all unit tests | No | Includes androidUnitTest (MockK, JVM) |
| `./gradlew :kamper:api:test` | API contracts | No | After modifying api layer |
| `./gradlew :kamper:engine:test` | Engine | No | After modifying engine layer |
| `./gradlew test` | All modules all unit tests | No | Full pre-commit sweep |
| `./gradlew detekt` | Static analysis | No | Before every commit (zero-tolerance) |
| `./gradlew connectedAndroidTest` | Instrumented (on-device) | YES - emulator or physical | NEVER in CI without device; SKIP in Claude sessions |
| `./gradlew assembleXCFramework` | iOS/macOS XCFramework | No (compiles on host) | Only when publishing iOS artifacts |

### Why jvmTest is preferred
- No Android SDK / emulator required
- Runs in 1-3 seconds per module
- Covers all `commonTest` sources via the JVM target
- The jvmTest target is the fast, deterministic feedback loop for iterative work

### connectedAndroidTest - explicitly OFF-LIMITS
This task requires a connected Android device or running emulator. In a Claude Code
autonomous session, no such device is attached, so this task will FAIL. Do not run it
unless the user has explicitly confirmed a device is attached.

### Settings allowlist
The following Gradle commands are pre-approved in `.claude/settings.json` (no permission
prompt required):
- `./gradlew *:jvmTest:*`
- `./gradlew *:test:*`
- `./gradlew :kamper:api:test`
- `./gradlew :kamper:engine:test`
- `./gradlew test`
- `./gradlew detekt`

Anything else (e.g. `connectedAndroidTest`, `assembleXCFramework`, `publish`) will trigger
a permission prompt; consider it a sign you may be reaching for the wrong tool.
```

Total length: target 80-140 lines.
  </action>
  <verify>
    <automated>test -f .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "^## Purpose" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "^## When to Use" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "^## Steps" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "^## Reference" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "jvmTest" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "connectedAndroidTest" .claude/skills/kamper-check/SKILL.md &amp;&amp; grep -q "detekt" .claude/skills/kamper-check/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/kamper-check/SKILL.md` exits 0
    - `grep -cE "^## (Purpose|When to Use|Steps|Reference)" .claude/skills/kamper-check/SKILL.md` returns 4
    - `grep -q "jvmTest" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q "connectedAndroidTest" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q "detekt" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q "device" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q ":kamper:modules:" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q ":kamper:api:test" .claude/skills/kamper-check/SKILL.md` succeeds
    - `grep -q ":kamper:engine:test" .claude/skills/kamper-check/SKILL.md` succeeds
    - `wc -l .claude/skills/kamper-check/SKILL.md | awk '{print ($1 >= 80)}'` returns 1
    - `grep -c "^\`\`\`" .claude/skills/kamper-check/SKILL.md` returns an even number
    - `grep -cE "[\xF0-\xF7][\x80-\xBF]{3}" .claude/skills/kamper-check/SKILL.md || true` returns 0
  </acceptance_criteria>
  <done>kamper-check SKILL.md created with 4 sections, distinguishes fast from slow commands, explicitly excludes connectedAndroidTest, no emojis.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create kamper-module-review skill SKILL.md</name>
  <files>.claude/skills/kamper-module-review/SKILL.md</files>
  <read_first>
    - .planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-RESEARCH.md
    - .planning/codebase/CONVENTIONS.md
    - .planning/codebase/ARCHITECTURE.md
    - kamper/modules/cpu/build.gradle.kts
    - CLAUDE.md
  </read_first>
  <action>
First, create the directory: `mkdir -p .claude/skills/kamper-module-review`.

Then create `.claude/skills/kamper-module-review/SKILL.md` with the 4 standard project-skill sections. The skill must contain the full convention checklist (Naming / Completeness / Safety / Testing) verbatim from `<interfaces>`.

Required structure (heading text MUST match these strings so grep gates pass):

```
# kamper-module-review

## Purpose
One sentence: reviews a Kamper module against the project's locked conventions - naming
patterns, 4-class structure, INVALID sentinel, Builder/DEFAULT pattern, expect/actual
completeness across all 7 platform actual source sets, safety try/catch rule, and test
coverage - and emits a pass/fail checklist with file references.

## When to Use
- User invokes `/kamper-module-review <name>` where <name> is the module directory under `kamper/modules/`.
- After scaffolding a new module with `/kamper-new-module` and before merging the first PR.
- During code review of any change touching a module's surface (Info, Config, Watcher,
  Performance, Module.kt expect/actual files).

## Steps
1. **Validate the module exists** - `kamper/modules/<name>/` must be a directory.
2. **Walk the four checklist groups** below in order. For each item:
   - Use `grep`/`ls`/`cat` to verify presence of the artifact or pattern.
   - Mark each item PASS / FAIL / PARTIAL with the specific file path inspected.
3. **For the expect/actual completeness check**, list each of the 7 platform actual files
   that should exist (`androidMain`, `iosMain`, `jvmMain`, `macosMain`, `tvosMain`,
   `jsMain`, `wasmJsMain`) and confirm each contains `actual val {Name}Module`.
4. **For the safety check**, search every file under `kamper/modules/<name>/src/` for:
   - `while (true)` (FAIL - should be `while (isActive)`)
   - `println(` in production source (FAIL - should be `logger.log(...)`)
   - `// TODO` or `// FIXME` (FAIL - Detekt rejects)
   - Platform source method bodies that call platform APIs without `try/catch` wrapping (FAIL).
5. **Run `./gradlew :kamper:modules:<name>:jvmTest`** if the kamper-check skill's allowlisted
   commands are available, and report pass/fail.
6. **Emit a final report** as a markdown checklist with each item marked, total pass count,
   and a "READY / NOT READY" verdict.

## Reference

### Convention Checklist (mirrors .planning/codebase/CONVENTIONS.md)

#### Naming
- [ ] Module directory: `kamper/modules/{name}/`
- [ ] Package: `com.smellouk.kamper.{name}`
- [ ] Info class: `{Name}Info` (data class, implements `Info`)
- [ ] Config class: `{Name}Config` (data class, implements `Config`)
- [ ] Watcher class: `{Name}Watcher` (internal, extends `Watcher<{Name}Info>`)
- [ ] Performance class: `{Name}Performance` (internal, extends `Performance<...>`)

#### Completeness
- [ ] `{Name}Info` companion `INVALID` constant exists
- [ ] `{Name}Config.Builder` object with `build()` function
- [ ] `{Name}Config.DEFAULT` companion val
- [ ] `expect val {Name}Module` in `commonMain/Module.kt`
- [ ] `actual val {Name}Module` in ALL 7 platform actual source sets:
      androidMain, iosMain, jvmMain, macosMain, tvosMain, jsMain, wasmJsMain
- [ ] Registered in `settings.gradle.kts`

#### Safety
- [ ] All platform calls in `XxxInfoSource` wrapped in `try/catch`
- [ ] Watcher loop uses `while (isActive)`, not `while (true)`
- [ ] No `println()` in production code - uses `logger.log()`
- [ ] No `TODO:` or `FIXME:` comments (Detekt will fail)

#### Testing
- [ ] `{Name}ConfigBuilderTest` in `commonTest`
- [ ] `{Name}InfoMapperTest` in `commonTest`
- [ ] Platform source test in `androidTest` or `jvmTest`

### Source files to read for context
- `.planning/codebase/CONVENTIONS.md` - full naming and code style rules
- `.planning/codebase/ARCHITECTURE.md` - 4-layer model and module pattern
- `kamper/modules/cpu/` - canonical reference; the gold-standard module
- `CLAUDE.md` - project-level rules (commit format, build commands, layer model)

### Common findings and fixes
- **Missing INVALID sentinel** then add `companion object { val INVALID = {Name}Info(...) }` to the Info class
- **Missing actual val on a platform** then add `actual val {Name}Module` in `{platform}Main/Module.kt`
- **`while (true)` in Watcher** then replace with `while (isActive)` and import `kotlinx.coroutines.isActive`
- **`println(...)` in production** then inject Logger via constructor and use `logger.log(...)`
- **Platform call without try/catch** then wrap in try/catch, log via `logger.log(e.stackTraceToString())`, return `INVALID`
- **Missing test files** then add `{Name}ConfigBuilderTest.kt` and `{Name}InfoMapperTest.kt` in `commonTest`
```

Total length: target 100-180 lines.
  </action>
  <verify>
    <automated>test -f .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "^## Purpose" .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "^## When to Use" .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "^## Steps" .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "^## Reference" .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "INVALID" .claude/skills/kamper-module-review/SKILL.md &amp;&amp; grep -q "try/catch" .claude/skills/kamper-module-review/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/kamper-module-review/SKILL.md` exits 0
    - `grep -cE "^## (Purpose|When to Use|Steps|Reference)" .claude/skills/kamper-module-review/SKILL.md` returns 4
    - `grep -q "INVALID" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "Builder" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "DEFAULT" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "expect" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "actual" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "try/catch" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "while (isActive)" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -c "androidMain\|iosMain\|jvmMain\|macosMain\|tvosMain\|jsMain\|wasmJsMain" .claude/skills/kamper-module-review/SKILL.md` returns at least 7
    - `grep -q "kamper/modules/cpu" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `grep -q "CONVENTIONS" .claude/skills/kamper-module-review/SKILL.md` succeeds
    - `wc -l .claude/skills/kamper-module-review/SKILL.md | awk '{print ($1 >= 100)}'` returns 1
    - `grep -c "^\`\`\`" .claude/skills/kamper-module-review/SKILL.md` returns an even number
    - `grep -cE "[\xF0-\xF7][\x80-\xBF]{3}" .claude/skills/kamper-module-review/SKILL.md || true` returns 0
  </acceptance_criteria>
  <done>kamper-module-review SKILL.md created with 4 sections, full Naming/Completeness/Safety/Testing checklist embedded verbatim, all 7 platform names listed, no emojis.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend .claude/settings.json allowlist with safe Gradle commands</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json
    - .planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-RESEARCH.md
  </read_first>
  <action>
Edit `.claude/settings.json` to add 8 new entries to the `permissions.allow` array. Use the Edit tool (not Write) to preserve the existing structure and ordering of entries.

The 8 new entries to add (in this order, after the existing `Bash(git tag:*)` entry):

```
"Bash(./gradlew *:jvmTest:*)",
"Bash(./gradlew *:test:*)",
"Bash(./gradlew :kamper:api:test:*)",
"Bash(./gradlew :kamper:engine:test:*)",
"Bash(./gradlew test:*)",
"Bash(./gradlew detekt:*)",
"Bash(test:*)",
"Bash(find:*)"
```

Rationale per entry:
- `./gradlew *:jvmTest:*` - allows fast JVM unit tests on any module
- `./gradlew *:test:*` - allows full unit tests (incl. androidUnitTest via Robolectric/MockK) on any module
- `./gradlew :kamper:api:test:*` - allows API contract tests
- `./gradlew :kamper:engine:test:*` - allows engine tests
- `./gradlew test:*` - allows project-wide unit test sweep
- `./gradlew detekt:*` - allows static analysis (zero-tolerance pre-commit)
- `Bash(test:*)` - allows the POSIX `test` command for file existence checks (used in plan verification)
- `Bash(find:*)` - allows `find` for module review (locating files matching patterns)

DO NOT add any of:
- `connectedAndroidTest` (requires device)
- `assembleXCFramework` (publishing only)
- `publish*` (release-time only)
- Any task that modifies remote state

After editing, validate the file is still valid JSON:
```
python3 -c "import json; json.load(open('.claude/settings.json'))"
```

(or `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json', 'utf8'))"` if python3 is not available).
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('.claude/settings.json')); allow=d['permissions']['allow']; assert any('jvmTest' in s for s in allow), 'missing jvmTest'; assert any('detekt' in s for s in allow), 'missing detekt'; assert not any('connectedAndroidTest' in s for s in allow), 'connectedAndroidTest must NOT be in allowlist'; assert any('Bash(./gradlew *:test:*)' == s for s in allow), 'missing exact gradlew test entry'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('.claude/settings.json'))"` exits 0 (valid JSON)
    - `grep -c "jvmTest" .claude/settings.json` returns at least 1
    - `grep -c "detekt" .claude/settings.json` returns at least 1
    - `grep -c "connectedAndroidTest" .claude/settings.json` returns 0 (deliberately excluded)
    - `grep -c "assembleXCFramework" .claude/settings.json` returns 0
    - `grep -c "Bash(./gradlew" .claude/settings.json` returns at least 6
    - All pre-existing allow entries still present: `grep -c "Bash(git" .claude/settings.json` returns at least 6
    - All pre-existing allow entries still present: `grep -q "Bash(grep:\*)" .claude/settings.json && grep -q "Bash(ls:\*)" .claude/settings.json && grep -q "Bash(mkdir:\*)" .claude/settings.json`
  </acceptance_criteria>
  <done>.claude/settings.json contains 8 new safe Gradle/test allowlist entries, no device-requiring or release tasks added, JSON remains valid, pre-existing entries preserved.</done>
</task>

</tasks>

<verification>
- All 3 files exist: `.claude/skills/kamper-check/SKILL.md`, `.claude/skills/kamper-module-review/SKILL.md`, `.claude/settings.json`
- Both SKILL.md files have 4 standard sections (Purpose, When to Use, Steps, Reference)
- kamper-check explicitly references jvmTest and warns against connectedAndroidTest
- kamper-module-review checklist names all 7 platform actual source sets and includes INVALID/Builder/DEFAULT/try/catch checks
- settings.json is valid JSON, contains jvmTest/detekt/test allowances, does NOT contain connectedAndroidTest
- No emojis in either SKILL.md
</verification>

<success_criteria>
1. `.claude/skills/kamper-check/SKILL.md` created with 4 sections per D-08
2. `.claude/skills/kamper-module-review/SKILL.md` created with 4 sections per D-09
3. `.claude/settings.json` extended with 8 allowlist entries enabling autonomous Gradle test/detekt invocation
4. `connectedAndroidTest` deliberately NOT in allowlist (per D-03 build/test guidance)
5. All locked decisions D-06..D-11 visibly satisfied across the 3 modified files
6. settings.json remains valid JSON after edit
</success_criteria>

<output>
After completion, create `.planning/phases/19-make-repo-claude-friend-and-research-which-skill-we-could-of/19-03-SUMMARY.md` describing:
- Final line counts of both SKILL.md files
- Number of new entries added to settings.json (should be 8)
- Confirmation that connectedAndroidTest is NOT in the allowlist
- Confirmation that all acceptance criteria passed
</output>

---
phase: 26-flutter-support-package-and-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/flutter/pubspec.yaml
  - libs/ui/flutter/lib/konitor_flutter.dart
  - libs/ui/flutter/lib/src/konitor.dart
  - libs/ui/flutter/lib/src/konitor_config.dart
  - libs/ui/flutter/lib/src/models/cpu_info.dart
  - libs/ui/flutter/lib/src/models/fps_info.dart
  - libs/ui/flutter/lib/src/models/memory_info.dart
  - libs/ui/flutter/lib/src/models/network_info.dart
  - libs/ui/flutter/lib/src/models/issue_info.dart
  - libs/ui/flutter/lib/src/models/jank_info.dart
  - libs/ui/flutter/lib/src/models/gc_info.dart
  - libs/ui/flutter/lib/src/models/thermal_info.dart
  - libs/ui/flutter/lib/src/models/gpu_info.dart
  - libs/ui/flutter/lib/src/models/user_event_info.dart
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "libs/ui/flutter/pubspec.yaml declares name: konitor_flutter with flutter plugin platforms android+ios"
    - "Dart barrel file exports all 10 model classes, KonitorConfig, and Konitor class"
    - "Konitor class exposes 9 typed metric EventChannel streams (cpuStream..gpuStream) plus userEventStream (10 EventChannels total) and 7 MethodChannel methods"
    - "All 10 model classes have factory XxxInfo.fromMap(Map<dynamic, dynamic> m) constructors"
    - "KonitorConfig has nullable bool fields for all 9 modules with toMap() serialization"
  artifacts:
    - path: "libs/ui/flutter/pubspec.yaml"
      provides: "Flutter plugin package manifest"
      contains: "flutter: plugin: platforms: android: ios:"
    - path: "libs/ui/flutter/lib/konitor_flutter.dart"
      provides: "Public barrel export"
      exports: ["Konitor", "KonitorConfig", "CpuInfo", "FpsInfo", "MemoryInfo", "NetworkInfo", "IssueInfo", "JankInfo", "GcInfo", "ThermalInfo", "GpuInfo", "UserEventInfo"]
    - path: "libs/ui/flutter/lib/src/konitor.dart"
      provides: "Dart API class with 9 streams + 7 control methods"
      contains: "class Konitor"
    - path: "libs/ui/flutter/lib/src/models/cpu_info.dart"
      provides: "CpuInfo model"
      contains: "factory CpuInfo.fromMap"
  key_links:
    - from: "lib/src/konitor.dart"
      to: "com.smellouk.konitor/cpu"
      via: "EventChannel constructor"
      pattern: "EventChannel.*com\\.smellouk\\.konitor/cpu"
    - from: "lib/src/konitor.dart"
      to: "com.smellouk.konitor/control"
      via: "MethodChannel constructor"
      pattern: "MethodChannel.*com\\.smellouk\\.konitor/control"
---

<objective>
Create the complete Dart layer of the `konitor_flutter` Flutter plugin package: the package manifest (pubspec.yaml), 10 typed model classes, KonitorConfig, the Konitor service class (9 EventChannel streams + 7 MethodChannel methods), and the barrel export file.

Purpose: Establish the Dart API surface that the demo app and consumers will depend on. These files are self-contained — no Android or iOS code is needed here.
Output: `libs/ui/flutter/pubspec.yaml`, `lib/konitor_flutter.dart`, `lib/src/konitor.dart`, `lib/src/konitor_config.dart`, and 10 files under `lib/src/models/`.
</objective>

<execution_context>
@/Users/smellouk/Developer/git/kamper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/Developer/git/kamper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/smellouk/Developer/git/kamper/.planning/ROADMAP.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-CONTEXT.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-RESEARCH.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-PATTERNS.md

<interfaces>
<!-- Field definitions from libs/ui/rn/src/types.ts (canonical reference — D-04) -->
CpuInfo:    totalUseRatio, appRatio, userRatio, systemRatio, ioWaitRatio  (all double)
FpsInfo:    fps (int)
MemoryInfo: heapAllocatedMb, heapMaxMb, ramUsedMb, ramTotalMb (double), isLowMemory (bool)
NetworkInfo: rxMb, txMb (double)
IssueInfo:  id, type, severity, message (String), timestamp (double), durationMs? (double?), threadName? (String?)
JankInfo:   droppedFrames (int), jankyRatio, worstFrameMs (double)
GcInfo:     gcCountDelta, gcPauseMsDelta, gcCount (double)
ThermalInfo: state (String), isThrottling (bool), temperatureC (double)
GpuInfo:    utilization, usedMemoryMb, totalMemoryMb, curFreqKhz, maxFreqKhz, appUtilization, rendererUtilization, tilerUtilization, computeUtilization (all double)
UserEventInfo: name (String), durationMs? (double?)
KonitorConfig: cpu?, fps?, memory?, network?, issues?, jank?, gc?, thermal?, gpu? (all bool?)

Channel names (D-02): com.smellouk.konitor/cpu, /fps, /memory, /network, /issues, /jank, /gc, /thermal, /gpu
MethodChannel (D-03): com.smellouk.konitor/control
Methods: start(config), stop(), showOverlay(), hideOverlay(), logEvent(name), startEvent(name)→int, endEvent(tokenId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pubspec.yaml and all 10 model classes + KonitorConfig</name>
  <read_first>
    - /Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-PATTERNS.md (KonitorConfig pattern lines 163-185, model class pattern lines 135-161)
    - /Users/smellouk/Developer/git/kamper/libs/ui/rn/src/types.ts (field names — canonical source)
  </read_first>
  <files>
    libs/ui/flutter/pubspec.yaml,
    libs/ui/flutter/lib/src/konitor_config.dart,
    libs/ui/flutter/lib/src/models/cpu_info.dart,
    libs/ui/flutter/lib/src/models/fps_info.dart,
    libs/ui/flutter/lib/src/models/memory_info.dart,
    libs/ui/flutter/lib/src/models/network_info.dart,
    libs/ui/flutter/lib/src/models/issue_info.dart,
    libs/ui/flutter/lib/src/models/jank_info.dart,
    libs/ui/flutter/lib/src/models/gc_info.dart,
    libs/ui/flutter/lib/src/models/thermal_info.dart,
    libs/ui/flutter/lib/src/models/gpu_info.dart,
    libs/ui/flutter/lib/src/models/user_event_info.dart
  </files>
  <action>
Create `libs/ui/flutter/` directory structure and the following files:

**pubspec.yaml** (`libs/ui/flutter/pubspec.yaml`):
```yaml
name: konitor_flutter
description: Konitor performance monitoring plugin for Flutter (Android + iOS).
version: 0.1.0
homepage: https://github.com/smellouk/konitor

environment:
  sdk: ^3.2.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.smellouk.konitor.flutter
        pluginClass: KonitorFlutterPlugin
      ios:
        pluginClass: KonitorFlutterPlugin
```

**KonitorConfig** (`libs/ui/flutter/lib/src/konitor_config.dart`):
```dart
class KonitorConfig {
  final bool? cpu, fps, memory, network, issues, jank, gc, thermal, gpu;

  const KonitorConfig({
    this.cpu, this.fps, this.memory, this.network,
    this.issues, this.jank, this.gc, this.thermal, this.gpu,
  });

  Map<String, dynamic> toMap() => {
    if (cpu != null)     'cpu': cpu,
    if (fps != null)     'fps': fps,
    if (memory != null)  'memory': memory,
    if (network != null) 'network': network,
    if (issues != null)  'issues': issues,
    if (jank != null)    'jank': jank,
    if (gc != null)      'gc': gc,
    if (thermal != null) 'thermal': thermal,
    if (gpu != null)     'gpu': gpu,
  };
}
```

**10 model classes** — create each in `libs/ui/flutter/lib/src/models/`. All are immutable (const constructors), use `factory XxxInfo.fromMap(Map<dynamic, dynamic> m)`, and cast via `(m['field'] as num).toDouble()` for numeric fields, `m['field'] as String` for strings, `m['field'] as bool` for booleans. Nullable fields use `m['field'] != null ? (m['field'] as num).toDouble() : null`.

cpu_info.dart — fields: `totalUseRatio, appRatio, userRatio, systemRatio, ioWaitRatio` (all double)
fps_info.dart — field: `fps` (int — use `(m['fps'] as num).toInt()`)
memory_info.dart — fields: `heapAllocatedMb, heapMaxMb, ramUsedMb, ramTotalMb` (double), `isLowMemory` (bool)
network_info.dart — fields: `rxMb, txMb` (double)
issue_info.dart — fields: `id, type, severity, message` (String), `timestamp` (double), `durationMs` (double?), `threadName` (String?)
jank_info.dart — fields: `droppedFrames` (int), `jankyRatio, worstFrameMs` (double)
gc_info.dart — fields: `gcCountDelta, gcPauseMsDelta, gcCount` (double)
thermal_info.dart — fields: `state` (String), `isThrottling` (bool), `temperatureC` (double)
gpu_info.dart — fields: `utilization, usedMemoryMb, totalMemoryMb, curFreqKhz, maxFreqKhz, appUtilization, rendererUtilization, tilerUtilization, computeUtilization` (all double)
user_event_info.dart — fields: `name` (String), `durationMs` (double?)
  </action>
  <verify>
    <automated>grep -r "factory.*fromMap" /Users/smellouk/Developer/git/kamper/libs/ui/flutter/lib/src/models/ | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains exactly: `name: konitor_flutter`
    - pubspec.yaml contains: `flutter: plugin: platforms: android:` and `ios:`
    - pubspec.yaml contains: `pluginClass: KonitorFlutterPlugin` (appears twice — android and ios)
    - All 10 model files exist: `find libs/ui/flutter/lib/src/models/ -name "*.dart" | wc -l` outputs 10
    - grep output for `factory.*fromMap` in models/ equals 10
    - konitor_config.dart contains `toMap()` method
    - konitor_config.dart contains `final bool? cpu, fps, memory, network, issues, jank, gc, thermal, gpu`
  </acceptance_criteria>
  <done>10 model files + konitor_config.dart + pubspec.yaml created; each model has a factory fromMap constructor; pubspec declares flutter plugin with android+ios platforms</done>
</task>

<task type="auto">
  <name>Task 2: Create Konitor service class and barrel export file</name>
  <read_first>
    - /Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-PATTERNS.md (Konitor class pattern lines 80-111)
    - /Users/smellouk/Developer/git/kamper/.planning/phases/26-flutter-support-package-and-demo/26-RESEARCH.md (Pattern 3 Dart API layer)
  </read_first>
  <files>
    libs/ui/flutter/lib/src/konitor.dart,
    libs/ui/flutter/lib/konitor_flutter.dart
  </files>
  <action>
**konitor.dart** (`libs/ui/flutter/lib/src/konitor.dart`):

```dart
import 'dart:async';
import 'package:flutter/services.dart';
import 'konitor_config.dart';
import 'models/cpu_info.dart';
import 'models/fps_info.dart';
import 'models/memory_info.dart';
import 'models/network_info.dart';
import 'models/issue_info.dart';
import 'models/jank_info.dart';
import 'models/gc_info.dart';
import 'models/thermal_info.dart';
import 'models/gpu_info.dart';
import 'models/user_event_info.dart';

class Konitor {
  static const _control = MethodChannel('com.smellouk.konitor/control');

  static Stream<CpuInfo> get cpuStream =>
      const EventChannel('com.smellouk.konitor/cpu')
          .receiveBroadcastStream()
          .map((event) => CpuInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<FpsInfo> get fpsStream =>
      const EventChannel('com.smellouk.konitor/fps')
          .receiveBroadcastStream()
          .map((event) => FpsInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<MemoryInfo> get memoryStream =>
      const EventChannel('com.smellouk.konitor/memory')
          .receiveBroadcastStream()
          .map((event) => MemoryInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<NetworkInfo> get networkStream =>
      const EventChannel('com.smellouk.konitor/network')
          .receiveBroadcastStream()
          .map((event) => NetworkInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<IssueInfo> get issuesStream =>
      const EventChannel('com.smellouk.konitor/issues')
          .receiveBroadcastStream()
          .map((event) => IssueInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<JankInfo> get jankStream =>
      const EventChannel('com.smellouk.konitor/jank')
          .receiveBroadcastStream()
          .map((event) => JankInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<GcInfo> get gcStream =>
      const EventChannel('com.smellouk.konitor/gc')
          .receiveBroadcastStream()
          .map((event) => GcInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<ThermalInfo> get thermalStream =>
      const EventChannel('com.smellouk.konitor/thermal')
          .receiveBroadcastStream()
          .map((event) => ThermalInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<GpuInfo> get gpuStream =>
      const EventChannel('com.smellouk.konitor/gpu')
          .receiveBroadcastStream()
          .map((event) => GpuInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Stream<UserEventInfo> get userEventStream =>
      const EventChannel('com.smellouk.konitor/user_event')
          .receiveBroadcastStream()
          .map((event) => UserEventInfo.fromMap(Map<dynamic, dynamic>.from(event as Map)));

  static Future<void> start([KonitorConfig? config]) =>
      _control.invokeMethod('start', config?.toMap() ?? {});

  static Future<void> stop() => _control.invokeMethod('stop');

  static Future<void> showOverlay() => _control.invokeMethod('showOverlay');

  static Future<void> hideOverlay() => _control.invokeMethod('hideOverlay');

  static Future<void> logEvent(String name) =>
      _control.invokeMethod('logEvent', {'name': name});

  static Future<int> startEvent(String name) async =>
      (await _control.invokeMethod<int>('startEvent', {'name': name}))!;

  static Future<void> endEvent(int tokenId) =>
      _control.invokeMethod('endEvent', {'tokenId': tokenId});
}
```

**barrel file** (`libs/ui/flutter/lib/konitor_flutter.dart`):

```dart
library konitor_flutter;

export 'src/konitor.dart';
export 'src/konitor_config.dart';
export 'src/models/cpu_info.dart';
export 'src/models/fps_info.dart';
export 'src/models/memory_info.dart';
export 'src/models/network_info.dart';
export 'src/models/issue_info.dart';
export 'src/models/jank_info.dart';
export 'src/models/gc_info.dart';
export 'src/models/thermal_info.dart';
export 'src/models/gpu_info.dart';
export 'src/models/user_event_info.dart';
```
  </action>
  <verify>
    <automated>grep -c "EventChannel" /Users/smellouk/Developer/git/kamper/libs/ui/flutter/lib/src/konitor.dart</automated>
  </verify>
  <acceptance_criteria>
    - konitor.dart contains exactly 10 EventChannel declarations (one per metric including user_event)
    - konitor.dart contains: `MethodChannel('com.smellouk.konitor/control')`
    - konitor.dart contains: `EventChannel('com.smellouk.konitor/cpu')`
    - konitor.dart contains all 7 static methods: start, stop, showOverlay, hideOverlay, logEvent, startEvent, endEvent
    - konitor_flutter.dart contains 12 export lines (konitor + konitor_config + 10 models)
    - `grep -c "^export" libs/ui/flutter/lib/konitor_flutter.dart` outputs 12
  </acceptance_criteria>
  <done>konitor.dart exposes 9 metric streams + userEventStream + 7 control methods; barrel file exports all public symbols</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart→MethodChannel | Dart serializes KonitorConfig to Map<String, dynamic>; native reads it back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | konitor.dart `start()` config map | accept | Dart layer passes whatever the caller provides; validation happens on the native side in `flag()` helper — this plan is Dart-only and cannot validate |
</threat_model>

<verification>
```bash
# All 10 model files exist
find /Users/smellouk/Developer/git/kamper/libs/ui/flutter/lib/src/models/ -name "*.dart" | wc -l | tr -d ' '
# Expected: 10

# Each model has fromMap factory
grep -rl "factory.*fromMap" /Users/smellouk/Developer/git/kamper/libs/ui/flutter/lib/src/models/ | wc -l | tr -d ' '
# Expected: 10

# pubspec has plugin declaration
grep "pluginClass" /Users/smellouk/Developer/git/kamper/libs/ui/flutter/pubspec.yaml
# Expected: KonitorFlutterPlugin (twice)

# Konitor class has correct channel names
grep "com.smellouk.konitor" /Users/smellouk/Developer/git/kamper/libs/ui/flutter/lib/src/konitor.dart | wc -l | tr -d ' '
# Expected: 10+ (9 event channels + 1 control channel)
```
</verification>

<success_criteria>
- libs/ui/flutter/pubspec.yaml exists with name: konitor_flutter and flutter plugin platforms android+ios
- 10 model files exist in lib/src/models/ each with factory XxxInfo.fromMap
- lib/src/konitor_config.dart with nullable bool fields and toMap()
- lib/src/konitor.dart with 9 metric streams + userEventStream + 7 control methods using correct channel names
- lib/konitor_flutter.dart barrel exports all 12 public symbols
</success_criteria>

<output>
After completion, create `.planning/phases/26-flutter-support-package-and-demo/26-01-SUMMARY.md`
</output>

---
phase: 21
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - CLAUDE.md
  - README.md
  - CAPACITY.md
  - .claude/settings.json
  - .claude/settings.local.json
  - .claude/skills/kamper-check/SKILL.md
  - .claude/skills/kamper-new-module/SKILL.md
  - .claude/skills/kamper-module-review/SKILL.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/PRINCIPLES.md
  - .planning/codebase/STACK.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/adr/adr-001-plugin-architecture.md
  - .planning/codebase/adr/adr-002-kmp-expect-actual.md
  - .planning/codebase/adr/adr-003-listener-pattern.md
  - .planning/codebase/adr/adr-004-no-breaking-changes.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.0-REQUIREMENTS.md
  - .planning/milestones/v1.0-ROADMAP.md
autonomous: true
requirements: []
tags: [monorepo, refactor, docs, claude-skills]

must_haves:
  truths:
    - "CLAUDE.md Build & Test commands reference :libs:* and Claude agents can run them via the allowlist"
    - "All 3 .claude/skills/*/SKILL.md files use libs/ paths and :libs:* commands"
    - ".claude/settings.json allowlist accepts :libs:api:test and :libs:engine:test without prompting"
    - "Documentation across CLAUDE.md, README.md, CAPACITY.md, .planning/codebase/, .planning/milestones/, .planning/ROADMAP.md uses libs/ instead of kamper/"
    - "STRUCTURE.md no longer references the obsolete kamper/publish.gradle.kts (replaced by build-logic/src/main/kotlin/KamperPublishPlugin.kt)"
    - "No deprecated/stale files remain that reference the old kamper/ tree (D-09)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Operational reference for Claude agents and contributors"
      contains: "./gradlew :libs:"
    - path: ".claude/settings.json"
      provides: "Pre-approved Gradle command allowlist"
      contains: "Bash(./gradlew :libs:api:test:*)"
    - path: ".claude/skills/kamper-new-module/SKILL.md"
      provides: "Module scaffolder skill with libs/ paths"
      contains: "include(\":libs:modules:"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Build layout documentation, refreshed for libs/ + correct publish plugin reference"
      contains: "build-logic/src/main/kotlin/KamperPublishPlugin.kt"
  key_links:
    - from: "CLAUDE.md Build & Test table"
      to: ".claude/settings.json allowlist"
      via: "Both reference identical Gradle command shape :libs:api:test :libs:engine:test"
      pattern: ":libs:(api|engine):test"
    - from: ".claude/skills/kamper-new-module/SKILL.md"
      to: "settings.gradle.kts include() pattern"
      via: "Skill scaffolds new modules under libs/ and registers them with include(\":libs:modules:{name}\")"
      pattern: "include\\(\":libs:modules:"
    - from: ".planning/codebase/STRUCTURE.md"
      to: "build-logic/src/main/kotlin/KamperPublishPlugin.kt"
      via: "Replaces stale kamper/publish.gradle.kts reference (D-09 cleanup)"
      pattern: "KamperPublishPlugin"
---

<objective>
Sweep all human- and Claude-facing documentation, the Claude Code allowlist, and project skills so they reference the new `libs/` structure. Fix the stale `kamper/publish.gradle.kts` reference in STRUCTURE.md (D-09), and ensure the Claude Code agent will not be prompted for the renamed Gradle commands.

Purpose: Without this wave, future Claude sessions and contributors continue to see `kamper/` paths in CLAUDE.md and the skills, and the allowlist (`.claude/settings.json`) prompts on every test invocation (Pitfall 6 in 21-RESEARCH.md). This wave is the only one that makes the rename feel "done" from the contributor experience side.

Output: 23 documentation/config files updated, including 3 SKILL.md files, 2 .claude/settings*.json files, CLAUDE.md, README.md, CAPACITY.md, all 8 `.planning/codebase/*.md` files, all 4 ADR files, ROADMAP.md, and 2 milestone files. One commit titled `docs(21): sweep documentation and skills for libs/ rename`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-CONTEXT.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-RESEARCH.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-PATTERNS.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-01-PLAN.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-02-PLAN.md

<interfaces>
Allowlist contract: `.claude/settings.json` allowlist entries must literally match the Gradle command shape a Claude agent invokes. Mismatch causes a permission prompt on every command. `.claude/settings.json` (project-shared) is authoritative; `.claude/settings.local.json` (session) accumulates stale entries — sweep these too. (Pitfall 6 + Pitfall 8 in 21-RESEARCH.md.)

D-09 stale-file cleanup target identified in 21-RESEARCH.md: `.planning/codebase/STRUCTURE.md` line 203 references `kamper/publish.gradle.kts` which no longer exists; the actual file is `build-logic/src/main/kotlin/KamperPublishPlugin.kt`. This is the only confirmed stale-file fix in scope. CONTRIBUTING.md is "stale but already superseded by CLAUDE.md per Phase 19" — no action this phase. `program.kexe` is gitignored.

Names that MUST stay verbatim in docs (per 21-PATTERNS.md "What Must NOT Change"):
- Plugin IDs: `kamper.kmp.library`, `kamper.publish`, `kamper.android.config`
- Maven coordinates: `com.smellouk.kamper:*`
- Class names: `KamperPublishPlugin`, `KamperUi`, `Kamper` (object), `KamperPanel`, etc.
- npm package: `react-native-kamper`
- Repository URLs: `https://github.com/smellouk/kamper`
- Skill folder names: `.claude/skills/kamper-{check,new-module,module-review}/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md, .claude/settings.json, .claude/settings.local.json, and 3 .claude/skills/*/SKILL.md</name>
  <files>
    CLAUDE.md,
    .claude/settings.json,
    .claude/settings.local.json,
    .claude/skills/kamper-check/SKILL.md,
    .claude/skills/kamper-new-module/SKILL.md,
    .claude/skills/kamper-module-review/SKILL.md
  </files>
  <read_first>
    - CLAUDE.md (full file — Build and Test table, Module Patterns, Architecture and Key Files, Canonical Reference Module section)
    - .claude/settings.json (full file — locate allowlist entries `Bash(./gradlew :kamper:api:test:*)` and `Bash(./gradlew :kamper:engine:test:*)`)
    - .claude/settings.local.json (full file — 35 stale `:kamper:*` entries from prior sessions; identify all)
    - .claude/skills/kamper-check/SKILL.md (full file — command table and module path examples)
    - .claude/skills/kamper-new-module/SKILL.md (full file — directory tree, settings.gradle.kts registration template, verification command, build.gradle.kts template, canonical reference module path)
    - .claude/skills/kamper-module-review/SKILL.md (full file — module path, expect/actual platform paths, Gradle command, source files to read)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-PATTERNS.md (Wave 3 sections, lines 387-449)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-RESEARCH.md (Pitfalls 5, 6, 8)
  </read_first>
  <action>
    Apply this single substitution rule across each file:
    - Every literal `:kamper:` (Gradle project path prefix) becomes `:libs:`
    - Every literal `kamper/` (filesystem path prefix as a directory segment, NOT inside URLs like `github.com/smellouk/kamper`) becomes `libs/`

    DO NOT change:
    - Any occurrence of the word `kamper` standalone (not followed by `/` or `:`) — class names like `Kamper`, repo URLs `https://github.com/smellouk/kamper`, framework filenames
    - Plugin ID strings `kamper.kmp.library`, `kamper.publish`, `kamper.android.config`
    - Maven coordinate strings `com.smellouk.kamper:*`
    - Class/object/file names like `KamperPublishPlugin`, `KamperUi`, `KamperPanel`, `Kamper.xcframework`, `Kamper.podspec`
    - npm package name `react-native-kamper`
    - Skill folder names `.claude/skills/kamper-{check,new-module,module-review}/` and the strings `kamper-check`, `kamper-new-module`, `kamper-module-review`
    - Phase directory names in any planning history that mention skill names (e.g., `phase 19 created the kamper-new-module skill` — leave it)

    Specific edits per file:

    CLAUDE.md (~15 occurrences):
    - Build and Test table: every `:kamper:*` Gradle command becomes `:libs:*`. Concrete edits — every cell that says `./gradlew :kamper:modules:<name>:jvmTest` becomes `./gradlew :libs:modules:<name>:jvmTest`; same for `:test`, and the `:kamper:api:test :kamper:engine:test` smoke command becomes `:libs:api:test :libs:engine:test`.
    - Quick Start "Smoke test:" line: same Gradle command swap.
    - Module Patterns section paragraph mentioning `kamper/modules/cpu/` as canonical reference: `kamper/modules/cpu/` becomes `libs/modules/cpu/` (multiple occurrences).
    - "expect/actual Module Declaration" section file-path comment `// commonMain/kotlin/com/smellouk/kamper/{name}/Module.kt` is unchanged (this is a Kotlin package path, not a Gradle path).
    - "Canonical Reference Module" header paragraph: `kamper/modules/cpu/` becomes `libs/modules/cpu/`.
    - Architecture diagram: every `kamper/api/...`, `kamper/engine/...`, `kamper/modules/...`, `kamper/ui/android/...` path becomes `libs/...`.
    - Architecture and Key Files table File column: every `kamper/...` path becomes `libs/...`. The `build-logic/` entry is unchanged.
    - "New modules must also be registered in `settings.gradle.kts`" code block: `include(":kamper:modules:{name}")` becomes `include(":libs:modules:{name}")`.
    - Plugin IDs in the build.gradle.kts code block (`id("kamper.kmp.library")`, `id("kamper.publish")`) — UNCHANGED.
    - `namespace = "com.smellouk.kamper.{name}"` line — UNCHANGED.
    - Optionally (Claude's Discretion per CONTEXT.md): add a brief `## Monorepo Structure` section after "Architecture and Key Files" describing the new layout. If added, keep it under 25 lines.

    .claude/settings.json (2 substitutions in the `allow` array):
    - `"Bash(./gradlew :kamper:api:test:*)"` becomes `"Bash(./gradlew :libs:api:test:*)"`
    - `"Bash(./gradlew :kamper:engine:test:*)"` becomes `"Bash(./gradlew :libs:engine:test:*)"`

    .claude/settings.local.json (35 stale entries):
    - Replace every `:kamper:` substring inside string values within the JSON arrays with `:libs:`. These are entries like `"Bash(./gradlew :kamper:modules:cpu:jvmTest)"` — bulk-edit by reading the file then writing the substituted version.
    - Entries containing `:samples:*` are unaffected (different module hierarchy).

    .claude/skills/kamper-check/SKILL.md (~8 occurrences):
    - Command table: every `./gradlew :kamper:*` becomes `./gradlew :libs:*`
    - Module path examples (e.g., `kamper/modules/cpu/`) become `libs/modules/cpu/`
    - Settings allowlist references: `:kamper:api:test` becomes `:libs:api:test` and `:kamper:engine:test` becomes `:libs:engine:test`
    - The skill name `kamper-check` itself (in headings, frontmatter, file path references to the skill) is unchanged.

    .claude/skills/kamper-new-module/SKILL.md (~10 occurrences):
    - Step 3 directory tree (`kamper/modules/{name}/...`): `kamper/` becomes `libs/` everywhere in the tree
    - Step 9 settings.gradle.kts registration: `include(":kamper:modules:{name}")` becomes `include(":libs:modules:{name}")`
    - Step 10 verification command: `./gradlew :kamper:modules:{name}:...` becomes `./gradlew :libs:modules:{name}:...`
    - Reference build.gradle.kts template: `project(":kamper:api")` becomes `project(":libs:api")`
    - Canonical reference module: `kamper/modules/cpu/` becomes `libs/modules/cpu/`
    - Plugin IDs in template (`id("kamper.kmp.library")`, `id("kamper.publish")`) — UNCHANGED
    - The skill name `kamper-new-module` itself is unchanged.

    .claude/skills/kamper-module-review/SKILL.md (~8 occurrences):
    - Step 1 validation path: `kamper/modules/<name>/` becomes `libs/modules/<name>/`
    - Step 3 expect/actual paths: every `kamper/modules/<name>/src/{platform}Main/...` becomes `libs/modules/<name>/src/{platform}Main/...`
    - Step 5 Gradle command: `./gradlew :kamper:modules:<name>:jvmTest` becomes `./gradlew :libs:modules:<name>:jvmTest`
    - Reference checklist: `kamper/modules/` becomes `libs/modules/` and `:kamper:modules:` becomes `:libs:modules:`
    - "Source files to read" section: `kamper/modules/cpu/` becomes `libs/modules/cpu/`
    - The skill name `kamper-module-review` itself is unchanged.

    Verify after edits — the following grep should return zero non-empty lines (the inverse-match whitelist allows GitHub URLs, Maven coordinates, plugin IDs, and skill folder names):
    GREP_VERIFY_CMD: `grep -nh ':kamper:\|kamper/' CLAUDE.md .claude/settings.json .claude/settings.local.json .claude/skills/kamper-check/SKILL.md .claude/skills/kamper-new-module/SKILL.md .claude/skills/kamper-module-review/SKILL.md | grep -v 'github.com/smellouk/kamper\|com.smellouk.kamper\|kamper.kmp.library\|kamper.publish\|kamper.android.config\|skills/kamper-'`
  </action>
  <verify>
    <automated>bash -c "grep -nh ':kamper:\|kamper/' CLAUDE.md .claude/settings.json .claude/settings.local.json .claude/skills/kamper-check/SKILL.md .claude/skills/kamper-new-module/SKILL.md .claude/skills/kamper-module-review/SKILL.md 2>/dev/null | grep -v 'github.com/smellouk/kamper' | grep -v 'com.smellouk.kamper' | grep -v 'kamper.kmp.library' | grep -v 'kamper.publish' | grep -v 'kamper.android.config' | grep -v 'skills/kamper-' | wc -l | tr -d ' '" | grep -q '^0$' &amp;&amp; grep -c 'Bash(./gradlew :libs:api:test:\*)' .claude/settings.json | grep -q '^1$' &amp;&amp; grep -c 'Bash(./gradlew :libs:engine:test:\*)' .claude/settings.json | grep -q '^1$'</automated>
  </verify>
  <done>
    - CLAUDE.md contains zero unwhitelisted `kamper/` filesystem paths and zero `:kamper:` Gradle paths (whitelist: GitHub URLs, Maven coordinates, plugin IDs)
    - CLAUDE.md contains at least 5 occurrences of `:libs:` (Gradle commands and project path references)
    - .claude/settings.json contains exactly one `Bash(./gradlew :libs:api:test:*)` entry and one `Bash(./gradlew :libs:engine:test:*)` entry; zero `:kamper:` strings remain anywhere in the file
    - .claude/settings.local.json contains zero `:kamper:` strings (35 entries swept to `:libs:`)
    - All 3 SKILL.md files contain zero unwhitelisted `kamper/` or `:kamper:` strings (skill folder names `kamper-check`, `kamper-new-module`, `kamper-module-review` whitelisted)
    - Plugin IDs `kamper.kmp.library`, `kamper.publish`, `kamper.android.config` STILL appear in CLAUDE.md and SKILL.md code-block templates
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md, CAPACITY.md, all .planning/codebase/*.md (8 + 4 ADR), ROADMAP.md, and milestones</name>
  <files>
    README.md,
    CAPACITY.md,
    .planning/codebase/STRUCTURE.md,
    .planning/codebase/ARCHITECTURE.md,
    .planning/codebase/CONVENTIONS.md,
    .planning/codebase/CONCERNS.md,
    .planning/codebase/INTEGRATIONS.md,
    .planning/codebase/PRINCIPLES.md,
    .planning/codebase/STACK.md,
    .planning/codebase/TESTING.md,
    .planning/codebase/adr/adr-001-plugin-architecture.md,
    .planning/codebase/adr/adr-002-kmp-expect-actual.md,
    .planning/codebase/adr/adr-003-listener-pattern.md,
    .planning/codebase/adr/adr-004-no-breaking-changes.md,
    .planning/ROADMAP.md,
    .planning/milestones/v1.0-REQUIREMENTS.md,
    .planning/milestones/v1.0-ROADMAP.md
  </files>
  <read_first>
    - README.md (1 occurrence in contributing section)
    - CAPACITY.md (2 occurrences of file paths under kamper/)
    - .planning/codebase/STRUCTURE.md (extensive references throughout PLUS line containing `kamper/publish.gradle.kts` which is stale; the real file is `build-logic/src/main/kotlin/KamperPublishPlugin.kt`)
    - .planning/codebase/ARCHITECTURE.md (path references)
    - .planning/codebase/CONVENTIONS.md (path references)
    - .planning/codebase/CONCERNS.md (path references)
    - .planning/codebase/INTEGRATIONS.md (path references)
    - .planning/codebase/PRINCIPLES.md (path references)
    - .planning/codebase/STACK.md (path references)
    - .planning/codebase/TESTING.md (path references)
    - .planning/codebase/adr/adr-001-plugin-architecture.md
    - .planning/codebase/adr/adr-002-kmp-expect-actual.md
    - .planning/codebase/adr/adr-003-listener-pattern.md
    - .planning/codebase/adr/adr-004-no-breaking-changes.md
    - .planning/ROADMAP.md (2 occurrences — phase descriptions and completion notes that reference `kamper/modules/`)
    - .planning/milestones/v1.0-REQUIREMENTS.md (path references)
    - .planning/milestones/v1.0-ROADMAP.md (path references)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-PATTERNS.md (Wave 3 sections, especially STRUCTURE.md notes at lines 451-475)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-RESEARCH.md ("Stale / Deprecated Files (D-09)" section at lines 343-348)
  </read_first>
  <action>
    Apply the same substitution rule used in Task 1:
    - Every literal `:kamper:` (Gradle project path prefix) becomes `:libs:`
    - Every literal `kamper/` (filesystem path segment, NOT inside URLs) becomes `libs/`

    Per file:

    README.md (1 occurrence):
    - The contributing section reference `kamper/modules/` becomes `libs/modules/`. Do NOT change `https://github.com/smellouk/kamper` URLs, the package name `com.smellouk.kamper:*`, or the project title `Kamper`.

    CAPACITY.md (2 occurrences):
    - Every `kamper/...` filesystem path becomes `libs/...`. Maven coordinates and class names unchanged.

    .planning/codebase/STRUCTURE.md (extensive sweep + D-09 stale-file fix):
    - Replace all `kamper/` filesystem path segments with `libs/` and all `:kamper:` Gradle paths with `:libs:`.
    - Locate the line that references `kamper/publish.gradle.kts` (per 21-RESEARCH.md "Stale / Deprecated Files" section, this is on or near line 203) and replace the entire reference with `build-logic/src/main/kotlin/KamperPublishPlugin.kt`. If the surrounding sentence claims the file is a Groovy `apply from:` script, update the explanation to describe it as a Kotlin convention plugin applied via `id("kamper.publish")`. The plugin ID `kamper.publish` itself is unchanged.

    .planning/codebase/ARCHITECTURE.md, CONVENTIONS.md, CONCERNS.md, INTEGRATIONS.md, PRINCIPLES.md, STACK.md, TESTING.md (7 files):
    - For each file, replace `kamper/` filesystem segments with `libs/` and `:kamper:` Gradle paths with `:libs:`. Class names, plugin IDs, and Maven coordinates remain unchanged.

    .planning/codebase/adr/adr-001-plugin-architecture.md, adr-002-kmp-expect-actual.md, adr-003-listener-pattern.md, adr-004-no-breaking-changes.md (4 files):
    - Same substitution rule. ADR-001 may reference `kamper/engine/` and `kamper/modules/` — both become `libs/engine/` and `libs/modules/`. ADR-002 may reference module paths in expect/actual examples. ADR-003 references engine/modules paths. ADR-004 (API freeze) confirms Maven coordinates `com.smellouk.kamper:*` are unchanged — DO NOT alter those.

    .planning/ROADMAP.md (2 occurrences):
    - Phase descriptions or completion notes that reference `kamper/modules/...` become `libs/modules/...`. The phase entries' titles, dates, plan checkboxes, and other content are unchanged.

    .planning/milestones/v1.0-REQUIREMENTS.md and v1.0-ROADMAP.md:
    - Same substitution rule applied across path references. Requirement IDs (e.g., `PERF-01`, `OSS-02`) are unchanged.

    Final D-09 cleanup verification — run these commands and ensure they return zero matches (verify there are no remaining stale references in the cleaned up docs):
    - `grep -rn 'publish.gradle.kts' .planning/codebase/`
    - `grep -rn 'kamper/publish' .planning/`

    If grep finds additional `kamper/` references inside `.planning/` outside this plan's file list (e.g., in `.planning/phases/<old-phase>/SUMMARY.md` files), leave them alone — those are historical phase artifacts and out of scope for this rename.
  </action>
  <verify>
    <automated>bash -c "grep -rn ':kamper:\|kamper/' README.md CAPACITY.md .planning/codebase/ .planning/ROADMAP.md .planning/milestones/ 2>/dev/null | grep -v 'github.com/smellouk/kamper' | grep -v 'com.smellouk.kamper' | grep -v 'kamper.kmp.library' | grep -v 'kamper.publish' | grep -v 'kamper.android.config' | wc -l | tr -d ' '" | grep -q '^0$' &amp;&amp; bash -c "grep -rn 'publish.gradle.kts' .planning/codebase/ 2>/dev/null | wc -l | tr -d ' '" | grep -q '^0$' &amp;&amp; grep -c 'KamperPublishPlugin' .planning/codebase/STRUCTURE.md | grep -qE '^[1-9]'</automated>
  </verify>
  <done>
    - README.md, CAPACITY.md, all 8 `.planning/codebase/*.md` files, all 4 ADR files, ROADMAP.md, and both milestone files contain zero unwhitelisted `kamper/` or `:kamper:` strings
    - .planning/codebase/STRUCTURE.md no longer references `kamper/publish.gradle.kts`; it references `build-logic/src/main/kotlin/KamperPublishPlugin.kt` instead (D-09 cleanup complete)
    - `grep -rn 'publish.gradle.kts' .planning/codebase/` returns no matches
    - The plugin ID `kamper.publish` STILL appears wherever it was referenced as a plugin ID (whitelisted)
    - No phase-history SUMMARY.md files in `.planning/phases/<old>/` are touched
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → human reader | Stale `kamper/` paths cause new contributors to run failing commands or look in the wrong directories |
| `.claude/settings.json` → Claude Code agent | Allowlist mismatch causes permission prompts on every renamed Gradle command, slowing automated sessions |
| `.claude/skills/*/SKILL.md` → Claude Code skill execution | Stale paths in SKILL.md cause skill-driven scaffolding/review to operate on non-existent paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Tampering | `.claude/settings.json` allowlist coherence | mitigate | Replace both `:kamper:api:test` and `:kamper:engine:test` allowlist entries with `:libs:` equivalents in one edit. Verified by grep counting exactly one match for each updated entry and zero `:kamper:` strings in the file. Pitfall 6 in 21-RESEARCH.md. |
| T-21-10 | Information Disclosure | Public docs (README.md, CAPACITY.md) | mitigate | Sweep `kamper/` filesystem references; preserve `https://github.com/smellouk/kamper` URLs and `com.smellouk.kamper:*` Maven coordinates explicitly. Verified by whitelisted grep showing zero unintended residue. |
| T-21-11 | Tampering | `.planning/codebase/STRUCTURE.md` integrity | mitigate | D-09 cleanup: replace `kamper/publish.gradle.kts` (non-existent) with the real `build-logic/src/main/kotlin/KamperPublishPlugin.kt`. Verified by grep `publish.gradle.kts` returning zero matches and `KamperPublishPlugin` returning at least one match. |
| T-21-12 | Repudiation | Phase-history artifacts | accept | Old `.planning/phases/<N>/SUMMARY.md` files contain `kamper/` references as a historical record of work done at that phase boundary. These are intentionally not modified — the historical record stays accurate to its commit-time state. |
</threat_model>

<verification>
After completing this plan:

1. Allowlist coherence:
   - `grep 'Bash(./gradlew :libs:api:test:\*)' .claude/settings.json` returns 1 line
   - `grep 'Bash(./gradlew :libs:engine:test:\*)' .claude/settings.json` returns 1 line
   - `grep ':kamper:' .claude/settings.json .claude/settings.local.json` returns no matches

2. CLAUDE.md / skills swept clean:
   - `grep -nh ':kamper:\|kamper/' CLAUDE.md .claude/skills/*/SKILL.md | grep -v 'github.com/smellouk/kamper\|com.smellouk.kamper\|kamper.kmp.library\|kamper.publish\|kamper.android.config\|skills/kamper-'` returns no matches

3. STRUCTURE.md D-09 cleanup:
   - `grep -n 'publish.gradle.kts' .planning/codebase/STRUCTURE.md` returns no matches
   - `grep -n 'KamperPublishPlugin' .planning/codebase/STRUCTURE.md` returns at least one match

4. All `.planning/` documentation swept:
   - `grep -rn ':kamper:\|kamper/' .planning/codebase/ .planning/ROADMAP.md .planning/milestones/ | grep -v 'github.com/smellouk/kamper\|com.smellouk.kamper\|kamper.kmp.library\|kamper.publish\|kamper.android.config'` returns no matches

5. Sample command from updated CLAUDE.md actually works:
   - Pick the smoke-test command from the updated CLAUDE.md Build and Test table
   - Run it: `./gradlew :libs:api:test :libs:engine:test`
   - Expected: exits 0 (no permission prompt because Plan 21-03 Task 1 updated the allowlist)
</verification>

<success_criteria>
- 23 documentation/config files updated as specified
- Zero unwhitelisted `kamper/` filesystem references or `:kamper:` Gradle project path references in:
  CLAUDE.md, README.md, CAPACITY.md, all 8 `.planning/codebase/*.md`, all 4 ADR files, `.planning/ROADMAP.md`, both milestone files, both `.claude/settings*.json`, all 3 SKILL.md files
- D-09 cleanup: `kamper/publish.gradle.kts` replaced with `build-logic/src/main/kotlin/KamperPublishPlugin.kt` in STRUCTURE.md
- `./gradlew :libs:api:test :libs:engine:test` runs without prompting (allowlist coherent)
- Whitelisted strings (GitHub URLs, plugin IDs, Maven coordinates, skill folder names) verified preserved via grep
- All changes land in one commit titled `docs(21): sweep documentation and skills for libs/ rename`
</success_criteria>

<output>
After completion, create `.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-03-SUMMARY.md` covering:
- File-by-file edit counts and notable changes (especially STRUCTURE.md D-09 cleanup)
- Allowlist coherence verification (sample Gradle command runs without prompt)
- Verification grep results (zero unwhitelisted matches)
- Single commit SHA for this wave
- Note any inline structural loose ends discovered and cleaned up under Claude's Discretion (e.g., stray references in IDE config, additional stale doc lines)
</output>

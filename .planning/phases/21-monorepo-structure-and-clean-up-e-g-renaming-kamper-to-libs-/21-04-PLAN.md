---
phase: 21
plan: 04
type: execute
wave: 4
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - README.md
autonomous: true
requirements: []
tags: [docs, readme, oss-polish, phase21]

must_haves:
  truths:
    - "README.md has exactly 11 top-level sections in the order specified by D-12"
    - "Modules table lists all 8 modules (cpu, fps, memory, network, jank, gc, thermal, issues) with 3 columns: Name, Description, Platforms"
    - "No `## Demos` section exists in README.md"
    - "No `## How-tos` section exists in README.md"
    - "No duplicate `## Kamper UI — Android debug overlay` section exists (Kamper UI appears exactly once as the flagship section)"
    - "Every screenshot/GIF reference from the original README is preserved verbatim (1.gif, 2.gif, 3-14 png)"
    - "Quick start shows the full first-time-visitor flow: Maven Central dependency → `Kamper.install(CpuModule)` → `addInfoListener<CpuInfo> { cpu -> ... }` in 3 code blocks (~15 lines total)"
    - "Contributing section links CONTRIBUTING.md, CLAUDE.md, and GitHub Releases"
    - "Service Integrations section keeps full depth for Sentry, Firebase Crashlytics, and OpenTelemetry (dependency coordinate, DSL options table, platform support note per integration)"
    - "Badge strip contains license, release, issues, stars, and a CI badge sourced from `.github/workflows/pull-request.yml`"
    - "All filesystem paths in the rewritten README use `libs/` (not `kamper/`); Maven coordinates remain `com.smellouk.kamper:*` (D-06 unchanged)"
  artifacts:
    - path: "README.md"
      provides: "Renovated project landing page with the 11-section order from D-12"
      contains: "## Modules"
  key_links: []
---

<objective>
Replace the current 554-line README.md with a fully renovated structure that follows the 11-section order locked in D-12. This is not a surgical edit — it is a from-scratch rebuild of the document where existing content (modules, integrations, Kamper UI screenshots, lifecycle, versioning, license) is reorganised, not discarded. Sections that belong in docs/wiki (`## Demos`, `## How-tos`) and the duplicate Kamper UI section are removed.

Purpose: After Phase 20's surgical fixes and Phase 21's monorepo rename, the README still has the old Phase 19 structure: highlights buried under a misplaced Kamper UI section, modules listed twice (table + collapsible details), Quick start AFTER Modules instead of before, and a `## Demos` table that duplicates the demos directory. The Phase 20 retrospective deferred a full structural rewrite to Phase 21 so it ships alongside the `libs/` rename — no double-touch on README.md path references and no double-commit on contributor-visible docs.

Output: One file modified (README.md), one commit titled `docs(21): full README renovation`. The renovated README opens with badges → tagline → modules table → quick start, with Kamper UI as the visual flagship section and integrations preserved at full depth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-CONTEXT.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-RESEARCH.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-PATTERNS.md
@.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-03-PLAN.md

<interfaces>
<!-- The renovated README is a creative document. Below are the EXACT structural contracts the executor must produce. -->

## Locked structural contract — 11 sections in order (D-12)

After completion, `grep -nE '^## ' README.md` MUST output the following sections in this exact order (badges and tagline are above the first `##` header so they don't appear in this grep):

```
## Modules                       <- 3rd visible block (after badges + tagline)
## Quick start                   <- 4th
## Kamper UI                     <- 5th — flagship visual section, screenshots/GIFs preserved
## Service Integrations          <- 6th — full depth, three integrations
## Platform support              <- 7th
## Versioning                    <- 8th
## Contributing                  <- 9th — links to CONTRIBUTING.md, CLAUDE.md, GitHub Releases
## Acknowledgements              <- 10th
## License                       <- 11th
```

(Badge strip + tagline+description are sections 1 and 2 conceptually but live above the first `## ` header.)

## Locked module list (D-12 step 3)

The Modules table MUST contain exactly these 8 module rows in this order: cpu, fps, memory, network, jank, gc, thermal, issues. The table MUST have exactly 3 columns: Name, Description, Platforms.

## Locked integrations (D-14)

Service Integrations section MUST contain three sub-sections in this order: Sentry, Firebase Crashlytics, OpenTelemetry. Each integration MUST contain: dependency coordinate code block, DSL options table, platform support note. Do NOT shorten any integration to a snippet — current depth is the right call (D-14).

## Path & coordinate rules (already enforced by 21-01..03)

- Filesystem paths: use `libs/` (e.g., `libs/modules/`, `libs/integrations/`). NEVER `kamper/`.
- Maven coordinates: use `com.smellouk.kamper:*` verbatim (D-06 — unchanged).
- Repository URLs: use `https://github.com/smellouk/kamper` verbatim.
- Plugin IDs: not normally referenced from README, but if added, use `kamper.kmp.library`, `kamper.publish`, `kamper.android.config` verbatim.

## Screenshots/GIFs — preserve verbatim (D-15)

The following image references from the current README MUST appear in the renovated README's Kamper UI section (URLs preserved character-for-character):

- `screenshots/1.gif` (right-aligned hero, width 320)
- `screenshots/2.gif` (centered, width 300)
- `screenshots/3.png`, `screenshots/4.png`, `screenshots/5.png` (chip states, width 160)
- `screenshots/7.png`, `screenshots/8.png`, `screenshots/9.png`, `screenshots/10.png` (panel tabs, width 160)

The other screenshots (6.png, 11–14) are demos screenshots and MAY be omitted with the `## Demos` section removal.

## CI badge — workflow choice (D-12 step 1, Claude's Discretion)

`.github/workflows/pull-request.yml` is the suitable CI workflow. Use this badge URL:

`https://img.shields.io/github/actions/workflow/status/smellouk/kamper/pull-request.yml?branch=main&label=CI`

Linked to: `https://github.com/smellouk/kamper/actions/workflows/pull-request.yml`

## Quick start — exact 3 code blocks (D-12 step 4)

Block 1 — Gradle Maven Central dependency (~5 lines):
```kotlin
// build.gradle.kts (Module: app)
dependencies {
    implementation("com.smellouk.kamper:engine:$kamperVersion")
    implementation("com.smellouk.kamper:cpu-module:$kamperVersion")
}
```

Block 2 — Kamper engine setup + module install (~5 lines):
```kotlin
Kamper.setup {
    logger = Logger.DEFAULT
}.apply {
    install(CpuModule)
    start()
}
```

Block 3 — listener registration (~5 lines):
```kotlin
Kamper.addInfoListener<CpuInfo> { cpu ->
    if (cpu == CpuInfo.INVALID) return@addInfoListener
    println("CPU: ${cpu.totalUseRatio}")
}
```

Lifecycle (D-12 step 4 says "Lifecycle condensed here") — append a 4-line code block at the bottom of Quick start:
```kotlin
Kamper.start()   // begin polling
Kamper.stop()    // pause polling
Kamper.clear()   // uninstall + remove listeners
```

## Sections that MUST NOT exist in the renovated README (D-13)

- `## Demos` — gone (table belongs in `demos/README.md` or wiki, not project README)
- `## How-tos` — gone (publish-to-local-maven and create-custom-module belong in CONTRIBUTING.md)
- A second `## Kamper UI — Android debug overlay` section — the original README had Kamper UI documented twice; the renovation keeps exactly ONE Kamper UI section (the flagship section #5)
- The collapsible per-module `<details>` blocks under "## Modules" — replaced by the 3-column table
- The "## Installation" section as a standalone heading — folded into Quick start step 1
- The "## Lifecycle" standalone heading — folded into Quick start (D-12 step 4)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full structural rewrite of README.md (11 sections per D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — current state after 21-03 path sweep; this is the source material being reorganised, NOT discarded)
    - CONTRIBUTING.md (verify the link target exists; the Contributing section links to it)
    - CLAUDE.md (verify it exists at repo root; Contributing section links to it as the contributor reference for commit conventions and build commands per CONTEXT.md `<specifics>`)
    - .github/workflows/pull-request.yml (confirm this is the suitable CI workflow for the badge — used to construct the shields.io badge URL)
    - libs/modules/ directory listing — confirm the 8 modules: cpu, fps, gc, issues, jank, memory, network, thermal (post-rename names)
    - libs/integrations/ directory listing — confirm the 3 integrations: firebase, opentelemetry, sentry (post-rename names)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-CONTEXT.md (D-11 through D-17 — the renovation decisions)
    - .planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-03-PLAN.md (Task 2 — confirms 21-03 only changed README.md path references in the Contributing section, not structure)
    - screenshots/ directory listing — verify all referenced GIFs/PNGs exist (1.gif, 2.gif, 3-14.png)
  </read_first>
  <action>
    Rewrite `README.md` from scratch following the structural contract in `<interfaces>` above. Existing content is REORGANISED, not discarded — every module description, every integration DSL table, every Kamper UI screenshot from the current README finds a home in the new structure.

    Produce the file in the EXACT order below. Each section's content requirements are spelled out concretely so the executor does not have to interpret.

    ---

    ## Section 1 — Badge strip (D-12 step 1)

    First lines of the file (after the H1 `# Kamper`):

    Five badges, each on its own line in the order: License, Release, Issues, Stars, CI.

    Use these exact badge URLs:

    1. License: `[![License](https://img.shields.io/github/license/smellouk/kamper)](LICENSE)` — preserve from current README
    2. Release: `[![Release](https://img.shields.io/github/v/tag/smellouk/kamper?label=release&color=blue)](https://github.com/smellouk/kamper/releases)` — preserve
    3. Issues: `[![Issues](https://img.shields.io/github/issues/smellouk/kamper)](https://github.com/smellouk/kamper/issues)` — preserve
    4. Stars: `[![Stars](https://img.shields.io/github/stars/smellouk/kamper?style=social)](https://github.com/smellouk/kamper/stargazers)` — preserve
    5. CI: `[![CI](https://img.shields.io/github/actions/workflow/status/smellouk/kamper/pull-request.yml?branch=main&label=CI)](https://github.com/smellouk/kamper/actions/workflows/pull-request.yml)` — NEW (sourced from `.github/workflows/pull-request.yml`)

    ## Section 2 — Tagline + one-paragraph description (D-12 step 2)

    Reuse the current README's tagline verbatim:

    > **Kotlin Multiplatform performance monitoring.** A plugin-based library that gives you live CPU, FPS, memory, network, jank, GC, thermal, and issue detection across Android, iOS, JVM, macOS, and Web — through a single unified API.

    Follow with a one-paragraph description (3–4 sentences) that calls out: zero-boilerplate `install()` model, the Kamper UI debug overlay flagship feature, and the multiplatform reach (Android, iOS, JVM, macOS, JS, Wasm). Do NOT replicate the existing `### Highlights` bullet list verbatim — fold its key points into prose. The hero `screenshots/1.gif` (right-aligned, width 320) MAY be inline here OR moved to the Kamper UI section. Choose whichever produces tighter visual hierarchy. (Recommend: keep `screenshots/1.gif` here for hero placement.)

    ## Section 3 — Modules (D-12 step 3)

    H2 header: `## Modules`

    Lead with one short sentence: "Kamper ships eight independently-installable performance modules. Install only what you need."

    Then a 3-column markdown table with exactly these 8 rows in this order:

    | Name | Description | Platforms |
    |------|-------------|-----------|
    | CPU | Total, user, system, and per-app CPU usage ratios | Android · iOS · JVM · macOS · Web |
    | FPS | Frames per second via platform frame-timing APIs | Android · iOS · JVM · macOS · Web |
    | Memory | Heap usage, PSS (Android), and available RAM | Android · iOS · JVM · macOS · Web¹ |
    | Network | Bytes received / transmitted per interval | Android² · iOS · JVM · macOS · Web³ |
    | Jank | Dropped frames and slow renders | Android · JVM |
    | GC | Garbage collection runs and pause time | Android · JVM |
    | Thermal | Device thermal state and throttling | Android |
    | Issues | ANR, crash, dropped frames, memory pressure, slow start | Android · JVM |

    Preserve the existing footnote block (¹ ² ³) from the current Platform support section verbatim — these footnotes describe the partial-platform asterisks.

    REMOVE the per-module `<details>` collapsible code blocks that the current README has under `## Modules` — they are replaced by the table. The detailed install + listener snippets for each module live in `libs/modules/<name>/README.md` (out of scope for this plan).

    ## Section 4 — Quick start (D-12 step 4)

    H2 header: `## Quick start`

    Lead with one sentence: "Three steps to live performance metrics."

    Three code blocks in order — the EXACT contents are specified in `<interfaces>` above:

    1. **Step 1 — Add Maven Central dependencies** (Gradle Kotlin DSL block, ~5 lines, includes engine + cpu-module)
    2. **Step 2 — Set up Kamper and install a module** (Kamper.setup + install(CpuModule) + start, ~5 lines)
    3. **Step 3 — Register a listener** (Kamper.addInfoListener<CpuInfo>, ~5 lines)

    After the three steps, append a `### Lifecycle` H3 sub-section (D-12 step 4 — "Lifecycle condensed here") with one 4-line code block:

    ```kotlin
    Kamper.start()   // begin polling
    Kamper.stop()    // pause polling
    Kamper.clear()   // uninstall + remove listeners
    ```

    Optionally include a one-liner immediately after about Android lifecycle integration: "On Android, `lifecycle.addObserver(Kamper)` wires `start`/`stop`/`clear` to the Activity lifecycle automatically." The 6-line `class MainActivity` example from the current README is OPTIONAL — include it only if it fits the ~15-line quick start budget. Default: omit (callers can find it in the Android demo).

    ## Section 5 — Kamper UI (D-12 step 5, D-15)

    H2 header: `## Kamper UI`

    This is the flagship visual section. It MUST appear exactly ONCE (the current README has it twice — second occurrence at line 300 is removed per D-13).

    Required content:
    - One paragraph describing what the UI is: floating chip overlay, drag-and-snap, shake-to-restore.
    - **Android: zero app code required** — note the auto-init `ContentProvider` for debug builds.
    - **iOS: one line in `AppDelegate`** — show the `KamperUi.shared.attach()` swift snippet.
    - The `screenshots/2.gif` centered image (width 300) — preserve URL verbatim.
    - **Panel tabs** — `<table>` with 4 columns: Activity, Perfetto, Issues, Settings; uses `screenshots/7.png`, `screenshots/8.png`, `screenshots/9.png`, `screenshots/10.png` (preserve URLs verbatim, width 160).
    - **Chip states** — `<table>` with 3 columns: Peek (default), Expanded — core metrics, Expanded — all metrics; uses `screenshots/3.png`, `screenshots/5.png`, `screenshots/4.png` (preserve URLs verbatim, width 160).
    - The `KamperUi.configure { ... }` snippet showing `isEnabled` + `position` (ChipPosition values).
    - The Perfetto tracing paragraph — preserve content; keep the `ui.perfetto.dev` link.
    - The `debugImplementation("com.smellouk.kamper:ui-android:$kamperVersion")` install line for Android.

    Source these from the current README's two Kamper UI sections; merge them into one cohesive section. The current "## Kamper UI" (lines 24–66) and "## Kamper UI — Android debug overlay" (lines 300–336) are merged.

    ## Section 6 — Service Integrations (D-12 step 6, D-14)

    H2 header: `## Service Integrations`

    Lead paragraph (preserve from current README, lines 365–367): describes that integrations forward Kamper events to third-party observability services, all flags default to `false`, attach via `addIntegration()`.

    Quick teaser code block (preserve from current README, lines 371–381): the `Kamper.install(...).addIntegration(SentryModule(...) { forwardIssues = true })` example.

    Then THREE H3 sub-sections in this order — each at FULL DEPTH (D-14 — do NOT shorten):

    **`### Sentry`** — preserve the entire current Sentry block (lines 384–406):
    - One-paragraph description of what Sentry does (captureException for issues, breadcrumbs for CPU/Memory/FPS).
    - **Dependency:** code block with `implementation("com.smellouk.kamper:sentry-integration:$kamperVersion")`.
    - **Supported platforms:** Android, iOS, JVM, macOS (JS and WasmJS excluded — sentry-kotlin-multiplatform 0.13.0 limitation).
    - **DSL options:** the 5-row table (dsn, forwardIssues, forwardCpuAbove, forwardMemoryAbove, forwardFps) — preserve verbatim.

    **`### Firebase Crashlytics`** — preserve the entire current Firebase block (lines 410–441):
    - One-paragraph description (IssueInfo → Crashlytics non-fatal exceptions; CPU/memory/FPS not forwarded).
    - **Dependency:** code block with `implementation("com.smellouk.kamper:firebase-integration:$kamperVersion")`.
    - **Supported platforms:** Android, iOS; no-op on JVM/macOS/JS/WasmJS.
    - The `> Note:` blockquote about `google-services.json` and `GoogleService-Info.plist`.
    - **DSL usage:** code block showing `FirebaseModule { forwardIssues = true }`.

    **`### OpenTelemetry (Grafana, Datadog, New Relic, Honeycomb, …)`** — preserve the entire current OTel block (lines 445–490):
    - Description (OTLP HTTP gauge export, one endpoint covers all backends).
    - **Dependency:** `implementation("com.smellouk.kamper:opentelemetry-integration:$kamperVersion")`.
    - **Supported platforms:** Android, JVM (real OTLP); no-op on iOS/macOS/JS/WasmJS.
    - **DSL usage:** the OpenTelemetryModule code block showing otlpEndpointUrl, otlpAuthToken, forwardCpu, forwardMemory, forwardFps, exportIntervalSeconds.
    - **DSL options:** the 6-row table — preserve verbatim.

    ## Section 7 — Platform support (D-12 step 7)

    H2 header: `## Platform support`

    Reuse the current README's matrix verbatim (lines 70–82): 9 rows (CPU, FPS, Memory, Network, Jank, GC, Thermal, Issues, Kamper UI) × 5 platform columns (Android, iOS, JVM, macOS, Web).

    Preserve the four footnotes (¹ ² ³ ⁴) verbatim. Note: if the footnotes are already attached under the Modules table (Section 3), avoid duplication — render them under the Modules table only and reference here as "see Modules section for partial-platform notes" OR keep them under Platform support and reference from Modules — either is acceptable but pick ONE location for the footnotes to live and reference from the other if needed.

    ## Section 8 — Versioning (D-12 step 8)

    H2 header: `## Versioning`

    Preserve the current Versioning section verbatim (lines 514–522): semantic versioning bullets (Patch, Minor, Major), GitHub Releases link, CHANGELOG.md link.

    ## Section 9 — Contributing (D-12 step 9)

    H2 header: `## Contributing`

    Three explicit links (D-12 step 9 — "Contributing + Links: CONTRIBUTING.md, CLAUDE.md, GitHub Releases"):

    - [`CONTRIBUTING.md`](CONTRIBUTING.md) — contribution guide (PR process, commit conventions)
    - [`CLAUDE.md`](CLAUDE.md) — operational reference for build commands, module patterns, and commit conventions used by both Claude agents and human contributors
    - [GitHub Releases](https://github.com/smellouk/kamper/releases) — published release notes

    Lead with one sentence: "Contributions are welcome." Then list the three links as a bullet list.

    Optionally include a sub-paragraph: "New module idea? Browse [`libs/modules/`](libs/modules/) for the canonical 4-class structure (Info, Config, Watcher, Performance), or run the `/kamper-new-module` Claude skill if you have Claude Code." This replaces the removed `## How-tos > Create a custom performance module` section content.

    ## Section 10 — Acknowledgements (D-12 step 10)

    H2 header: `## Acknowledgements`

    Preserve verbatim from current README (line 534): "Inspired by [AndroidGodEye](https://github.com/Kyson/AndroidGodEye) by Kyson."

    ## Section 11 — License (D-12 step 11)

    H2 header: `## License`

    Preserve verbatim from current README (lines 540–554): the full Apache 2.0 license block.

    ---

    ## Sections that MUST NOT exist in the output

    Verify these sections are GONE (per D-13):

    - No `## Demos` H2 anywhere — the demos table at lines 339–349 is removed.
    - No `## How-tos` H2 anywhere — the publish-to-local-maven and create-custom-module collapsibles at lines 493–509 are removed (the create-custom-module hint is folded into Section 9 Contributing).
    - No second `## Kamper UI — Android debug overlay` H2 — the duplicate at line 300 is merged into Section 5.
    - No standalone `## Installation` H2 — folded into Section 4 step 1.
    - No standalone `## Lifecycle` H2 — folded into Section 4 sub-section.
    - No per-module `<details>` collapsibles under Modules — replaced by the 3-column table.

    ## Path & coordinate enforcement

    - All filesystem paths in the renovated README MUST use `libs/` (e.g., `libs/modules/`, `libs/integrations/`). Plan 21-03 already swept the existing README's one occurrence (`kamper/modules/` → `libs/modules/` in the contributing section) — keep it `libs/`.
    - Maven coordinates MUST be `com.smellouk.kamper:*` verbatim (D-06). NEVER rewrite to `com.smellouk.libs:*`.
    - Repository URLs MUST be `https://github.com/smellouk/kamper` verbatim.

    ## Editing strategy

    Use the Write tool to OVERWRITE the existing README.md with the renovated content. Do NOT use Edit (this is a structural rewrite, not a surgical edit). Read README.md first (the read_first list mandates this) so the executor sees the source content being reorganised.

    Section dividers (`---` horizontal rules) between H2 sections are encouraged but not required — current README uses them; preserve them in the rewrite.
  </action>
  <verify>
    <automated>bash -c "set -e; F=README.md; \
      grep -nE '^## ' \"$F\" | awk -F'## ' '{print $2}' | head -20; \
      test \"$(grep -cE '^## Modules\\b' \"$F\")\" -ge 1 && \
      test \"$(grep -cE '^## Quick start\\b' \"$F\")\" -ge 1 && \
      test \"$(grep -cE '^## Kamper UI\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Service Integrations\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Platform support\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Versioning\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Contributing\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Acknowledgements\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## License\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^## Demos\\b' \"$F\")\" -eq 0 && \
      test \"$(grep -cE '^## How-tos\\b' \"$F\")\" -eq 0 && \
      test \"$(grep -cE '^## Installation\\b' \"$F\")\" -eq 0 && \
      test \"$(grep -cE '^## Lifecycle\\b' \"$F\")\" -eq 0 && \
      test \"$(grep -cE '^## Kamper UI — Android debug overlay' \"$F\")\" -eq 0 && \
      grep -q 'screenshots/1.gif' \"$F\" && \
      grep -q 'screenshots/2.gif' \"$F\" && \
      grep -q 'screenshots/3.png' \"$F\" && \
      grep -q 'screenshots/4.png' \"$F\" && \
      grep -q 'screenshots/5.png' \"$F\" && \
      grep -q 'screenshots/7.png' \"$F\" && \
      grep -q 'screenshots/8.png' \"$F\" && \
      grep -q 'screenshots/9.png' \"$F\" && \
      grep -q 'screenshots/10.png' \"$F\" && \
      grep -q 'img.shields.io/github/license/smellouk/kamper' \"$F\" && \
      grep -q 'img.shields.io/github/v/tag/smellouk/kamper' \"$F\" && \
      grep -q 'img.shields.io/github/issues/smellouk/kamper' \"$F\" && \
      grep -q 'img.shields.io/github/stars/smellouk/kamper' \"$F\" && \
      grep -q 'actions/workflow/status/smellouk/kamper/pull-request.yml' \"$F\" && \
      grep -q 'com.smellouk.kamper:engine' \"$F\" && \
      grep -q 'com.smellouk.kamper:cpu-module' \"$F\" && \
      grep -q 'com.smellouk.kamper:sentry-integration' \"$F\" && \
      grep -q 'com.smellouk.kamper:firebase-integration' \"$F\" && \
      grep -q 'com.smellouk.kamper:opentelemetry-integration' \"$F\" && \
      grep -q 'install(CpuModule)' \"$F\" && \
      grep -qE 'addInfoListener<CpuInfo>' \"$F\" && \
      grep -q 'CONTRIBUTING.md' \"$F\" && \
      grep -q 'CLAUDE.md' \"$F\" && \
      grep -q 'github.com/smellouk/kamper/releases' \"$F\" && \
      test \"$(grep -cE '^### Sentry\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^### Firebase Crashlytics\\b' \"$F\")\" -eq 1 && \
      test \"$(grep -cE '^### OpenTelemetry' \"$F\")\" -eq 1 && \
      ! grep -E 'kamper/modules/|kamper/integrations/' \"$F\" | grep -v 'github.com/smellouk/kamper'; \
      echo OK"</automated>
  </verify>
  <done>
    - README.md exists and contains exactly 9 H2 sections in order: Modules, Quick start, Kamper UI, Service Integrations, Platform support, Versioning, Contributing, Acknowledgements, License (badges + tagline are above the first H2 — sections 1 and 2 conceptually).
    - The H2 `## Kamper UI` appears exactly once (no `## Kamper UI — Android debug overlay` duplicate).
    - The H2s `## Demos`, `## How-tos`, `## Installation`, `## Lifecycle` do NOT appear.
    - The Modules table is a markdown table with 3 columns (Name, Description, Platforms) and 8 rows for cpu, fps, memory, network, jank, gc, thermal, issues.
    - Quick start contains 3 numbered steps and one Lifecycle code block; the listener block uses `addInfoListener<CpuInfo>`.
    - Service Integrations contains exactly 3 H3 sub-sections: `### Sentry`, `### Firebase Crashlytics`, `### OpenTelemetry (...)`. Each has its dependency code block, DSL options table (or DSL usage block for Firebase), and platform support note.
    - Badge strip contains 5 badges: license, release, issues, stars, CI. The CI badge URL points to `actions/workflow/status/smellouk/kamper/pull-request.yml`.
    - All preserved screenshots (1.gif, 2.gif, 3.png, 4.png, 5.png, 7.png, 8.png, 9.png, 10.png) appear in the rewritten README with URLs verbatim.
    - Maven coordinate strings `com.smellouk.kamper:engine`, `:cpu-module`, `:sentry-integration`, `:firebase-integration`, `:opentelemetry-integration` are present.
    - Contributing section links CONTRIBUTING.md, CLAUDE.md, and `github.com/smellouk/kamper/releases`.
    - No `kamper/modules/` or `kamper/integrations/` filesystem paths anywhere (only `libs/`-prefixed paths or no path).
    - All shields.io badge URLs and GitHub URLs continue to use the canonical `smellouk/kamper` repository slug (those are URLs, not filesystem paths — D-06 / 21-03 whitelist).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → first-time visitor | Stale or wrong install instructions cause failed first-build experiences and abandonment |
| README.md → search engines / package indexes | The README is the SEO surface for the project; structural quality affects discoverability |
| README.md → contributors | Wrong paths or missing contribution links delay first PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Information Disclosure | README.md install instructions | mitigate | Quick start uses verified Maven coordinates `com.smellouk.kamper:engine` and `:cpu-module` (D-06 — unchanged). Verified by grep ensuring those exact strings appear in the rewritten README. |
| T-21-14 | Information Disclosure | README.md modules table accuracy | mitigate | Modules table enumerated from `libs/modules/` directory listing (8 directories: cpu, fps, gc, issues, jank, memory, network, thermal). Verified by `read_first` requirement to enumerate the directory before writing. |
| T-21-15 | Tampering | Screenshot/GIF references | mitigate | All preserved screenshot URLs (`screenshots/1.gif` through `screenshots/10.png` minus 6) verified to exist on disk before final write. Grep verifies each preserved URL appears in the output. |
| T-21-16 | Information Disclosure | Stale path references after rename | mitigate | Verify command checks no `kamper/modules/` or `kamper/integrations/` filesystem paths exist (only `libs/`). The whitelist exception for `github.com/smellouk/kamper` URLs is enforced by grep `-v` filter. |
| T-21-17 | Repudiation | CI badge accuracy | accept | The CI badge points to `pull-request.yml`. If that workflow is later renamed or deleted, the badge breaks. Accepted because (a) the workflow is the most stable CI workflow in `.github/workflows/`, (b) shields.io renders a clear "no status" if the workflow is gone, and (c) follow-up renames would happen as part of the rename phase that touches the badge URL. |
</threat_model>

<verification>
After completing this plan:

1. **Section structure** (run after Task 1):
   - `grep -nE '^## ' README.md` shows the 9 H2 headers in order: Modules, Quick start, Kamper UI, Service Integrations, Platform support, Versioning, Contributing, Acknowledgements, License.
   - `grep -cE '^## Demos\b|^## How-tos\b|^## Installation\b|^## Lifecycle\b|^## Kamper UI — Android debug overlay' README.md` returns 0.

2. **Modules table integrity:**
   - `grep -A 12 '^## Modules$' README.md | grep -c '^|'` returns at least 10 (header row + separator row + 8 data rows).
   - All 8 module names appear: `grep -ciE '\bCPU\b|\bFPS\b|\bMemory\b|\bNetwork\b|\bJank\b|\bGC\b|\bThermal\b|\bIssues\b' README.md` returns at least 8.

3. **Screenshots preserved (D-15):**
   - All 9 preserved image references resolve to files on disk:
     `for f in 1.gif 2.gif 3.png 4.png 5.png 7.png 8.png 9.png 10.png; do test -f screenshots/$f || echo MISSING $f; done` produces no output.
   - All 9 image references appear in README.md: grep each.

4. **Quick start integrity (D-12 step 4):**
   - The three Quick start code blocks are present: `grep -c '^```kotlin' README.md` returns at least 8 (3 in Quick start + 1 lifecycle + 3 in Service Integrations + 1+ in Kamper UI).
   - The full chain `engine` → `install(CpuModule)` → `addInfoListener<CpuInfo>` is verifiable: `grep -q 'com.smellouk.kamper:engine' README.md && grep -q 'install(CpuModule)' README.md && grep -q 'addInfoListener<CpuInfo>' README.md`.

5. **Service Integrations full depth (D-14):**
   - `grep -cE '^### (Sentry|Firebase Crashlytics|OpenTelemetry)' README.md` returns 3.
   - All three integration Maven coordinates appear.

6. **Badge strip with CI (D-12 step 1):**
   - `grep -c 'img.shields.io/github/' README.md` returns 5 (license, release, issues, stars, CI).
   - CI badge URL: `grep -q 'actions/workflow/status/smellouk/kamper/pull-request.yml' README.md`.

7. **Contributing links (D-12 step 9):**
   - All three links present: `grep -q 'CONTRIBUTING.md' README.md && grep -q 'CLAUDE.md' README.md && grep -q 'github.com/smellouk/kamper/releases' README.md`.

8. **Path coherence (no rename regressions):**
   - `grep 'kamper/modules/\|kamper/integrations/' README.md | grep -v 'github.com/smellouk/kamper'` returns no output (only `libs/` paths or no paths).
   - Maven coordinates `com.smellouk.kamper:*` preserved: `grep -c 'com.smellouk.kamper:' README.md` returns at least 5 (engine, cpu-module, sentry-, firebase-, opentelemetry-).

9. **Visual inspection** (Claude reads the rendered file once after writing):
   - Read README.md end-to-end to confirm prose flows naturally and no orphaned content from the source rewrite was dropped (Versioning, License, Acknowledgements all intact).
</verification>

<success_criteria>
- README.md is rewritten with exactly 11 conceptual sections (5 badges as section 1, tagline+description as section 2, then 9 H2 sections from Modules through License) in the order locked by D-12.
- Modules table contains all 8 modules in 3 columns (Name, Description, Platforms).
- `## Demos`, `## How-tos`, and the duplicate `## Kamper UI — Android debug overlay` sections are removed (D-13).
- Service Integrations keeps full depth — three sub-sections, each with dependency, DSL options, and platform support note (D-14).
- All preserved screenshots/GIFs from the original README appear with URLs verbatim (D-15).
- Quick start shows the full first-time-visitor flow in 3 code blocks plus a Lifecycle block.
- Badge strip includes a CI badge sourced from `.github/workflows/pull-request.yml`.
- Contributing section links CONTRIBUTING.md, CLAUDE.md, and GitHub Releases.
- All filesystem paths use `libs/` (Plan 21-03's prior sweep is preserved); Maven coordinates remain `com.smellouk.kamper:*` (D-06 unchanged).
- The change lands in one commit titled `docs(21): full README renovation`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-monorepo-structure-and-clean-up-e-g-renaming-kamper-to-libs-/21-04-SUMMARY.md` covering:
- Section-by-section overview of the renovated README and what content was reused vs newly written.
- Confirmation of D-13 removals (`## Demos`, `## How-tos`, duplicate Kamper UI).
- Confirmation of D-15 screenshot preservation (list each preserved URL).
- Confirmation of D-14 integrations full depth (list each integration's preserved blocks).
- CI badge URL chosen and the workflow it points to (`pull-request.yml`).
- Verification grep outputs (section list, badge count, integration count, no stale `kamper/modules/` references).
- Single commit SHA for this wave.
- The exact commit message used: `docs(21): full README renovation`.
</output>

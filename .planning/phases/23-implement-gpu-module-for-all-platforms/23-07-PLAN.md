---
phase: 23
plan: 07
type: execute
wave: 1
depends_on: [23-03, 23-05]
files_modified:
  - demos/jvm/build.gradle.kts
  - demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt
  - demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt
  - demos/web/build.gradle.kts
  - demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt
  - demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt
autonomous: true
gap_closure: true
requirements: [D-01, D-04, D-09, D-13]
must_haves:
  truths:
    - "JVM demo (`./gradlew :demos:jvm:run`) shows a GPU tab between CPU and FPS in the JTabbedPane"
    - "JVM demo installs GpuModule and updates the GPU tab on every poll; UNSUPPORTED on hosts without OSHI GPU info renders \"Unsupported\""
    - "Web demo (`./gradlew :demos:web:jsBrowserDevelopmentRun`) shows a GPU tab between CPU and FPS in the nav"
    - "Web demo installs GpuModule and renders \"Unsupported\" (jsMain returns GpuInfo.UNSUPPORTED unconditionally per D-08)"
  artifacts:
    - path: "demos/jvm/build.gradle.kts"
      provides: "Adds :libs:modules:gpu dependency"
      contains: 'implementation(project(":libs:modules:gpu"))'
    - path: "demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt"
      provides: "Wires GpuPanel + GpuModule install + GpuInfo listener"
      contains: "install(GpuModule)"
    - path: "demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt"
      provides: "Swing JPanel mirroring ThermalPanel.kt — shows utilization %, used/total VRAM MB, UNSUPPORTED state"
      min_lines: 80
    - path: "demos/web/build.gradle.kts"
      provides: "Adds :libs:modules:gpu dependency to jsMain"
      contains: 'implementation(project(":libs:modules:gpu"))'
    - path: "demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt"
      provides: "Adds GPU tab name, GpuSection.build, GpuModule install, GpuInfo listener"
      contains: "install(GpuModule)"
    - path: "demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt"
      provides: "DOM-based GPU section mirroring ThermalSection.kt — UNSUPPORTED on web per D-08"
      min_lines: 50
  key_links:
    - from: "demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt"
      to: "GpuModule"
      via: "Kamper.install(GpuModule) inside setupKamper()"
      pattern: "install\\(GpuModule\\)"
    - from: "demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt"
      to: "GpuPanel.update"
      via: "addInfoListener<GpuInfo> { gpuPanel.update(it) }"
      pattern: "addInfoListener<GpuInfo>"
    - from: "demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt"
      to: "GpuModule"
      via: "Kamper.install(GpuModule)"
      pattern: "install\\(GpuModule\\)"
    - from: "demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt"
      to: "GpuSection.update"
      via: "addInfoListener<GpuInfo> { GpuSection.update(it) }"
      pattern: "addInfoListener<GpuInfo>"
---

<objective>
Wire `GpuModule` into the JVM (Swing) and Web (HTML/DOM) demo apps, mirroring the existing `ThermalModule` pattern. Add a GPU tab between CPU and FPS in each demo, install `GpuModule`, register a `GpuInfo` listener, and create a per-platform UI component (`GpuPanel.kt` Swing JPanel + `GpuSection.kt` web DOM section). Both demos must distinguish `GpuInfo.UNSUPPORTED` (gray "Unsupported" label) from `GpuInfo.INVALID` (skip update like other modules) and from valid readings (utilization % + memory MB).

Purpose: Close UAT gap "All demo apps install GpuModule and show a GPU screen / tile / section mirroring the ThermalModule demo pattern" for the two fastest-to-verify platforms (pure JVM + JS, no device required).

Output: 6 file modifications across two demos. JVM demo runs via `./gradlew :demos:jvm:run`. Web demo runs via `./gradlew :demos:web:jsBrowserDevelopmentRun`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-CONTEXT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-UAT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-03-SUMMARY.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-05-SUMMARY.md

# Reference files — DO NOT modify, read for pattern only
@demos/jvm/build.gradle.kts
@demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt
@demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/ThermalPanel.kt
@demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/Theme.kt
@demos/web/build.gradle.kts
@demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt
@demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/ThermalSection.kt
@demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/DomUtils.kt

# GPU module API surface
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuInfo.kt
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuConfig.kt

<interfaces>
<!-- The two consumer-facing classes both demos depend on. -->

From libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuInfo.kt:
```kotlin
data class GpuInfo(
    val utilization: Double,    // 0..100, or -1.0 (INVALID), or -2.0 (UNSUPPORTED)
    val usedMemoryMb: Double,   // MB, or -1.0 (unknown), or -2.0 (UNSUPPORTED)
    val totalMemoryMb: Double   // MB, or -1.0 (unknown), or -2.0 (UNSUPPORTED)
) : Info {
    companion object {
        val INVALID     = GpuInfo(-1.0, -1.0, -1.0)  // transient read failure (D-03)
        val UNSUPPORTED = GpuInfo(-2.0, -2.0, -2.0)  // platform capability gap (D-04)
    }
}
```

From libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/Module.kt (commonMain):
```kotlin
expect val GpuModule: PerformanceModule<GpuConfig, GpuInfo>
```

JVM actual returns OSHI partial data: utilization is ALWAYS -1.0 because OSHI exposes no GPU
utilization API; totalMemoryMb may be a real value when OSHI finds a GPU. UNSUPPORTED when no
GPU visible. See 23-03-SUMMARY.md for details.

JS actual returns GpuInfo.UNSUPPORTED unconditionally (D-08, browser GPU APIs blocked by Spectre).
</interfaces>

<jvm_pattern_reference>
ThermalPanel pattern (mirror this exactly for GpuPanel):
- JPanel(BorderLayout(0, 0)) with three sections: top hero card (BorderLayout.NORTH), stats grid (CENTER), footer (SOUTH)
- bigLabel JLabel for the primary metric — large monospaced font, color-coded
- update(info) checks INVALID (return), UNSUPPORTED (set "N/A" + MUTED), valid (set value + accent color)
- All UI updates wrapped in SwingUtilities.invokeLater
- Theme.MAUVE accent for GPU (matches the libs/ui/kmm KamperPanel UI-SPEC choice in 23-06)
</jvm_pattern_reference>

<web_pattern_reference>
ThermalSection pattern (mirror this exactly for GpuSection):
- internal object with build(container) + update(info) functions
- Uses DomUtils helpers (div, p, span, button) to construct DOM
- Stores element refs in lateinit private vars
- update() handles INVALID/UNSUPPORTED/valid the same way as Thermal
- Color hex: GPU = "#cba6f7" (Catppuccin mauve, matches KamperPanel MAUVE)
</web_pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire GpuModule into JVM Swing demo (build.gradle.kts + Main.kt + GpuPanel.kt)</name>
  <files>demos/jvm/build.gradle.kts, demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt, demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt</files>
  <action>
1. **demos/jvm/build.gradle.kts** — Add `implementation(project(":libs:modules:gpu"))` to the `dependencies { ... }` block. Place it alphabetically (after `:libs:modules:gc`, before `:libs:modules:issues`) to match module order conventions.

2. **demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt** — Create new file. Mirror ThermalPanel.kt structurally:
   - Class `GpuPanel : JPanel(BorderLayout(0, 0))`
   - Three labels:
     - `bigLabel: JLabel` — primary utilization, monospaced 48pt, initialized "—"
     - `unitLabel: JLabel` — fixed text "GPU usage %"
     - `memoryLabel: JLabel` — "Memory:  —" (replaces ThermalPanel's `throttlingLabel`)
   - No stress button (GPU has no stress simulator yet — leave footer empty or omit footer entirely; mirror what JankPanel does if present, otherwise skip the footer)
   - `update(info: GpuInfo)`:
     - if `info == GpuInfo.INVALID` → return (no UI churn per D-13)
     - if `info == GpuInfo.UNSUPPORTED` → SwingUtilities.invokeLater { bigLabel.text = "Unsupported"; bigLabel.foreground = Theme.MUTED; memoryLabel.text = "Memory:  N/A" }
     - else → SwingUtilities.invokeLater { bigLabel.text = "%.1f%%".format(info.utilization); bigLabel.foreground = Theme.MAUVE (D-10 accent); memoryLabel.text = if info.usedMemoryMb >= 0.0 && info.totalMemoryMb >= 0.0 then "Memory:  %.0f MB / %.0f MB".format(usedMb, totalMb) else if info.totalMemoryMb >= 0.0 then "Memory:  — / %.0f MB".format(totalMb) else "Memory:  N/A" }
   - Color choice rationale (D-10/D-11): mauve = `Theme.MAUVE` from existing Theme.kt (already exported). MUST match the KamperPanel GPU tile's MAUVE accent (consistent with libs/ui/kmm).
   - Use Theme.LABEL_FONT for memoryLabel; Font("Monospaced", Font.BOLD, 48) for bigLabel
   - Border / padding: copy ThermalPanel structure verbatim (topCard with EmptyBorder(24,20,16,20), statsPanel GridLayout(1,1) with EmptyBorder(16,20,12,20))
   - **Detekt:** Avoid magic numbers — extract any literal floats/percentages into a `private companion object` (e.g., `private const val UNSUPPORTED_LABEL = "Unsupported"`). Magic literals like 48, 24, 20 in border/font sizes are accepted (already used in ThermalPanel). Do NOT add new percent constants if formatting via `"%.1f%%"`.

3. **demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt** — Three changes:
   - Add imports: `com.smellouk.kamper.gpu.GpuInfo`, `com.smellouk.kamper.gpu.GpuModule`, `com.smellouk.kamper.jvm.ui.GpuPanel`
   - Add `private val gpuPanel = GpuPanel()` after `private val cpuPanel = CpuPanel()` (vertical-slice ordering: GPU follows CPU per D-10)
   - Add `addTab("  GPU  ", gpuPanel)` between CPU and FPS in the `JTabbedPane` configuration (preserves UI-SPEC ordering: CPU → GPU → FPS → Memory → Network → Issues → Jank → GC → Thermal)
   - Inside `setupKamper()`:
     - Add `install(GpuModule)` after `install(CpuModule)` (any position is functionally fine; alphabetical-by-domain places it after CPU)
     - Add `addInfoListener<GpuInfo> { gpuPanel.update(it) }` after the `addInfoListener<CpuInfo>` line

Reference existing code: ThermalPanel.kt is the canonical reference. CONTEXT D-10 (GPU after CPU), D-11 (utilization + memory), D-12 (UNSUPPORTED grayed), D-13 (INVALID skip).

Gap reason: "Demos apps are not updated as expected for all platforms" — JVM demo currently has no GPU tab and never installs GpuModule.
  </action>
  <verify>
    <automated>./gradlew :demos:jvm:compileKotlin --quiet && grep -q 'install(GpuModule)' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt && grep -q 'addInfoListener<GpuInfo>' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt && grep -q 'addTab("  GPU  "' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt && grep -q 'implementation(project(":libs:modules:gpu"))' demos/jvm/build.gradle.kts && test -f demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt && ./gradlew detekt --quiet</automated>
  </verify>
  <done>
- `./gradlew :demos:jvm:compileKotlin` passes
- `GpuPanel.kt` exists with class GpuPanel extending JPanel
- `Main.kt` imports GpuModule + GpuInfo + GpuPanel, installs GpuModule, registers a GpuInfo listener, adds GPU tab between CPU and FPS
- `build.gradle.kts` has the `:libs:modules:gpu` dependency
- `./gradlew detekt` passes (zero issues — CLAUDE.md maxIssues: 0)
- The OSHI partial-data path (jvmActual returns utilization=-1.0, totalMemoryMb=vramMb) renders "Memory:  — / N MB" — utilization shows "Unsupported" because -1.0 hits the INVALID branch (per 23-03-SUMMARY semantics: utilization=-1.0 is INVALID's discriminant value; on partial-data path the FULL GpuInfo equality with INVALID will be false because totalMemoryMb≠-1.0, so the else branch fires — confirm in update()): when ALL three fields are -1.0 → equals INVALID → skip; when only memory is -1.0 but utilization is also -1.0 (OSHI case) → does NOT equal INVALID instance because the partial GpuInfo is `(-1.0, -1.0, vramMb)` ≠ `(-1.0, -1.0, -1.0)` → falls through to "valid" branch → must format utilization. **Action:** in the "valid" branch, also handle `info.utilization < 0.0` by showing "—%" instead of a negative percentage. Add this check in the formatter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GpuModule into Web (jsMain) demo (build.gradle.kts + App.kt + GpuSection.kt)</name>
  <files>demos/web/build.gradle.kts, demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt, demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt</files>
  <action>
1. **demos/web/build.gradle.kts** — Add `implementation(project(":libs:modules:gpu"))` to the `jsMain` source set's dependencies block. Place it after `:libs:modules:gc` (alphabetical) or matching the order used in JVM demo.

2. **demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt** — Create new file. Mirror ThermalSection.kt structurally:
   - `internal object GpuSection`
   - Three lateinit private vars: `utilSpan: HTMLElement`, `memorySpan: HTMLElement`, `unitSpan: HTMLElement` (the unit/label span — "%")
   - `build(container: HTMLElement)`:
     - `container.div("card") { p("card-title") { textContent = "GPU" } ... }`
     - hero block: `div("fps-hero") { utilSpan = span("fps-number") { style.fontSize = "48px"; style.color = "#7f849c"; textContent = "—" }; p("fps-unit") { textContent = "GPU usage %" } }`
     - stat-rows block: one row showing "Memory" label + value span
     - **Omit the footer button entirely** (no stress simulator for GPU; ThermalSection has it but GPU doesn't need it on web — web is always UNSUPPORTED)
   - `update(info: GpuInfo)`:
     - if `info == GpuInfo.INVALID` → return
     - if `info == GpuInfo.UNSUPPORTED` → utilSpan.textContent = "Unsupported"; utilSpan.style.color = "#7f849c" (Catppuccin overlay1); memorySpan.textContent = "N/A"; return
     - else (defensive — web jsMain always returns UNSUPPORTED per D-08, but coding defensively): utilSpan.textContent = "${info.utilization.asDynamic().toFixed(1)}%"; utilSpan.style.color = "#cba6f7" (mauve); memorySpan.textContent = if usedMemoryMb >= 0 && totalMemoryMb >= 0 then "${usedMemoryMb.toFixed(0)} MB / ${totalMemoryMb.toFixed(0)} MB" else "N/A"
   - Use the same hex palette as ThermalSection.kt (no new constants)

3. **demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt** — Five changes:
   - Add imports: `com.smellouk.kamper.gpu.GpuInfo`, `com.smellouk.kamper.gpu.GpuModule`
   - Update `tabNames` list in `buildTabs(...)`: insert `"GPU"` between `"CPU"` and `"FPS"` → new list: `listOf("CPU", "GPU", "FPS", "Memory", "Network", "Issues", "Jank", "GC", "Thermal")`
   - Update `buildSections(panels)`: insert `GpuSection.build(panels[1])` and shift all subsequent indices by +1: FpsSection.build(panels[2]), MemorySection.build(panels[3]), NetworkSection.build(panels[4]), IssuesSection.build(panels[5]), JankSection.build(panels[6]), GcSection.build(panels[7]), ThermalSection.build(panels[8])
   - Inside `setupKamper()`:
     - Add `Kamper.install(GpuModule)` after `Kamper.install(CpuModule)`
     - Add `Kamper.addInfoListener<GpuInfo> { GpuSection.update(it) }` after the `<CpuInfo>` listener

Reference existing code: ThermalSection.kt is canonical. CSS classes (`card`, `card-title`, `fps-hero`, `fps-number`, `fps-unit`, `stat-row`, `stat-label`, `stat-value`) are already defined in the demo's CSS — no new styles required.

Gap reason: "Demos apps are not updated as expected for all platforms" — Web demo currently has no GPU tab and never installs GpuModule. Per D-08, web is always UNSUPPORTED, so the section will display "Unsupported" — that is correct behavior, not a bug.
  </action>
  <verify>
    <automated>./gradlew :demos:web:compileKotlinJs --quiet && grep -q 'Kamper.install(GpuModule)' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt && grep -q 'Kamper.addInfoListener<GpuInfo>' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt && grep -q '"CPU", "GPU", "FPS"' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt && grep -q 'implementation(project(":libs:modules:gpu"))' demos/web/build.gradle.kts && test -f demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt && ./gradlew detekt --quiet</automated>
  </verify>
  <done>
- `./gradlew :demos:web:compileKotlinJs` passes
- `GpuSection.kt` exists with `internal object GpuSection` + `build(container)` + `update(info: GpuInfo)`
- `App.kt` tabNames list contains "GPU" between "CPU" and "FPS"; buildSections wires panels[1] → GpuSection
- `App.kt` setupKamper installs GpuModule and registers a GpuInfo listener
- `build.gradle.kts` has the `:libs:modules:gpu` jsMain dependency
- `./gradlew detekt` passes (zero issues)
- When run via `./gradlew :demos:web:jsBrowserDevelopmentRun`, the GPU tab is the second tab and shows "Unsupported" + "Memory: N/A" (correct per D-08)
  </done>
</task>

</tasks>

<verification>
**Per-task verify automation runs in <task>.verify above.**

**Plan-level cross-checks:**

```bash
# Both demos compile
./gradlew :demos:jvm:compileKotlin :demos:web:compileKotlinJs --quiet

# Detekt clean (CLAUDE.md hard rule)
./gradlew detekt --quiet

# All 4 GPU integration points present
grep -c 'install(GpuModule)' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt   # >= 1
grep -c 'install(GpuModule)' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt # >= 1
grep -c 'addInfoListener<GpuInfo>' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt   # >= 1
grep -c 'addInfoListener<GpuInfo>' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/App.kt # >= 1

# UI files exist
test -f demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt
test -f demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt

# GpuPanel size sanity (≥ 80 lines)
test "$(grep -v '^[[:space:]]*$' demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/ui/GpuPanel.kt | wc -l)" -ge 80

# GpuSection size sanity (≥ 50 non-blank lines)
test "$(grep -v '^[[:space:]]*$' demos/web/src/jsMain/kotlin/com/smellouk/kamper/web/ui/GpuSection.kt | wc -l)" -ge 50
```
</verification>

<success_criteria>
1. JVM demo compiles, detekt clean, GpuPanel renders in JTabbedPane between CPU and FPS, listener wired
2. Web demo compiles to JS, detekt clean, GpuSection renders in nav between CPU and FPS, listener wired
3. JVM demo on a host where OSHI finds a GPU shows "Memory: — / N MB" (utilization unknown per OSHI limitation, total VRAM real); on a host without OSHI GPU access shows "Unsupported"
4. Web demo always shows "Unsupported" + "Memory: N/A" — correct per D-08 (no public browser GPU API)
5. ROADMAP plan list updated with this plan as `23-07-PLAN.md`
</success_criteria>

<output>
After completion, create `.planning/phases/23-implement-gpu-module-for-all-platforms/23-07-SUMMARY.md` capturing:
- The 6 modified files
- OSHI host behavior observed during `./gradlew :demos:jvm:run`
- Web "Unsupported" rendering confirmed
- Any detekt fixes auto-applied
- Commits created (one per task is fine, or one combined commit)
</output>

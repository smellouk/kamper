---
phase: 23
plan: 12
type: execute
wave: 2
depends_on: [23-04]
files_modified:
  - demos/macos/build.gradle.kts
  - demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt
  - demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt
autonomous: true
gap_closure: true
requirements: [D-01, D-04, D-07, D-09, D-10, D-12, D-13]
must_haves:
  truths:
    - "macOS demo (`./gradlew :demos:macos:compileKotlinMacosArm64`) compiles with GpuModule wired and a new GpuView in the tab list between CPU and FPS"
    - "GpuView renders utilization % when MacosGpuInfoSource succeeds (IOAccelerator service available), grayed \"Unsupported\" when sandboxed, and \"—\" on transient INVALID"
    - "Tab order in segmented control + NSTabView is: CPU → GPU → FPS → Memory → Network → Issues → Jank → GC → Thermal (D-10)"
  artifacts:
    - path: "demos/macos/build.gradle.kts"
      provides: "Adds :libs:modules:gpu to commonMain dependencies"
      contains: 'implementation(project(":libs:modules:gpu"))'
    - path: "demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt"
      provides: "Wires GpuView into KamperDemoWindow tabs + installs GpuModule + listener"
      contains: "install(GpuModule)"
    - path: "demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt"
      provides: "AppKit NSView mirroring ThermalView.kt — utilization label, memory label, UNSUPPORTED state"
      min_lines: 80
  key_links:
    - from: "demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt"
      to: "GpuModule"
      via: "Kamper.install(GpuModule) inside setupKamper()"
      pattern: "install\\(GpuModule\\)"
    - from: "demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt"
      to: "GpuView.update"
      via: "addInfoListener<GpuInfo> { gpuView.update(it) }"
      pattern: "addInfoListener<GpuInfo>"
---

<objective>
Wire `GpuModule` into the macOS AppKit demo. Add a `GpuView` (NSView) between CPU and FPS in the tab list, update the segmented control labels, install `GpuModule` in `setupKamper()`, and register a `GpuInfo` listener.

macOS is the ONE Apple platform where the GPU module returns real data: per 23-04-SUMMARY, `MacosGpuInfoSource` calls `kamper_gpu_utilization()` from the IOKit cinterop binding, returning utilization 0..100 on AGX/Intel/AMD GPUs (memory fields stay at -1.0 because IOAccelerator PerformanceStatistics doesn't expose VRAM totals via this path). When the IOAccelerator service is unavailable (sandboxed app, no GPU), the source returns UNSUPPORTED.

Purpose: Close UAT gap "All demo apps install GpuModule and show a GPU screen / tile / section mirroring the ThermalModule demo pattern" for the macOS AppKit demo (`demos/macos`).

Output: 3 files modified/created. Demo compiles via `./gradlew :demos:macos:compileKotlinMacosArm64` and `:compileKotlinMacosX64`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-CONTEXT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-UAT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-04-SUMMARY.md

# Reference files — DO NOT modify, read for pattern only
@demos/macos/build.gradle.kts
@demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt
@demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/ThermalView.kt

# GPU module API surface
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuInfo.kt
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuConfig.kt

<interfaces>
GpuInfo (commonMain): `data class GpuInfo(utilization: Double, usedMemoryMb: Double, totalMemoryMb: Double)`
- INVALID = GpuInfo(-1.0, -1.0, -1.0)
- UNSUPPORTED = GpuInfo(-2.0, -2.0, -2.0)

macosMain `actual val GpuModule` (per 23-04-SUMMARY):
- IOAccelerator service available → `GpuInfo(utilization=X, usedMemoryMb=-1.0, totalMemoryMb=-1.0)` where X is 0..100
- IOAccelerator missing → `GpuInfo.UNSUPPORTED`
- exception → `GpuInfo.INVALID`

Theme palette (existing macos Theme.kt): MAUVE (Catppuccin) for valid GPU primary color (D-10),
MUTED for UNSUPPORTED label (D-12). nsColor() helper already provided.
</interfaces>

<macos_pattern_reference>
ThermalView.kt is canonical for GpuView.kt. Key elements:
- File header: `@file:OptIn(kotlinx.cinterop.ExperimentalForeignApi::class, kotlinx.cinterop.BetaInteropApi::class)`
- `class GpuView : NSView`
- Lateinit / non-null lateinit fields:
  - `bigLabel: NSTextField` — primary metric, monospaced 48pt bold (NSFont.monospacedSystemFontOfSize(48.0, NSFontWeightBold))
  - `unitLabel: NSTextField` — "GPU usage %", LABEL_FONT, MUTED textColor, alignment Center
  - `memoryLabel: NSTextField` — "Memory:  —", LABEL_FONT, TEXT textColor
  - `vramLabel: NSTextField` — "VRAM total:  —" (optional split row mirroring ThermalView's temperatureLabel)
- @OverrideInit constructor(frame: CValue<CGRect>) : super(frame) — same pattern as ThermalView
- **OMIT** the stress button + ActionTarget + stressJobs list + stressTarget (no GPU stress simulator) — replace with a small hint NSTextField "IOKit IOAccelerator probe (D-07)" or omit entirely
- NSLayoutConstraint stack mirroring ThermalView (top 24.0, leading/trailing pad 20.0, vertical spacing as in ThermalView)
- `fun update(info: GpuInfo)`:
  - if `info == GpuInfo.INVALID` → return (defensive guard, matches ThermalView pattern)
  - if `info == GpuInfo.UNSUPPORTED` → bigLabel.stringValue="Unsupported"; bigLabel.textColor=Theme.MUTED; memoryLabel.stringValue="Memory:  N/A"; vramLabel.stringValue="VRAM total:  N/A"
  - else: bigLabel.stringValue="${"%.1f".format(info.utilization)}%"; bigLabel.textColor=Theme.MAUVE; memoryLabel.stringValue formatted per D-02 partial-data; vramLabel.stringValue similarly
  - Defensive utilization < 0 → "—%" with MUTED color
- `override fun drawRect(dirtyRect)` — fills view with Theme.BASE (mirror ThermalView final block)
</macos_pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GpuView.kt (AppKit NSView)</name>
  <files>demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt</files>
  <action>
Create `demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt` mirroring `ThermalView.kt` exactly:

- File header: `@file:OptIn(kotlinx.cinterop.ExperimentalForeignApi::class, kotlinx.cinterop.BetaInteropApi::class)`
- Package: `com.smellouk.kamper.macos.ui`
- Imports: `com.smellouk.kamper.gpu.GpuInfo`, kotlinx.cinterop.*, kotlinx.coroutines.* (only if needed — NOT needed since we have no stress button), platform.AppKit.*, platform.CoreGraphics.CGRect, platform.Foundation.*
- `class GpuView : NSView { ... }` with `@OverrideInit constructor(frame: CValue<CGRect>) : super(frame) { ... }`
- Three NSTextField fields:
  - `bigLabel = NSTextField.labelWithString("—").apply { font = NSFont.monospacedSystemFontOfSize(48.0, NSFontWeightBold); textColor = Theme.MUTED; alignment = NSTextAlignmentCenter; translatesAutoresizingMaskIntoConstraints = false }`
  - `unitLabel = NSTextField.labelWithString("GPU usage %").apply { font = Theme.LABEL_FONT; textColor = Theme.MUTED; alignment = NSTextAlignmentCenter; translatesAutoresizingMaskIntoConstraints = false }`
  - `memoryLabel = NSTextField.labelWithString("Memory:  —").apply { font = Theme.LABEL_FONT; textColor = Theme.TEXT; translatesAutoresizingMaskIntoConstraints = false }`
- Constructor body (inside `@OverrideInit constructor`):
  - `translatesAutoresizingMaskIntoConstraints = false`
  - `wantsLayer = true`
  - Create separator `sep = NSBox(...).apply { boxType = NSBoxSeparator; ... }`
  - addSubview for bigLabel, unitLabel, memoryLabel, sep — but **do NOT add a stress button**
  - Build NSLayoutConstraint list mirroring ThermalView with the same anchors (top 24.0 from topAnchor, leading/trailing pad 20.0, unitLabel below bigLabel +4.0, memoryLabel below unitLabel +24.0, sep separator near bottom). Omit the stressButton constraints entirely. Add a final constraint pinning sep.bottomAnchor to bottomAnchor with -10.0 OR omit sep entirely if there's no footer button.
- `fun update(info: GpuInfo)`:
  ```kotlin
  if (info == GpuInfo.INVALID) return
  if (info == GpuInfo.UNSUPPORTED) {
      bigLabel.stringValue = "Unsupported"
      bigLabel.textColor = Theme.MUTED
      memoryLabel.stringValue = "Memory:  N/A"
      return
  }
  if (info.utilization < 0.0) {
      bigLabel.stringValue = "—%"
      bigLabel.textColor = Theme.MUTED
  } else {
      bigLabel.stringValue = "${"%.1f".format(info.utilization)}%"
      bigLabel.textColor = Theme.MAUVE
  }
  memoryLabel.stringValue = when {
      info.usedMemoryMb >= 0.0 && info.totalMemoryMb >= 0.0 ->
          "Memory:  ${info.usedMemoryMb.toInt()} MB / ${info.totalMemoryMb.toInt()} MB"
      info.totalMemoryMb >= 0.0 -> "Memory:  — / ${info.totalMemoryMb.toInt()} MB"
      else -> "Memory:  N/A"
  }
  ```
- `override fun drawRect(dirtyRect: CValue<CGRect>) { super.drawRect(dirtyRect); Theme.BASE.setFill(); NSBezierPath.bezierPathWithRect(bounds).fill() }`

**Detekt:** Mirror ThermalView's literal handling exactly. ThermalView uses `48.0`, `24.0`, `20.0`, `4.0` inline without companion constants and detekt accepted it; do the same. If detekt now flags any literal in GpuView, extract to a `private companion object` block.

Reference existing code: ThermalView.kt (132 lines).

Gap reason: macOS demo currently has no GpuView class.
  </action>
  <verify>
    <automated>./gradlew :demos:macos:compileKotlinMacosArm64 --quiet && test -f demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt && grep -q 'class GpuView' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt && grep -q 'fun update(info: GpuInfo)' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt</automated>
  </verify>
  <done>
- File exists, defines `class GpuView : NSView` with @OverrideInit constructor and `fun update(info: GpuInfo)`
- compileKotlinMacosArm64 passes
- update() handles INVALID (return), UNSUPPORTED (gray + N/A), valid (mauve + formatted memory), defensive utilization < 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GpuView into KamperDemoWindow + install GpuModule + add :libs:modules:gpu dep</name>
  <files>demos/macos/build.gradle.kts, demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt</files>
  <action>
1. **demos/macos/build.gradle.kts** — Add `implementation(project(":libs:modules:gpu"))` to the `commonMain.dependencies { ... }` block, after `:libs:modules:gc` and before `:libs:modules:thermal` (alphabetical order to match the existing module list).

2. **demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt** — Six changes inside `KamperDemoWindow`:
   - Add imports: `com.smellouk.kamper.gpu.GpuInfo`, `com.smellouk.kamper.gpu.GpuModule`, `com.smellouk.kamper.macos.ui.GpuView`
   - Add `private val gpuView = GpuView(NSMakeRect(0.0, 0.0, 0.0, 0.0))` after `private val cpuView = CpuView(...)`
   - Inside `buildTabView()`:
     - Add `addTab("GPU", gpuView)` between `addTab("CPU", cpuView)` and `addTab("FPS", fpsView)` (preserves D-10 ordering)
     - Update segmented control: change `seg.segmentCount = 8` to `seg.segmentCount = 9`
     - Add `seg.setLabel("GPU", forSegment = 1)` and renumber subsequent labels by +1: FPS at 2, Memory at 3, Network at 4, Issues at 5, Jank at 6, GC at 7, Thermal at 8
   - Inside `setupKamper()`:
     - Add `install(GpuModule)` after `install(CpuModule)`
     - Add `addInfoListener<GpuInfo> { gpuView.update(it) }` after the `<CpuInfo>` listener

Reference existing code: `KamperDemoWindow` lines 65-188 in Main.kt. The pattern is highly mechanical — add one view, one tab, two seg label updates (count + new GPU label), shift other seg labels, and add two lines in setupKamper.

Gap reason: macOS demo currently has no GPU view, no GpuModule install, no listener.

**Detekt:** No new magic numbers; the existing `seg.segmentCount = 8` becomes `9` (a literal). If detekt flags it, extract `private const val TAB_COUNT = 9` at top of file. The existing code has `8` inline, so likely detekt is already tolerant.
  </action>
  <verify>
    <automated>./gradlew :demos:macos:compileKotlinMacosArm64 :demos:macos:compileKotlinMacosX64 --quiet && grep -q 'install(GpuModule)' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt && grep -q 'addInfoListener<GpuInfo>' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt && grep -q 'addTab("GPU", gpuView)' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt && grep -q 'seg.setLabel("GPU"' demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt && grep -q 'implementation(project(":libs:modules:gpu"))' demos/macos/build.gradle.kts && ./gradlew detekt --quiet</automated>
  </verify>
  <done>
- Both compile tasks pass: macosArm64 + macosX64
- KamperDemoWindow declares gpuView, places it between cpuView and fpsView, updates segmentCount to 9, sets GPU label at segment 1
- setupKamper installs GpuModule and registers a GpuInfo listener
- build.gradle.kts has the gpu dependency
- `./gradlew detekt` passes (zero issues)
  </done>
</task>

</tasks>

<verification>
**Per-task verify automation runs in <task>.verify above.**

**Plan-level cross-checks:**

```bash
# Both macOS targets compile
./gradlew :demos:macos:compileKotlinMacosArm64 :demos:macos:compileKotlinMacosX64 --quiet

# Detekt clean
./gradlew detekt --quiet

# All wiring points present
grep -c 'install(GpuModule)'         demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt   # 1
grep -c 'addInfoListener<GpuInfo>'   demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt   # 1
grep -c 'addTab("GPU"'               demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt   # 1
grep -c 'seg.setLabel("GPU"'         demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/Main.kt   # 1

# UI file exists
test -f demos/macos/src/macosMain/kotlin/com/smellouk/kamper/macos/ui/GpuView.kt
```
</verification>

<success_criteria>
1. `./gradlew :demos:macos:compileKotlinMacosArm64` and `:compileKotlinMacosX64` both pass
2. `./gradlew detekt` reports zero issues
3. Main.kt: gpuView is between cpuView and fpsView in NSTabView, seg has 9 segments with GPU label at index 1, setupKamper installs GpuModule and registers listener
4. GpuView.kt: NSView class with `update(info: GpuInfo)` handling all three states with mauve accent on valid utilization
5. ROADMAP plan list updated with this plan as `23-12-PLAN.md`
</success_criteria>

<output>
After completion, create `.planning/phases/23-implement-gpu-module-for-all-platforms/23-12-SUMMARY.md` capturing:
- The 3 modified/created files
- Compile status for both macOS targets
- Detekt status
- Runtime behavior observed if `./gradlew :demos:macos:runDebugExecutableMacosArm64` was executed (utilization should appear on hosts with IOAccelerator service; UNSUPPORTED otherwise)
- Commits created
</output>

---
phase: 23
plan: 09
type: execute
wave: 1
depends_on: [23-03, 23-04, 23-05]
files_modified:
  - demos/compose/build.gradle.kts
  - demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt
  - demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt
  - demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt
  - demos/compose/src/androidMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
  - demos/compose/src/iosMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
  - demos/compose/src/desktopMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
  - demos/compose/src/wasmJsMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
autonomous: true
gap_closure: true
requirements: [D-01, D-04, D-09, D-10, D-12, D-13]
must_haves:
  truths:
    - "Compose demo on each platform (android, ios, desktop, wasmJs) installs GpuModule and updates a Compose `gpuInfo` state field"
    - "App.kt routes a GPU tab between CPU and FPS in the tab strip"
    - "GpuTab.kt renders utilization %, used/total VRAM MB, and a grayed UNSUPPORTED state per D-12"
    - "KamperState.gpuInfo defaults to GpuInfo.INVALID and is updated on every non-INVALID listener emission"
  artifacts:
    - path: "demos/compose/build.gradle.kts"
      provides: "Adds :libs:modules:gpu to commonMain dependencies"
      contains: 'implementation(project(":libs:modules:gpu"))'
    - path: "demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt"
      provides: "Adds `var gpuInfo by mutableStateOf(GpuInfo.INVALID)` field"
      contains: "var gpuInfo"
    - path: "demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt"
      provides: "Adds GPU tab title between CPU and FPS, routes selectedTab branch to GpuTab"
      contains: '"GPU"'
    - path: "demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt"
      provides: "Compose tab mirroring ThermalTab.kt — utilization hero, memory rows, MAUVE accent, grayed UNSUPPORTED"
      min_lines: 70
    - path: "demos/compose/src/androidMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt"
      provides: "Installs GpuModule + GpuInfo listener on android"
      contains: "Kamper.install(GpuModule)"
    - path: "demos/compose/src/iosMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt"
      provides: "Installs GpuModule + GpuInfo listener on iOS (always UNSUPPORTED per D-07)"
      contains: "Kamper.install(GpuModule)"
    - path: "demos/compose/src/desktopMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt"
      provides: "Installs GpuModule + GpuInfo listener on JVM desktop (OSHI partial data per D-06)"
      contains: "Kamper.install(GpuModule)"
    - path: "demos/compose/src/wasmJsMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt"
      provides: "Installs GpuModule + GpuInfo listener on wasmJs (always UNSUPPORTED per D-08)"
      contains: "Kamper.install(GpuModule)"
  key_links:
    - from: "demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt"
      to: "GpuTab"
      via: "selectedTab == 1 -> GpuTab(info = state.gpuInfo)"
      pattern: "GpuTab\\("
    - from: "All four KamperSetup.kt actuals"
      to: "Kamper engine"
      via: "Kamper.install(GpuModule) + Kamper.addInfoListener<GpuInfo>"
      pattern: "addInfoListener<GpuInfo>"
---

<objective>
Wire `GpuModule` into the Compose Multiplatform demo across all four platform actuals (android, ios, desktopJvm, wasmJs). Add a `gpuInfo` field to the shared `KamperState`, create a new `GpuTab.kt` Compose component mirroring `ThermalTab.kt`, route the new GPU tab between CPU and FPS in `App.kt`, and update each `KamperSetup.kt` actual to install `GpuModule` + register a `GpuInfo` listener.

The Compose demo already uses `libs/ui/kmm` (which had its KamperPanel GPU tile wired in 23-06). This plan addresses the demo's OWN KamperState/App/Tab presentation — the in-app tabs the user sees, separate from the floating KamperPanel overlay.

Purpose: Close UAT gap "All demo apps install GpuModule and show a GPU screen / tile / section mirroring the ThermalModule demo pattern" for the Compose Multiplatform demo (`demos/compose`) on all 4 target platforms.

Output: 8 files modified/created. Compose demo compiles for android, desktop, iosArm64, iosSimulatorArm64, and wasmJs targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-CONTEXT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-UAT.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-03-SUMMARY.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-04-SUMMARY.md
@.planning/phases/23-implement-gpu-module-for-all-platforms/23-05-SUMMARY.md

# Reference files — DO NOT modify, read for pattern only
@demos/compose/build.gradle.kts
@demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt
@demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt
@demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/ThermalTab.kt
@demos/compose/src/androidMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
@demos/compose/src/iosMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt
@demos/compose/src/desktopMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt

# GPU module API surface
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuInfo.kt
@libs/modules/gpu/src/commonMain/kotlin/com/smellouk/kamper/gpu/GpuConfig.kt

<interfaces>
GpuInfo (commonMain): `data class GpuInfo(utilization: Double, usedMemoryMb: Double, totalMemoryMb: Double)`
- INVALID = GpuInfo(-1.0, -1.0, -1.0) — transient read failure
- UNSUPPORTED = GpuInfo(-2.0, -2.0, -2.0) — platform capability gap

KamperState (commonMain) currently:
```kotlin
class KamperState {
    var cpuInfo by mutableStateOf(CpuInfo.INVALID)
    var fpsInfo by mutableStateOf(FpsInfo.INVALID)
    var memoryInfo by mutableStateOf(MemoryInfo.INVALID)
    var networkInfo by mutableStateOf(NetworkInfo.INVALID)
    var isRunning by mutableStateOf(false)
    val issues = mutableStateListOf<Issue>()
    var jankInfo by mutableStateOf(JankInfo.INVALID)
    var gcInfo by mutableStateOf(GcInfo.INVALID)
    var thermalInfo by mutableStateOf(ThermalInfo.INVALID)
    fun addIssue(...)
    fun clearIssues() = issues.clear()
}
```
ADD: `var gpuInfo by mutableStateOf(GpuInfo.INVALID)` between cpuInfo and fpsInfo (D-10 ordering).

KamperColors (commonMain ui/Theme.kt) — provides mauve/green/yellow/peach/mantle/bg/text/overlay1/surface0/surface1. Use `KamperColors.mauve` for GPU primary color (D-10).
</interfaces>

<compose_pattern_reference>
ThermalTab.kt is canonical for GpuTab.kt:
- @Composable fun GpuTab(info: GpuInfo, modifier: Modifier = Modifier)
- Top hero Column (mantle background, padding 24dp): big Text showing primary metric, sub-Text showing unit
- Middle Column (weight 1f, padding 20dp horizontal): rows with HorizontalDividers between
- Footer: ThermalTab has stress button — OMIT for GpuTab (no GPU stress simulator)
- Color logic for hero text:
  - INVALID → "—" with `KamperColors.overlay1`
  - UNSUPPORTED → "Unsupported" with `KamperColors.overlay1` (D-12 grayed-out)
  - else → "%.1f%%".format(info.utilization) with `KamperColors.mauve`
  - Defensive: if `info.utilization < 0.0` (partial-data path on Android Mali fallback or JVM OSHI), show "—%" instead of negative
- Memory rows:
  - usedMemoryMb >= 0 → "${usedMb} MB", else "—" or "N/A" if UNSUPPORTED
  - totalMemoryMb >= 0 → "${totalMb} MB", else "—" or "N/A" if UNSUPPORTED
</compose_pattern_reference>

<important>
The Compose demo has 4 platform actuals: androidMain, iosMain, desktopMain, wasmJsMain. Confirmed via `ls demos/compose/src/`. The wasmJsMain KamperSetup.kt is already present and follows the same shape as desktopMain (no platform-specific helpers).
</important>
</context>

<tasks>

<task type="auto">
  <name>Task 1: commonMain layer — KamperState field, GpuTab.kt, App.kt routing, build.gradle.kts dependency</name>
  <files>demos/compose/build.gradle.kts, demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt, demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt, demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt</files>
  <action>
1. **demos/compose/build.gradle.kts** — Add `implementation(project(":libs:modules:gpu"))` to the `commonMain.dependencies { ... }` block, after `:libs:modules:gc` and before `:libs:modules:thermal` (or wherever maintains alphabetical order). The dependency lives on commonMain because GPU expect/actual is published for all 4 of compose's targets.

2. **demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt** — Add one new field. Insert AFTER `cpuInfo` and BEFORE `fpsInfo` to mirror D-10 ordering used by libs/ui/kmm KamperUiState (CPU → GPU → FPS):
   ```kotlin
   import com.smellouk.kamper.gpu.GpuInfo

   class KamperState {
       var cpuInfo by mutableStateOf(CpuInfo.INVALID)
       var gpuInfo by mutableStateOf(GpuInfo.INVALID)   // NEW (D-10)
       var fpsInfo by mutableStateOf(FpsInfo.INVALID)
       // ... existing fields unchanged
   }
   ```

3. **demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt** — Create new file mirroring `ThermalTab.kt` structure exactly:
   - Package: `com.smellouk.kamper.compose.ui.tabs`
   - Imports: `com.smellouk.kamper.gpu.GpuInfo`, `com.smellouk.kamper.compose.ui.KamperColors`, all the Compose imports ThermalTab uses (Box, Column, Row, Spacer, Text, padding, etc.)
   - `@Composable fun GpuTab(info: GpuInfo, modifier: Modifier = Modifier)`
   - **OMIT** the stress button + scope/coroutines (no GPU stress simulator)
   - Top hero Column (background = `KamperColors.mantle`, vertical padding 24dp, horizontal alignment Center):
     - Compute primary text + color via local logic:
       - `info == GpuInfo.UNSUPPORTED` → text="Unsupported", color=`KamperColors.overlay1` (D-12)
       - `info == GpuInfo.INVALID || info.utilization < 0.0` → text="—", color=`KamperColors.overlay1`
       - else → text="%.1f%%".format(info.utilization), color=`KamperColors.mauve` (D-10)
     - Big Text: fontSize 48sp, fontWeight Bold, fontFamily FontFamily.Monospace, alignment Center
     - Subtext "GPU usage %": fontSize 16sp, color `KamperColors.overlay1`, alignment Center
   - Middle Column (weight 1f, fillMaxWidth, padding horizontal=20dp, vertical=16dp):
     - Row "Memory" / value via `memoryRowText(info)` helper
     - HorizontalDivider color = `KamperColors.surface0`, thickness 1.dp
     - Row "VRAM total" / value via `totalRowText(info)` helper
     - HorizontalDivider
   - Helper functions (private, non-composable):
     - `private fun memoryRowText(info: GpuInfo): String` — UNSUPPORTED → "N/A"; usedMemoryMb < 0 → "—"; else "%.0f MB".format(info.usedMemoryMb)
     - `private fun totalRowText(info: GpuInfo): String` — UNSUPPORTED → "N/A"; totalMemoryMb < 0 → "—"; else "%.0f MB".format(info.totalMemoryMb)
     - `@Composable private fun GpuStatRow(label: String, value: String)` — copy ThermalStatRow exactly (Row with overlay1 label + monospace bold text value)
   - Footer: omit entirely OR an empty Box matching ThermalTab's footer dimensions (mantle background, 12dp vertical padding) so the visual rhythm holds. Either is acceptable.
   - **Detekt:** Magic numbers like 48, 24, 20, 16, 1 wrapped in dp/sp builders are tolerated (ThermalTab uses them inline). Mirror ThermalTab exactly. If detekt flags any literal, extract to private constants.

4. **demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt** — Three changes:
   - Add import: `com.smellouk.kamper.compose.ui.tabs.GpuTab`
   - Update `tabs` list inside `KamperContent`: insert `"GPU"` between `"CPU"` and `"FPS"` → `listOf("CPU", "GPU", "FPS", "Memory", "Network", "Issues", "Jank", "GC", "Thermal")`
   - Update the `when (selectedTab)` block: insert `1 -> GpuTab(info = state.gpuInfo)` and shift all later branches by +1 (FpsTab→2, MemoryTab→3, NetworkTab→4, IssuesTab→5, JankTab→6, GcTab→7, ThermalTab→8)

Reference existing code: ThermalTab.kt is the canonical Compose tab. KamperState.kt and App.kt show exact insertion points.

Gap reason: Compose demo has no GPU tab in its commonMain UI.
  </action>
  <verify>
    <automated>./gradlew :demos:compose:compileKotlinDesktop --quiet && grep -q 'var gpuInfo' demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt && grep -q '"CPU", "GPU", "FPS"' demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt && grep -q 'GpuTab(info = state.gpuInfo)' demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt && test -f demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt && grep -q 'implementation(project(":libs:modules:gpu"))' demos/compose/build.gradle.kts</automated>
  </verify>
  <done>
- `./gradlew :demos:compose:compileKotlinDesktop` passes (validates commonMain compiles for at least one target)
- KamperState declares `gpuInfo` field initialized to `GpuInfo.INVALID`
- App.kt routes selectedTab==1 to GpuTab and the tabs list contains "GPU"
- GpuTab.kt exists with @Composable fun GpuTab(info: GpuInfo, ...)
- build.gradle.kts has the gpu dependency on commonMain
  </done>
</task>

<task type="auto">
  <name>Task 2: Platform actuals — install GpuModule and register listener in 4 KamperSetup.kt files</name>
  <files>demos/compose/src/androidMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt, demos/compose/src/iosMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt, demos/compose/src/desktopMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt, demos/compose/src/wasmJsMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt</files>
  <action>
For each of the four KamperSetup.kt files, make the SAME pair of changes:

1. Add imports: `com.smellouk.kamper.gpu.GpuInfo`, `com.smellouk.kamper.gpu.GpuModule`
2. Inside `actual fun KamperState.initialize(scope: CoroutineScope)`:
   - Add `Kamper.install(GpuModule)` after `Kamper.install(CpuModule)` (mirror the existing CPU install line)
   - Add `Kamper.addInfoListener<GpuInfo> { info -> if (info != GpuInfo.INVALID) scope.launch { gpuInfo = info } }` after the `<CpuInfo>` listener (mirror the same INVALID guard pattern)

**androidMain/KamperSetup.kt:** Standard pattern. Android `actual val GpuModule` is parameterless per 23-03-SUMMARY (mirrors CpuModule).

**iosMain/KamperSetup.kt:** Standard pattern. Per 23-04-SUMMARY iOS GpuModule returns `GpuInfo.UNSUPPORTED` unconditionally — listener receives UNSUPPORTED, propagates to state.gpuInfo, GpuTab renders grayed "Unsupported" tile per D-12. Correct.

**desktopMain/KamperSetup.kt:** Standard pattern. Per 23-03-SUMMARY desktop (JVM) uses OSHI — listener may receive `GpuInfo(utilization=-1.0, usedMemoryMb=-1.0, totalMemoryMb=vramMb)` (partial data) which is NOT equal to `GpuInfo.INVALID` (because totalMemoryMb≠-1.0), so it propagates. GpuTab handles via `info.utilization < 0.0` defensive branch. On hosts where OSHI finds no GPU, listener receives UNSUPPORTED and tile grays.

**wasmJsMain/KamperSetup.kt:** Standard pattern. Per 23-05-SUMMARY wasmJs returns UNSUPPORTED unconditionally — UI shows grayed "Unsupported".

Reference existing code: each file already has the exact pattern for CpuModule, FpsModule, etc. Just clone the CPU lines for GPU and adjust imports. The four files are nearly identical except androidMain has `appContext` and ctx-aware module installs (MemoryModule(ctx), IssuesModule(ctx, ...), JankModule(app), ThermalModule(ctx)) — `GpuModule` is parameterless on Android per 23-03-SUMMARY so no context needed.

Gap reason: Compose demo platform actuals do not install GpuModule.

**Detekt:** No new magic numbers introduced. The `info != GpuInfo.INVALID` comparison reuses the data class equals() — no special handling needed.
  </action>
  <verify>
    <automated>./gradlew :demos:compose:compileDebugKotlinAndroid :demos:compose:compileKotlinDesktop :demos:compose:compileKotlinIosArm64 :demos:compose:compileKotlinWasmJs --quiet && for f in demos/compose/src/androidMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt demos/compose/src/iosMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt demos/compose/src/desktopMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt demos/compose/src/wasmJsMain/kotlin/com/smellouk/kamper/compose/KamperSetup.kt; do grep -q 'Kamper.install(GpuModule)' "$f" && grep -q 'addInfoListener<GpuInfo>' "$f" || { echo "MISSING wiring in $f"; exit 1; }; done && ./gradlew detekt --quiet</automated>
  </verify>
  <done>
- All 4 platform compile tasks pass (compileDebugKotlinAndroid, compileKotlinDesktop, compileKotlinIosArm64, compileKotlinWasmJs)
- Each of the 4 KamperSetup.kt files contains both `Kamper.install(GpuModule)` and `addInfoListener<GpuInfo>`
- `./gradlew detekt` passes (zero issues)
- Compose demo runtime: gpuInfo state field updates via the listener; GpuTab shows correct UNSUPPORTED/valid state per platform
  </done>
</task>

</tasks>

<verification>
**Per-task verify automation runs in <task>.verify above.**

**Plan-level cross-checks:**

```bash
# All 4 Compose targets compile
./gradlew :demos:compose:compileDebugKotlinAndroid \
          :demos:compose:compileKotlinDesktop \
          :demos:compose:compileKotlinIosArm64 \
          :demos:compose:compileKotlinWasmJs --quiet

# Detekt clean
./gradlew detekt --quiet

# All wiring points present
grep -c 'var gpuInfo'                  demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/KamperState.kt   # 1
grep -c 'GpuTab(info = state.gpuInfo)' demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/App.kt           # 1
test -f demos/compose/src/commonMain/kotlin/com/smellouk/kamper/compose/ui/tabs/GpuTab.kt
for f in androidMain iosMain desktopMain wasmJsMain; do
  grep -c 'Kamper.install(GpuModule)' demos/compose/src/$f/kotlin/com/smellouk/kamper/compose/KamperSetup.kt   # 1 each
done
```
</verification>

<success_criteria>
1. All 4 Compose targets compile (android, desktop, iosArm64, wasmJs)
2. `./gradlew detekt` reports zero issues
3. KamperState declares `gpuInfo`, App.kt routes the GPU tab between CPU and FPS, GpuTab.kt renders all three states (INVALID skipped → "—", UNSUPPORTED → grayed "Unsupported", valid → mauve % + memory rows)
4. All 4 KamperSetup.kt actuals install GpuModule and register the listener with the INVALID guard
5. ROADMAP plan list updated with this plan as `23-09-PLAN.md`
</success_criteria>

<output>
After completion, create `.planning/phases/23-implement-gpu-module-for-all-platforms/23-09-SUMMARY.md` capturing:
- The 8 modified/created files
- Compile status for all 4 targets
- Detekt status
- Per-platform UNSUPPORTED behavior observed at runtime (if a target was launched)
- Commits created
</output>

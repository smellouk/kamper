---
phase: 17-medium-article-series-android-performance-bottlenecks-kamper
plan: 03
type: execute
wave: 2
depends_on:
  - 17-02
files_modified:
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt
  - kamper-demo/app/src/main/res/layout/screen_cpu.xml
  - kamper-demo/app/src/main/res/layout/screen_fps.xml
  - kamper-demo/app/src/main/res/layout/screen_memory.xml
  - kamper-demo/app/src/main/res/layout/screen_jank.xml
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "CpuScreen exists at kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt as a real Fragment with toggleable CPU stress and 5-bar metric rendering (totalUseRatio, appRatio, userRatio, systemRatio, ioWaitRatio)"
    - "FpsScreen exists with FPS label rendering"
    - "MemoryScreen exists with heap/RAM/PSS rendering and a 'Alloc 32 MB' button that holds a strong reference list"
    - "JankScreen exists with droppedFrames/jankyFrameRatio/worstFrameMs rendering and a 200 ms main-thread busy-wait trigger"
    - "MetricRowView custom View provides label/value/fraction/barColor properties used by CpuScreen and MemoryScreen"
    - "NoopScreens.kt has the matching 4 stub class declarations DELETED (CpuScreen, FpsScreen, MemoryScreen, JankScreen)"
    - "Each screen's update(info) method dispatches to UI thread and respects INVALID guards"
    - "Each screen nulls view references in onDestroyView (no leaked views)"
    - "kamper-demo project still parses (./gradlew help -q OR ./gradlew tasks --all -m succeeds)"
    - "assembleDebug task is still registered"
  artifacts:
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt"
      provides: "Real CpuScreen Fragment — 5 metric rows + toggleable CPU stress"
      contains: "fun update(info: CpuInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt"
      provides: "Real FpsScreen Fragment — fps label"
      contains: "fun update(info: FpsInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt"
      provides: "Real MemoryScreen Fragment — heap/RAM/PSS + Alloc/Release buttons"
      contains: "fun update(info: MemoryInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt"
      provides: "Real JankScreen Fragment — dropped/janky/worst-frame + Simulate Jank button"
      contains: "fun update(info: JankInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt"
      provides: "Reusable horizontal bar + label + value composite View"
      contains: "var fraction"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt"
      provides: "CpuWork helper class for the 4-thread CPU bogosort stress trigger"
      contains: "class CpuWork"
  key_links:
    - from: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt"
      to: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt"
      via: "5 MetricRowView instances per layout, fraction + valueText updated from CpuInfo ratios"
      pattern: "MetricRowView"
    - from: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt"
      to: "MemoryInfo nested structs"
      via: "info.heapMemoryInfo.allocatedInMb / info.ramInfo.totalRamInMb / info.pssInfo.totalPssInMb"
      pattern: "heapMemoryInfo|ramInfo|pssInfo"
    - from: "MainActivity (Plan 02)"
      to: "Real screens (this plan)"
      via: "screens become functional once stubs in NoopScreens.kt are deleted; MainActivity continues to call cpuScreen.update(it), etc."
      pattern: "cpuScreen.update|fpsScreen.update|memoryScreen.update|jankScreen.update"
---

<objective>
Replace 4 of the 8 noop screen stubs from Plan 02 with real, runnable Fragment implementations: CpuScreen, FpsScreen, MemoryScreen, JankScreen. Add the supporting MetricRowView custom view and Utils.kt helpers.

Purpose: Per D-09, the demo app must artificially induce each bottleneck so readers can clone, run, and watch Kamper detect it in real time. This plan delivers 4 of the 8 fully-functional bottleneck screens. Real screens preserve the Plan 02 central-install wiring (the Activity-level listeners now route to screens that actually update UI).

Output: 4 real screen Kotlin files + 4 layout XMLs + MetricRowView custom view + Utils.kt + matching deletions from NoopScreens.kt. Project still compiles (model-parses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-CONTEXT.md
@.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-PATTERNS.md
@CLAUDE.md
@demos/android/src/main/java/com/smellouk/kamper/android/CpuFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/FpsFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/MemoryFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/JankFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/Utils.kt
@demos/android/src/main/java/com/smellouk/kamper/android/views/MetricRowView.kt

<interfaces>
<!-- Each screen MUST expose `fun update(info: XxxInfo)` (public, no override) so MainActivity's listener calls compile. -->

CpuScreen → `fun update(info: CpuInfo)` — info has fields totalUseRatio, appRatio, userRatio, systemRatio, ioWaitRatio (all Double, 0.0–1.0)
FpsScreen → `fun update(info: FpsInfo)` — info has field fps (Int)
MemoryScreen → `fun update(info: MemoryInfo)` — info has nested:
  • heapMemoryInfo: allocatedInMb (Float), maxMemoryInMb (Float)
  • ramInfo: totalRamInMb (Float), availableRamInMb (Float), isLowMemory (Boolean)
  • pssInfo: totalPssInMb (Float), dalvikPssInMb (Float), nativePssInMb (Float), otherPssInMb (Float)
JankScreen → `fun update(info: JankInfo)` — info has fields droppedFrames (Int), jankyFrameRatio (Float), worstFrameMs (Long)

MetricRowView (custom View extending FrameLayout or LinearLayout) — properties:
- var label: String
- var valueText: String
- var fraction: Float (0..1)
- var barColor: Int

Pattern source: demos/android/src/main/java/com/smellouk/kamper/android/views/MetricRowView.kt — reproduce in kamper-demo with package com.smellouk.kamperdemo.views

Utils.kt — `class CpuWork` from demos/android/.../Utils.kt lines 39-79 (bogosort across N threads). Used by CpuScreen toggleLoad().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CpuScreen + FpsScreen + supporting MetricRowView/Utils + layouts</name>
  <files>
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt,
    kamper-demo/app/src/main/res/layout/screen_cpu.xml,
    kamper-demo/app/src/main/res/layout/screen_fps.xml,
    kamper-demo/app/src/main/res/layout/view_metric_row.xml,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt
  </files>
  <read_first>
    - demos/android/src/main/java/com/smellouk/kamper/android/CpuFragment.kt (full file — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/FpsFragment.kt (full file — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/Utils.kt (lines 39-79 CpuWork class — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/views/MetricRowView.kt (custom view — analog)
    - demos/android/src/main/res/layout/fragment_cpu.xml (CPU layout structure)
    - demos/android/src/main/res/layout/fragment_fps.xml (FPS layout structure)
    - demos/android/src/main/res/layout/view_metric_row.xml (custom view layout)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt (verify field names `cpuScreen` / `fpsScreen` and the package CpuScreen/FpsScreen are imported from)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt (currently contains stub classes — to be removed for these 2 + Task 2's 2)
    - kamper/modules/cpu/src/commonMain/kotlin/com/smellouk/kamper/cpu/CpuInfo.kt (verify field names — totalUseRatio, appRatio, userRatio, systemRatio, ioWaitRatio + INVALID sentinel)
    - kamper/modules/fps/src/commonMain (verify FpsInfo.fps and INVALID sentinel)
  </read_first>
  <action>
    **Step 1: Add the supporting view + utility files**.

    **`kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt`** — copy structurally from `demos/android/src/main/java/com/smellouk/kamper/android/views/MetricRowView.kt` (read the file first). Change package to `com.smellouk.kamperdemo.views`. Update layout reference if it inflates a layout XML to point to `R.layout.view_metric_row` in the kamper-demo package. Properties: `var label: String`, `var valueText: String`, `var fraction: Float`, `var barColor: Int`.

    **`kamper-demo/app/src/main/res/layout/view_metric_row.xml`** — copy structurally from `demos/android/src/main/res/layout/view_metric_row.xml`. Adjust color references to use `@color/kamper_*` from the kamper-demo colors.xml created in Plan 02.

    **`kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt`** — port `CpuWork`/`RandomSort` classes from `demos/android/.../Utils.kt` lines 39-79. Change package to `com.smellouk.kamperdemo`. The class should expose `fun execute()` that spins up a single thread running bogosort over a 1M-int array. CpuScreen's toggleLoad() will spawn N CpuWork instances (4) when the user taps Start CPU Load. Do NOT include the `downloadBigBuckBunnyVideo` helper — Plan 04 NetworkScreen handles its own download.

    **Step 2: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt`** — port `CpuFragment.kt` analog with these adjustments:
    - Package: `com.smellouk.kamperdemo.cpu`
    - Class name: `CpuScreen`
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamperdemo.views.MetricRowView`, `com.smellouk.kamper.cpu.CpuInfo`
    - Layout reference: `R.layout.screen_cpu` (created next)
    - 5 MetricRowView fields: `totalRow`, `appRow`, `userRow`, `systemRow`, `ioWaitRow`. Each is `var XxxRow: MetricRowView? = null` (nullable; set in onViewCreated; nulled in onDestroyView).
    - 1 button field: `loadButton: MaterialButton? = null`
    - In onViewCreated: findViewById all 6 views, set label + barColor on each row (Total = `@color/kamper_blue` (0xFF89B4FA), App = `@color/kamper_green`, user = `@color/kamper_yellow`, system = `@color/kamper_peach`, ioWait = `@color/kamper_purple`). Wire `loadButton.setOnClickListener { toggleLoad() }`.
    - `fun update(info: CpuInfo)` — guarded by `val act = activity ?: return; act.runOnUiThread { ... }`. Update each row's `fraction` and `valueText`. Format: `"%.0f%%".format(info.appRatio * 100)`.
    - `@Volatile private var isLoading = false` flag.
    - `private fun toggleLoad()` — toggles flag, swaps button text "Start CPU Load" / "Stop CPU Load", swaps button tint (kamper_red for Stop, kamper_surface for Start). When isLoading=true, spawn 4 daemon threads each running `while (isLoading) { /* spin */ }` (per analog).
    - `override fun onDestroyView()` — set isLoading = false; null all 6 view fields; call super.

    **Step 3: Write `kamper-demo/app/src/main/res/layout/screen_cpu.xml`** — port from `demos/android/src/main/res/layout/fragment_cpu.xml`. Adjust:
    - Custom view tag: `<com.smellouk.kamperdemo.views.MetricRowView>` (NOT `com.smellouk.kamper.android.views.MetricRowView`)
    - Color references: `@color/kamper_*` palette
    - View IDs: `cpuTotalRow`, `cpuAppRow`, `cpuUserRow`, `cpuSystemRow`, `cpuIoWaitRow`, `cpuLoadButton`

    **Step 4: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt`** — port `FpsFragment.kt` analog (very simple — single TextView):
    - Package: `com.smellouk.kamperdemo.fps`
    - Class name: `FpsScreen`
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamper.fps.FpsInfo`
    - Layout: `R.layout.screen_fps`
    - One field: `private var fpsLabel: TextView? = null`
    - In onViewCreated: `fpsLabel = view.findViewById(R.id.fpsLabel)`
    - `fun update(info: FpsInfo)` — `activity?.runOnUiThread { fpsLabel?.text = info.fps.toString() }`
    - In onDestroyView: `fpsLabel = null; super.onDestroyView()`

    **Step 5: Write `kamper-demo/app/src/main/res/layout/screen_fps.xml`** — port `fragment_fps.xml`:
    - Big centered TextView id `fpsLabel`, default text "—", textSize 64sp, color `@color/kamper_blue`
    - Surrounding label "Frames per second", textColor `@color/kamper_grey`

    **Step 6: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt`** — DELETE the `class CpuScreen` and `class FpsScreen` declarations (lines that match `class CpuScreen : Fragment()` and `class FpsScreen : Fragment()`). Keep the other 6 stubs intact for now (Task 2 deletes 2 more; Plan 04 deletes the rest). Also keep the `placeholderView` extension function at the top — the remaining stubs still use it.

    Do NOT modify `MainActivity.kt` — its imports now resolve to the real screens via Kotlin's import resolution because the new files declare `class CpuScreen` and `class FpsScreen` in the same package `com.smellouk.kamperdemo.cpu` / `com.smellouk.kamperdemo.fps`. Update MainActivity imports if necessary: change `import com.smellouk.kamperdemo.CpuScreen` (auto-imported from NoopScreens.kt's old position) to `import com.smellouk.kamperdemo.cpu.CpuScreen` and `import com.smellouk.kamperdemo.fps.FpsScreen`. Verify with grep.

    **Step 7: Verify** — run `cd kamper-demo && ./gradlew help -q` (or fallback `./gradlew tasks --all -m`). The project must still parse.
  </action>
  <verify>
    <automated>test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/views/MetricRowView.kt &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_cpu.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_fps.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/view_metric_row.xml &amp;&amp; grep -q "class CpuScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt &amp;&amp; grep -q "fun update(info: CpuInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt &amp;&amp; grep -q "package com.smellouk.kamperdemo.cpu" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/cpu/CpuScreen.kt &amp;&amp; grep -q "class FpsScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt &amp;&amp; grep -q "fun update(info: FpsInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/fps/FpsScreen.kt &amp;&amp; grep -q "class CpuWork" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/Utils.kt &amp;&amp; ! grep -E "^class CpuScreen|^class FpsScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt &amp;&amp; cd kamper-demo &amp;&amp; (./gradlew help -q 2&gt;&amp;1 || ./gradlew tasks --all -m 2&gt;&amp;1) | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 7 new files exist (CpuScreen.kt, FpsScreen.kt, Utils.kt, MetricRowView.kt, screen_cpu.xml, screen_fps.xml, view_metric_row.xml)
    - CpuScreen.kt declares `class CpuScreen` and `fun update(info: CpuInfo)` and uses package `com.smellouk.kamperdemo.cpu`
    - FpsScreen.kt declares `class FpsScreen` and `fun update(info: FpsInfo)`
    - Utils.kt declares `class CpuWork`
    - NoopScreens.kt no longer contains a top-level `class CpuScreen` or `class FpsScreen` declaration (matched by `^class CpuScreen` / `^class FpsScreen`)
    - MainActivity.kt imports `com.smellouk.kamperdemo.cpu.CpuScreen` and `com.smellouk.kamperdemo.fps.FpsScreen` (verify with grep if needed)
    - kamper-demo Gradle project still parses
  </acceptance_criteria>
  <done>
    CpuScreen and FpsScreen are real, functional Fragment implementations with proper view lifecycle (onDestroyView nulls), thread-safe UI dispatch, and INVALID-guarded listener handling. The CPU stress trigger works via 4 daemon threads + @Volatile flag. Project still compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: MemoryScreen + JankScreen + layouts + NoopScreens cleanup</name>
  <files>
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt,
    kamper-demo/app/src/main/res/layout/screen_memory.xml,
    kamper-demo/app/src/main/res/layout/screen_jank.xml,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt
  </files>
  <read_first>
    - demos/android/src/main/java/com/smellouk/kamper/android/MemoryFragment.kt (full — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/JankFragment.kt (full — analog)
    - demos/android/src/main/res/layout/fragment_memory.xml (memory layout)
    - demos/android/src/main/res/layout/fragment_jank.xml (jank layout)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt (verify field names `memoryScreen`, `jankScreen` are used — NOT renamed)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt (contains MemoryScreen + JankScreen stubs to remove)
    - kamper/modules/memory/src/commonMain (verify nested struct field names: heapMemoryInfo.allocatedInMb / maxMemoryInMb, ramInfo.totalRamInMb / availableRamInMb / isLowMemory, pssInfo.totalPssInMb / dalvikPssInMb / nativePssInMb / otherPssInMb)
    - kamper/modules/jank/src/commonMain (verify JankInfo: droppedFrames, jankyFrameRatio, worstFrameMs)
  </read_first>
  <action>
    **Step 1: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt`** — port `MemoryFragment.kt`:
    - Package: `com.smellouk.kamperdemo.memory`
    - Class: `MemoryScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamperdemo.views.MetricRowView`, `com.smellouk.kamper.memory.MemoryInfo`
    - Layout: `R.layout.screen_memory`
    - Fields (all `var X: View? = null` lifecycle-scoped): heapRow (MetricRowView), heapDetail (TextView), ramRow (MetricRowView), ramDetail (TextView), pssDetail (TextView), lowMemBadge (TextView), allocButton (MaterialButton), releaseButton (MaterialButton)
    - Persistent state across views: `private val allocations = mutableListOf<ByteArray>()` — declared at class level (NOT view-scoped) so it survives view recreation per central-install pattern. (This is INTENTIONAL — the memory leak demo's whole point is that GC can't reclaim while references are held.)
    - In onViewCreated: findViewById all 8 views, set MetricRowView labels (Heap = `@color/kamper_green`, RAM = `@color/kamper_blue`). Wire allocButton: `allocations.add(ByteArray(32 * 1024 * 1024))`. Wire releaseButton: `allocations.clear(); System.gc()`.
    - `fun update(info: MemoryInfo)` — guarded by `val act = activity ?: return; act.runOnUiThread { ... }`. Update heap row from `info.heapMemoryInfo.allocatedInMb / info.heapMemoryInfo.maxMemoryInMb`; update RAM row from `(info.ramInfo.totalRamInMb - info.ramInfo.availableRamInMb) / info.ramInfo.totalRamInMb`; show isLowMemory badge if `info.ramInfo.isLowMemory`; update pssDetail with totalPssInMb/dalvikPssInMb/nativePssInMb/otherPssInMb (only if `info.pssInfo.totalPssInMb >= 0`).
    - In onDestroyView: null all 8 view fields. Do NOT clear `allocations` — that's the whole point of the leak demo (the user can switch tabs and see memory hold). Call super.

    **Step 2: Write `kamper-demo/app/src/main/res/layout/screen_memory.xml`** — port `fragment_memory.xml`:
    - 2 MetricRowView (heapRow, ramRow) with kamper_demo namespace
    - TextViews for heapDetail, ramDetail, pssDetail, lowMemBadge
    - 2 MaterialButtons: `allocButton` ("Alloc 32 MB"), `releaseButton` ("Release & GC")
    - View IDs match the field names exactly

    **Step 3: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt`** — port `JankFragment.kt`:
    - Package: `com.smellouk.kamperdemo.jank`
    - Class: `JankScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamper.jank.JankInfo`
    - Layout: `R.layout.screen_jank`
    - Fields: droppedLabel (TextView), ratioValue (TextView), worstValue (TextView), simulateButton (MaterialButton)
    - In onViewCreated: findViewById all 4. Wire simulateButton: `simulateJank()`.
    - `fun update(info: JankInfo)` — `activity?.runOnUiThread { droppedLabel?.text = info.droppedFrames.toString(); ratioValue?.text = "%.1f%%".format(info.jankyFrameRatio * 100); worstValue?.text = "${info.worstFrameMs} ms" }`
    - `private fun simulateJank()` — main-thread busy wait: `val end = System.nanoTime() + 200_000_000L; @Suppress("ControlFlowWithEmptyBody") while (System.nanoTime() < end) {}` — exactly 200 ms (per RESEARCH.md Pattern 5). This MUST execute on the main thread; this is NOT a bug — it's the bottleneck demo.
    - In onDestroyView: null all 4 view fields; super.

    **Step 4: Write `kamper-demo/app/src/main/res/layout/screen_jank.xml`** — port `fragment_jank.xml`:
    - 3 metric value TextViews: `droppedLabel` (large, `@color/kamper_peach`), `ratioValue`, `worstValue`
    - Surrounding label TextViews ("Dropped Frames", "Janky Ratio", "Worst Frame")
    - Button id `simulateButton` text "Simulate Jank (200 ms)"

    **Step 5: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt`** — DELETE `class MemoryScreen` and `class JankScreen` declarations. Remaining stubs after this task: NetworkScreen, GcScreen, ThermalScreen, IssuesScreen (Plan 04 deletes those).

    **Step 6: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt`** — verify imports point to the new package locations:
    - `import com.smellouk.kamperdemo.memory.MemoryScreen`
    - `import com.smellouk.kamperdemo.jank.JankScreen`
    Use Edit tool to fix imports if Plan 02 left them as `import com.smellouk.kamperdemo.MemoryScreen` (sourced from NoopScreens.kt). With both Tasks 1 and 2 complete, MainActivity.kt should now import 4 screens from sub-packages and 4 still from the root NoopScreens.kt.

    **Step 7: Verify** — run `cd kamper-demo && ./gradlew help -q` (or fallback `./gradlew tasks --all -m`). Project must still parse.
  </action>
  <verify>
    <automated>test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_memory.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_jank.xml &amp;&amp; grep -q "class MemoryScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt &amp;&amp; grep -q "fun update(info: MemoryInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt &amp;&amp; grep -q "32 \* 1024 \* 1024\|33554432" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/memory/MemoryScreen.kt &amp;&amp; grep -q "class JankScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt &amp;&amp; grep -q "fun update(info: JankInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt &amp;&amp; grep -q "200_000_000L" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/jank/JankScreen.kt &amp;&amp; ! grep -E "^class MemoryScreen|^class JankScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt &amp;&amp; grep -q "import com.smellouk.kamperdemo.memory.MemoryScreen\|import com.smellouk.kamperdemo.jank.JankScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; cd kamper-demo &amp;&amp; (./gradlew help -q 2&gt;&amp;1 || ./gradlew tasks --all -m 2&gt;&amp;1) | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 4 new files exist (MemoryScreen.kt, JankScreen.kt, screen_memory.xml, screen_jank.xml)
    - MemoryScreen.kt has package `com.smellouk.kamperdemo.memory`, declares `class MemoryScreen`, `fun update(info: MemoryInfo)`, allocates 32 MB ByteArray (literal substring `32 * 1024 * 1024` OR `33554432`)
    - JankScreen.kt has package `com.smellouk.kamperdemo.jank`, declares `class JankScreen`, `fun update(info: JankInfo)`, contains the 200 ms busy-wait literal `200_000_000L`
    - NoopScreens.kt no longer contains `class MemoryScreen` or `class JankScreen`
    - MainActivity.kt imports MemoryScreen and JankScreen from their new sub-packages
    - kamper-demo project still parses
  </acceptance_criteria>
  <done>
    MemoryScreen and JankScreen are functional. Memory leak demo holds strong refs across view recreation (intentional). Jank demo blocks main thread 200 ms exactly. Project still compiles. NoopScreens.kt now contains only 4 stubs (NetworkScreen, GcScreen, ThermalScreen, IssuesScreen) for Plan 04 to replace.
  </done>
</task>

</tasks>

<verification>
- 4 real screen files in package subdirectories (cpu/, fps/, memory/, jank/)
- Each defines `fun update(info: XxxInfo)` (verified by grep)
- NoopScreens.kt has had 4 of 8 stub declarations removed
- MainActivity imports updated to point to subpackages
- `./gradlew help -q` (or `tasks --all -m` fallback) still succeeds
</verification>

<success_criteria>
- 4 real Fragment implementations + MetricRowView + Utils committed under `kamper-demo/`
- All 4 screens follow the demos/android/ analogs structurally
- All 4 screens dispatch UI updates via `activity?.runOnUiThread { ... }` (RESEARCH.md Shared Pattern: Thread-Safe UI Dispatch)
- All 4 screens null view references in onDestroyView (RESEARCH.md Shared Pattern: View Cleanup)
- MainActivity central-install (Plan 02) is preserved and now routes to real screens
- DOC-03: half of the 8 demo screens are live and bottleneck-inducing
</success_criteria>

<output>
After completion, create `.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-03-SUMMARY.md`
</output>

---
phase: 17-medium-article-series-android-performance-bottlenecks-kamper
plan: 04
type: execute
wave: 3
depends_on:
  - 17-03
files_modified:
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt
  - kamper-demo/app/src/main/res/layout/screen_network.xml
  - kamper-demo/app/src/main/res/layout/screen_gc.xml
  - kamper-demo/app/src/main/res/layout/screen_thermal.xml
  - kamper-demo/app/src/main/res/layout/screen_issues.xml
  - kamper-demo/app/src/main/res/layout/item_issue.xml
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt
  - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "NetworkScreen exists with rx/tx (system) + rx/tx (app) rendering and 5 MB download trigger button"
    - "NetworkScreen handles NetworkInfo.NOT_SUPPORTED sentinel by displaying 'Not supported on this device'"
    - "GcScreen exists rendering gcCountDelta, gcPauseMsDelta, gcCount with a 'Trigger GC Pressure' button (200_000 ByteArray churn)"
    - "ThermalScreen exists with state label + isThrottling flag + Start CPU Stress (toggle) button"
    - "ThermalScreen layout includes the 'requires physical device' label visible in the UI (per RESEARCH.md Pitfall 3)"
    - "IssuesScreen exists with RecyclerView + 4 trigger buttons: Trigger ANR (6 s sleep), Trigger Crash, Trigger Memory Pressure (~100 MB), Trigger SlowSpan"
    - "NoopScreens.kt is deleted entirely (no remaining stub classes — all 8 replaced)"
    - "MainActivity imports updated to import 8 real screens from package subdirectories"
    - "kamper-demo project still parses (./gradlew help -q OR ./gradlew tasks --all -m succeeds)"
    - "assembleDebug task is still registered"
  artifacts:
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt"
      provides: "Real NetworkScreen Fragment — system + app rx/tx + 5 MB download"
      contains: "NetworkInfo.NOT_SUPPORTED"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt"
      provides: "Real GcScreen Fragment — count/pause/delta + GC pressure trigger"
      contains: "fun update(info: GcInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt"
      provides: "Real ThermalScreen Fragment — state + throttling + emulator-warning UI"
      contains: "fun update(info: ThermalInfo)"
    - path: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt"
      provides: "Real IssuesScreen Fragment — RecyclerView + 4 trigger buttons"
      contains: "fun addIssue(issue: Issue)"
    - path: "kamper-demo/app/src/main/res/layout/screen_thermal.xml"
      provides: "Thermal screen layout including the physical-device-required warning text"
      contains: "physical device"
  key_links:
    - from: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt"
      to: "https://speed.cloudflare.com/__down?bytes=5000000"
      via: "URL().openStream() inside an Executor-submitted Runnable"
      pattern: "speed.cloudflare.com|openStream"
    - from: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt"
      to: "Issue (com.smellouk.kamper.issues.Issue)"
      via: "RecyclerView adapter rendering Issue type/severity/message/timestampMs"
      pattern: "Issue|IssueType"
    - from: "kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt"
      to: "8 real screens in subpackages"
      via: "imports replaced from com.smellouk.kamperdemo.* (NoopScreens.kt) to com.smellouk.kamperdemo.{cpu,fps,memory,network,jank,gc,thermal,issues}.*"
      pattern: "import com.smellouk.kamperdemo.network.NetworkScreen|import com.smellouk.kamperdemo.gc.GcScreen|import com.smellouk.kamperdemo.thermal.ThermalScreen|import com.smellouk.kamperdemo.issues.IssuesScreen"
---

<objective>
Replace the remaining 4 noop screen stubs with real, runnable Fragment implementations: NetworkScreen, GcScreen, ThermalScreen, IssuesScreen. Delete NoopScreens.kt entirely. Update MainActivity imports to point all 8 screens at their package subdirectories.

Purpose: Per D-09, all 8 bottleneck demo screens must exist with working artificial triggers. After this plan, the demo app delivers the full bottleneck matrix (CPU/FPS/Memory/Network/Jank/GC/Thermal/Issues) ready for the human-verification checkpoint in Plan 05.

Output: 4 real screen Kotlin files + 4 layout XMLs + 1 issue list-item layout + NoopScreens.kt deletion + MainActivity import updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-CONTEXT.md
@.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-PATTERNS.md
@CLAUDE.md
@demos/android/src/main/java/com/smellouk/kamper/android/NetworkFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/GcFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/ThermalFragment.kt
@demos/android/src/main/java/com/smellouk/kamper/android/IssuesFragment.kt

<interfaces>
NetworkScreen → `fun update(info: NetworkInfo)` — info has fields rxSystemTotalInMb (Float), txSystemTotalInMb (Float), rxAppInMb (Float), txAppInMb (Float). Special sentinel: `NetworkInfo.NOT_SUPPORTED`.

GcScreen → `fun update(info: GcInfo)` — info has fields gcCount (Long), gcPauseMs (Long), gcCountDelta (Long), gcPauseMsDelta (Long).

ThermalScreen → `fun update(info: ThermalInfo)` — info has fields state (ThermalState enum: NONE, LIGHT, MODERATE, SEVERE, CRITICAL, EMERGENCY, SHUTDOWN, UNKNOWN), isThrottling (Boolean).

IssuesScreen → `fun addIssue(issue: Issue)` — Issue has id, type (IssueType enum: ANR, CRASH, DROPPED_FRAME, SLOW_SPAN, MEMORY_PRESSURE, NEAR_OOM, STRICT_VIOLATION, SLOW_START), severity (Severity enum: CRITICAL, ERROR, WARNING, INFO), message (String), timestampMs (Long), durationMs (Long?), stackTrace (String?), threadName (String?), details (Map<String, Any>?).

Color rules from 17-PATTERNS.md (Catppuccin Mocha palette):
- Severity.CRITICAL → kamper_red (0xFFF38BA8)
- Severity.ERROR → kamper_peach (0xFFFAB387)
- Severity.WARNING → kamper_yellow (0xFFF9E2AF)
- Severity.INFO → kamper_green (0xFFA6E3A1)
- IssueType.ANR / CRASH → kamper_red
- IssueType.DROPPED_FRAME → kamper_yellow
- IssueType.SLOW_SPAN / SLOW_START → kamper_blue
- IssueType.MEMORY_PRESSURE / NEAR_OOM → kamper_purple
- IssueType.STRICT_VIOLATION → kamper_teal

ThermalState color mapping (from analog ThermalFragment.kt lines 69-78):
- NONE → kamper_green
- LIGHT → kamper_teal
- MODERATE → kamper_yellow
- SEVERE/CRITICAL/EMERGENCY/SHUTDOWN → kamper_peach
- UNKNOWN → kamper_grey
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkScreen + GcScreen + layouts</name>
  <files>
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt,
    kamper-demo/app/src/main/res/layout/screen_network.xml,
    kamper-demo/app/src/main/res/layout/screen_gc.xml,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt
  </files>
  <read_first>
    - demos/android/src/main/java/com/smellouk/kamper/android/NetworkFragment.kt (full — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/GcFragment.kt (full — analog)
    - demos/android/src/main/res/layout/fragment_network.xml (network layout)
    - demos/android/src/main/res/layout/fragment_gc.xml (gc layout)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt (verify field names networkScreen, gcScreen)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt (currently has NetworkScreen + GcScreen + ThermalScreen + IssuesScreen stubs)
    - kamper/modules/network/src/commonMain (verify NetworkInfo fields + NOT_SUPPORTED sentinel)
    - kamper/modules/gc/src/commonMain/kotlin/com/smellouk/kamper/gc/GcInfo.kt (verify GcInfo fields)
  </read_first>
  <action>
    **Step 1: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt`** — port `NetworkFragment.kt`:
    - Package: `com.smellouk.kamperdemo.network`
    - Class: `NetworkScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamperdemo.views.MetricRowView`, `com.smellouk.kamper.network.NetworkInfo`, `java.net.URL`, `java.util.concurrent.Executors`
    - Layout: `R.layout.screen_network`
    - Fields: rxRow + txRow + appRxRow + appTxRow (MetricRowView), rxDetail + txDetail + appRxDetail + appTxDetail + statusText (TextView), downloadButton (View)
    - State: `private var peakRx = 0f; private var peakTx = 0f` (track peaks for chart scaling)
    - In onViewCreated: findViewById all 9. Set MetricRowView labels (Download = `kamper_teal`, Upload = `kamper_purple`, Rx App = `kamper_teal`, Tx App = `kamper_purple`). Wire downloadButton: triggerDownload().
    - `fun update(info: NetworkInfo)` — guard: `val act = activity ?: return; act.runOnUiThread { ... }`. **NOT_SUPPORTED handling**: if `info == NetworkInfo.NOT_SUPPORTED`, set rxDetail/txDetail to "Not supported on this device" and return@runOnUiThread. Otherwise update peakRx/peakTx, scale rows, format with helper `formatSpeed(mb: Float)` — see analog lines 110-114.
    - `private fun triggerDownload()` — `Executors.newSingleThreadExecutor().submit { runCatching { URL("https://speed.cloudflare.com/__down?bytes=5000000").openStream().use { it.readBytes() } }; act.runOnUiThread { if (isAdded) { statusText?.text = "Done"; downloadButton?.isEnabled = true } } }` (per analog lines 92-108).
    - In onDestroyView: null all view fields; super.

    **Step 2: Write `kamper-demo/app/src/main/res/layout/screen_network.xml`** — port `fragment_network.xml`:
    - 4 MetricRowView (rxRow, txRow, appRxRow, appTxRow) — namespace `com.smellouk.kamperdemo.views.MetricRowView`
    - 4 TextView details + 1 statusText TextView
    - MaterialButton id `downloadButton` text "Download 5 MB"
    - Color references → `@color/kamper_*`

    **Step 3: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt`** — port `GcFragment.kt`:
    - Package: `com.smellouk.kamperdemo.gc`
    - Class: `GcScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamper.gc.GcInfo`
    - Layout: `R.layout.screen_gc`
    - Fields: countDeltaLabel (TextView), pauseValue (TextView), totalValue (TextView), simulateButton (MaterialButton)
    - In onViewCreated: findViewById; wire simulateButton: simulateGc().
    - `fun update(info: GcInfo)` — `activity?.runOnUiThread { countDeltaLabel?.text = info.gcCountDelta.toString(); pauseValue?.text = "${info.gcPauseMsDelta} ms"; totalValue?.text = info.gcCount.toString() }`
    - `private fun simulateGc()` — `Thread { repeat(200_000) { ByteArray(1024) } }.apply { isDaemon = true; start() }` (per analog lines 44-49 + RESEARCH.md Pattern 4)
    - In onDestroyView: null all view fields; super.

    **Step 4: Write `kamper-demo/app/src/main/res/layout/screen_gc.xml`** — port `fragment_gc.xml`:
    - 1 large primary value TextView id `countDeltaLabel` (color `@color/kamper_purple`, textSize 64sp, default text "0")
    - 2 secondary value TextViews: `pauseValue`, `totalValue`
    - Surrounding labels ("GCs This Interval", "Pause Time This Interval", "Total GCs Since Start")
    - MaterialButton id `simulateButton` text "Trigger GC Pressure"

    **Step 5: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt`** — DELETE `class NetworkScreen` and `class GcScreen` declarations. Remaining stubs: ThermalScreen, IssuesScreen (Task 2 deletes those + the file).

    **Step 6: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt`** — fix imports:
    - `import com.smellouk.kamperdemo.network.NetworkScreen`
    - `import com.smellouk.kamperdemo.gc.GcScreen`
    Replace any `import com.smellouk.kamperdemo.NetworkScreen` / `GcScreen` references that came from NoopScreens.kt.

    **Step 7: Verify** — `cd kamper-demo && ./gradlew help -q` (or `./gradlew tasks --all -m`).
  </action>
  <verify>
    <automated>test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_network.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_gc.xml &amp;&amp; grep -q "class NetworkScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt &amp;&amp; grep -q "fun update(info: NetworkInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt &amp;&amp; grep -q "NetworkInfo.NOT_SUPPORTED" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt &amp;&amp; grep -q "speed.cloudflare.com" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/network/NetworkScreen.kt &amp;&amp; grep -q "class GcScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt &amp;&amp; grep -q "fun update(info: GcInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt &amp;&amp; grep -q "200_000" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/gc/GcScreen.kt &amp;&amp; ! grep -E "^class NetworkScreen|^class GcScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt &amp;&amp; grep -q "import com.smellouk.kamperdemo.network.NetworkScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; grep -q "import com.smellouk.kamperdemo.gc.GcScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; cd kamper-demo &amp;&amp; (./gradlew help -q 2&gt;&amp;1 || ./gradlew tasks --all -m 2&gt;&amp;1) | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 4 new files exist (NetworkScreen.kt, GcScreen.kt, screen_network.xml, screen_gc.xml)
    - NetworkScreen.kt is in `com.smellouk.kamperdemo.network`, declares `fun update(info: NetworkInfo)`, references `NetworkInfo.NOT_SUPPORTED` and the cloudflare speed test URL
    - GcScreen.kt is in `com.smellouk.kamperdemo.gc`, declares `fun update(info: GcInfo)`, contains the `200_000` ByteArray churn count
    - NoopScreens.kt no longer contains `class NetworkScreen` or `class GcScreen`
    - MainActivity.kt imports `com.smellouk.kamperdemo.network.NetworkScreen` AND `com.smellouk.kamperdemo.gc.GcScreen`
    - kamper-demo project still parses
  </acceptance_criteria>
  <done>
    NetworkScreen and GcScreen are functional. NetworkScreen handles NOT_SUPPORTED gracefully (RESEARCH.md Pitfall 3 analog for unsupported devices). GcScreen induces measurable GC pressure via ByteArray churn. Project still compiles. NoopScreens.kt now contains only ThermalScreen and IssuesScreen stubs.
  </done>
</task>

<task type="auto">
  <name>Task 2: ThermalScreen + IssuesScreen + layouts; delete NoopScreens.kt entirely</name>
  <files>
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt,
    kamper-demo/app/src/main/res/layout/screen_thermal.xml,
    kamper-demo/app/src/main/res/layout/screen_issues.xml,
    kamper-demo/app/src/main/res/layout/item_issue.xml,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt,
    kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt
  </files>
  <read_first>
    - demos/android/src/main/java/com/smellouk/kamper/android/ThermalFragment.kt (full — analog)
    - demos/android/src/main/java/com/smellouk/kamper/android/IssuesFragment.kt (full — analog; the longest, with RecyclerView adapter + 4 trigger buttons)
    - demos/android/src/main/res/layout/fragment_thermal.xml (thermal layout)
    - demos/android/src/main/res/layout/fragment_issues.xml (issues layout)
    - demos/android/src/main/res/layout/item_issue.xml (RecyclerView row layout)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt (verify field names thermalScreen, issuesScreen)
    - kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt (currently has ThermalScreen + IssuesScreen stubs to remove; whole file goes after this task)
    - kamper/modules/thermal/src/commonMain/kotlin/com/smellouk/kamper/thermal/ThermalInfo.kt + ThermalState enum
    - kamper/modules/issues/src/androidMain/kotlin/com/smellouk/kamper/issues (Issue, IssueType, Severity, IssueSpans data classes)
  </read_first>
  <action>
    **Step 1: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt`** — port `ThermalFragment.kt`:
    - Package: `com.smellouk.kamperdemo.thermal`
    - Class: `ThermalScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamper.thermal.ThermalInfo`, `com.smellouk.kamper.thermal.ThermalState`
    - Layout: `R.layout.screen_thermal`
    - Fields: stateLabel (TextView), throttlingValue (TextView), simulateButton (MaterialButton)
    - State: `@Volatile private var isStressing = false`
    - In onViewCreated: findViewById; wire simulateButton: toggleStress().
    - `fun update(info: ThermalInfo)` — `activity?.runOnUiThread { stateLabel?.apply { text = info.state.name; setTextColor(colorForState(info.state)) }; throttlingValue?.apply { text = if (info.isThrottling) "YES" else "NO"; setTextColor(if (info.isThrottling) 0xFFFAB387.toInt() else 0xFFA6E3A1.toInt()) } }`
    - `private fun toggleStress()` — when isStressing=true, spawn `Runtime.getRuntime().availableProcessors()` daemon threads each running `var x = 0.0; while (isStressing) { x = Math.sin(x + Math.PI) }` (per analog lines 53-67 + RESEARCH.md Pattern 6). Toggle button text "Start CPU Stress" / "Stop CPU Stress".
    - `private fun colorForState(state: ThermalState): Int` — exact mapping from RESEARCH.md PATTERNS.md lines 405-415 (NONE → green, LIGHT → teal, MODERATE → yellow, SEVERE/CRITICAL/EMERGENCY/SHUTDOWN → kamper_peach, UNKNOWN → grey)
    - In onDestroyView: `isStressing = false`; null view fields; super.

    **Step 2: Write `kamper-demo/app/src/main/res/layout/screen_thermal.xml`** — port `fragment_thermal.xml`. **MUST INCLUDE** a TextView visible at the top of the screen with text "⚠ Thermal detection requires a physical device." (or near-equivalent containing the literal substring "physical device") per RESEARCH.md Pitfall 3. Plus:
    - Large stateLabel TextView (textSize 48sp, default text "—")
    - throttlingValue TextView
    - Surrounding label TextViews ("Thermal State", "Throttling")
    - MaterialButton id `simulateButton` text "Start CPU Stress"

    **Step 3: Write `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt`** — port `IssuesFragment.kt` analog (longest fragment — read it carefully):
    - Package: `com.smellouk.kamperdemo.issues`
    - Class: `IssuesScreen` extends Fragment
    - Imports: `com.smellouk.kamperdemo.R`, `com.smellouk.kamper.issues.Issue`, `com.smellouk.kamper.issues.IssueSpans`, `com.smellouk.kamper.issues.IssueType`, `com.smellouk.kamper.issues.Severity`, plus AndroidX `RecyclerView`, `LinearLayoutManager`, `Handler`, `Looper`, `java.util.concurrent.Executors`
    - Layout: `R.layout.screen_issues`
    - Fields: recyclerView (RecyclerView), 4 buttons (anrButton, crashButton, memPressureButton, slowSpanButton), issuesList (mutableListOf<Issue>), adapter (an inner IssueAdapter class)
    - Inner `IssueAdapter` class — extends `RecyclerView.Adapter<ViewHolder>`. Layout `R.layout.item_issue`. Per-row views: typeLabel, severityLabel, messageLabel, timestampLabel, threadLabel (all TextView). Color the typeLabel by IssueType (per `<interfaces>` color rules above). Color the severityLabel by Severity. Format timestamp with `java.text.SimpleDateFormat("HH:mm:ss.SSS", Locale.US)`. If issue.stackTrace != null, show first line beneath messageLabel.
    - In onViewCreated: findViewById all 5; set up RecyclerView with LinearLayoutManager and adapter. Wire each button: anrButton → triggerAnr(), crashButton → triggerCrash(), memPressureButton → triggerMemoryPressure(), slowSpanButton → triggerSlowSpan().
    - `fun addIssue(issue: Issue)` — `activity?.runOnUiThread { issuesList.add(0, issue); adapter?.notifyItemInserted(0); recyclerView?.scrollToPosition(0) }`
    - `private fun triggerAnr()` — `Handler(Looper.getMainLooper()).post { Thread.sleep(6_000L) }` (per analog lines 92-95 + RESEARCH.md Pattern 7)
    - `private fun triggerCrash()` — `Thread { throw RuntimeException("Demo crash triggered from kamper-demo IssuesScreen") }.also { it.isDaemon = true; it.start() }` (per analog lines 97-101)
    - `private fun triggerMemoryPressure()` — `Executors.newSingleThreadExecutor().submit { val chunks = mutableListOf<ByteArray>(); try { repeat(20) { chunks.add(ByteArray(5 * 1024 * 1024)) }; Thread.sleep(3_000L) } finally { chunks.clear() } }` (per analog lines 103-113)
    - `private fun triggerSlowSpan()` — `Executors.newSingleThreadExecutor().submit { IssueSpans.measure("demo-background-op", thresholdMs = 500L) { Thread.sleep(1500) } }` (per analog lines 84-89)
    - In onDestroyView: null all view fields + adapter; super.

    **Step 4: Write `kamper-demo/app/src/main/res/layout/screen_issues.xml`** — port `fragment_issues.xml`:
    - 4 MaterialButtons in a horizontal LinearLayout (or grid): anrButton, crashButton, memPressureButton, slowSpanButton — each labeled appropriately ("Trigger ANR (6s)", "Trigger Crash", "Trigger Memory Pressure", "Trigger SlowSpan")
    - RecyclerView id `recyclerView` filling remaining vertical space

    **Step 5: Write `kamper-demo/app/src/main/res/layout/item_issue.xml`** — port `item_issue.xml`:
    - Vertical LinearLayout with: top row (typeLabel + severityLabel — short colored text); messageLabel (single line); timestampLabel + threadLabel (small grey text)

    **Step 6: DELETE `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt`** entirely. The 4 remaining stubs (ThermalScreen, IssuesScreen) AND the 4 already-removed stubs are all replaced. The `placeholderView` extension and the file header comment go away with the file.

    **Step 7: Update `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt`** imports — replace any remaining root-package imports with the subpackage imports. Final import list MUST include exactly these 8:
    - `import com.smellouk.kamperdemo.cpu.CpuScreen`
    - `import com.smellouk.kamperdemo.fps.FpsScreen`
    - `import com.smellouk.kamperdemo.memory.MemoryScreen`
    - `import com.smellouk.kamperdemo.network.NetworkScreen`
    - `import com.smellouk.kamperdemo.jank.JankScreen`
    - `import com.smellouk.kamperdemo.gc.GcScreen`
    - `import com.smellouk.kamperdemo.thermal.ThermalScreen`
    - `import com.smellouk.kamperdemo.issues.IssuesScreen`

    **Step 8: Verify** — `cd kamper-demo && ./gradlew help -q` (or `./gradlew tasks --all -m`). Ensure assembleDebug is still registered: `./gradlew :app:tasks --all 2>&1 | grep assembleDebug`. Also confirm the screen_placeholder.xml layout from Plan 02 is no longer referenced anywhere in src/ (it can stay as an orphan resource — it's harmless — or be deleted in Plan 05; do NOT delete it in this plan).
  </action>
  <verify>
    <automated>test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_thermal.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/screen_issues.xml &amp;&amp; test -f kamper-demo/app/src/main/res/layout/item_issue.xml &amp;&amp; test ! -f kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/NoopScreens.kt &amp;&amp; grep -q "class ThermalScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt &amp;&amp; grep -q "fun update(info: ThermalInfo)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/thermal/ThermalScreen.kt &amp;&amp; grep -q -i "physical device" kamper-demo/app/src/main/res/layout/screen_thermal.xml &amp;&amp; grep -q "class IssuesScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt &amp;&amp; grep -q "fun addIssue(issue: Issue)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt &amp;&amp; grep -q "Thread.sleep(6_000L)" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/issues/IssuesScreen.kt &amp;&amp; grep -q "import com.smellouk.kamperdemo.thermal.ThermalScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; grep -q "import com.smellouk.kamperdemo.issues.IssuesScreen" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; ! grep -E "import com.smellouk.kamperdemo.NoopScreens|import com.smellouk.kamperdemo\.(CpuScreen|FpsScreen|MemoryScreen|NetworkScreen|JankScreen|GcScreen|ThermalScreen|IssuesScreen)$" kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/MainActivity.kt &amp;&amp; cd kamper-demo &amp;&amp; (./gradlew help -q 2&gt;&amp;1 || ./gradlew tasks --all -m 2&gt;&amp;1) | tail -3 &amp;&amp; ./gradlew :app:tasks --all 2&gt;&amp;1 | grep -q assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - 5 new files exist (ThermalScreen.kt, IssuesScreen.kt, screen_thermal.xml, screen_issues.xml, item_issue.xml)
    - NoopScreens.kt has been DELETED (test ! -f succeeds)
    - ThermalScreen.kt declares `fun update(info: ThermalInfo)` in package `com.smellouk.kamperdemo.thermal`
    - screen_thermal.xml contains the literal substring "physical device" (case-insensitive grep)
    - IssuesScreen.kt declares `fun addIssue(issue: Issue)` in package `com.smellouk.kamperdemo.issues`, contains `Thread.sleep(6_000L)` for the ANR trigger
    - MainActivity.kt imports thermal.ThermalScreen and issues.IssuesScreen from subpackages
    - MainActivity.kt has NO root-level `import com.smellouk.kamperdemo.{Cpu|Fps|Memory|Network|Jank|Gc|Thermal|Issues}Screen` imports (all 8 are now subpackage imports)
    - kamper-demo project still parses; assembleDebug task is registered
  </acceptance_criteria>
  <done>
    All 8 demo screens are real, functional, and located in their package subdirectories. NoopScreens.kt is deleted. MainActivity routes 8 listeners to 8 functioning screens. The complete bottleneck matrix (CPU/FPS/Memory/Network/Jank/GC/Thermal/Issues) is now wired and ready for Plan 05's full assembleDebug + human checkpoint.
  </done>
</task>

</tasks>

<verification>
- 8 real screens live in 8 subdirectories under `kamper-demo/app/src/main/kotlin/com/smellouk/kamperdemo/`
- NoopScreens.kt is gone
- MainActivity has 8 subpackage imports, 0 root-package screen imports
- ThermalScreen layout includes the "physical device" warning
- IssuesScreen renders ANR/Crash/MemPressure/SlowSpan triggers
- `./gradlew help -q` (or `tasks --all -m`) succeeds
- `assembleDebug` is registered
</verification>

<success_criteria>
- All 4 final screens (Network, Gc, Thermal, Issues) ported from `demos/android/` analogs with kamper-demo package and Catppuccin Mocha colors
- ThermalScreen layout calls out the emulator limitation (RESEARCH.md Pitfall 3)
- IssuesScreen has all 4 trigger buttons + RecyclerView issue list
- NoopScreens.kt deleted; MainActivity references all 8 subpackages
- kamper-demo project parses end-to-end; assembleDebug task registered
- DOC-03: complete demo-app counterpart of the 6 article outlines
</success_criteria>

<output>
After completion, create `.planning/phases/17-medium-article-series-android-performance-bottlenecks-kamper/17-04-SUMMARY.md`
</output>

---
phase: 22
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/ios/ contains zero stale ':kamper:*' Gradle path references"
    - "./gradlew :demos:ios:linkDebugExecutableIosSimulatorArm64 completes BUILD SUCCESSFUL"
    - "User runs the linked iOS simulator binary (or opens via Xcode) and the binary exits cleanly or runs ≥30 seconds without crashing"
    - "Stdout/Xcode console shows non-INVALID values for ≥CPU and Memory during 30s observation"
    - "Smoke test results recorded in 22-04-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md"
      provides: "iOS demo smoke test outcome"
      contains: "outcome:"
  key_links:
    - from: "demos/ios/build.gradle.kts"
      to: ":libs:engine, :libs:modules:*, :libs:ui:kmm"
      via: "iosSimulatorArm64 + iosArm64 binary executable entryPoint"
      pattern: "implementation\\(project\\(\":libs:"
---

<objective>
Validate that the iOS demo (`demos/ios/`) builds via KMP `linkDebugExecutable...` task and passes a 30-second smoke test on a simulator. Per D-06, this is the fourth demo tested. Per D-08, Claude links the binary; the user launches it (Xcode or simulator).

Purpose: Confirm the iOS-specific actuals for `:libs:modules:*` (Darwin / Mach APIs for CPU, `mach_task_basic_info` for Memory, `CADisplayLink` for FPS) wire correctly through `:libs:engine` and that the iOS shake-detection coroutine respects `isActive` (Phase 3 fix DEBT-01 must hold).

Output: A 22-04-SUMMARY.md recording link build status, user's binary observation, and per-module health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md
@CLAUDE.md
@demos/ios/build.gradle.kts

<interfaces>
<!-- iOS KMP build pipeline contract -->

From demos/ios/build.gradle.kts (post-Phase-21):
```kotlin
kotlin {
    iosArm64 {
        binaries {
            executable { entryPoint = "com.smellouk.kamper.ios.main" }
        }
    }
    iosSimulatorArm64 {
        binaries {
            executable { entryPoint = "com.smellouk.kamper.ios.main" }
        }
    }
}

sourceSets {
    val commonMain by getting {
        dependencies {
            implementation(project(":libs:engine"))
            implementation(project(":libs:modules:cpu"))   // ... 8 modules
            implementation(project(":libs:ui:kmm"))
        }
    }
}
```

Build output location:
`demos/ios/build/bin/iosSimulatorArm64/debugExecutable/demos-ios.kexe`

Phase 3 invariant (DEBT-01): iOS shake-detection coroutine uses `while (isActive)` not `while (true)`.
A regression here manifests as a hang on Kamper.stop() during the 30-second observation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale ':kamper:' references in demos/ios/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/ios/build.gradle.kts
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
  </read_first>
  <action>
    Run and capture verbatim:

    1. `grep -rn ":kamper:" demos/ios/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/ios/src/ --include="*.kt" --include="*.swift" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/ios/ || echo "NO_MATCHES"`
    4. If `demos/ios/iosApp/` (Xcode project) exists, also: `grep -rn "kamper" demos/ios/iosApp/ --include="*.pbxproj" --include="*.xcconfig" --include="*.plist" || echo "NO_MATCHES"`

    Per 22-PATTERNS.md the build.gradle.kts is clean. Document any match in Xcode project files (these are post-Phase-21 candidates for stale refs).
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/ios/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All grep commands returned "NO_MATCHES" OR every match documented
    - Xcode project search performed if demos/ios/iosApp/ exists
  </acceptance_criteria>
  <done>Grep results captured.</done>
</task>

<task type="auto">
  <name>Task 2: Run ./gradlew :demos:ios:linkDebugExecutableIosSimulatorArm64 and locate the .kexe</name>
  <files>(builds binary, captures log)</files>
  <read_first>
    - demos/ios/build.gradle.kts
    - CLAUDE.md
  </read_first>
  <action>
    The simulator-arm64 target is the universal Apple Silicon target (works on M1/M2/M3 Macs):

    ```
    ./gradlew :demos:ios:linkDebugExecutableIosSimulatorArm64 --console=plain 2>&1 | tee /tmp/22-04-ios-link.log
    ```

    Then locate the binary:
    ```
    find demos/ios/build/bin/iosSimulatorArm64 -type f -name "*.kexe" 2>/dev/null
    ```

    Expected path: `demos/ios/build/bin/iosSimulatorArm64/debugExecutable/demos-ios.kexe` (filename derives from project name).

    If the user's machine is Intel-based (Mac x86_64), retry with `linkDebugExecutableIosX64` instead.
    Detect with: `uname -m` — `arm64` → use SimulatorArm64; `x86_64` → use IosX64.

    Do NOT attempt `xcodebuild` or `xcrun simctl` autonomously — defer launch to Task 3.
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-04-ios-link.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-04-ios-link.log contains BUILD SUCCESSFUL
    - At least one .kexe file produced under demos/ios/build/bin/iosSimulatorArm64/
    - The exact .kexe path is captured for Task 3
    - Architecture used (arm64 vs x64) recorded
  </acceptance_criteria>
  <done>iOS simulator binary linked; path captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User launches iOS simulator binary and observes 30 seconds</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Smoke Test Observation Guide row for iOS Simulator)
    - /tmp/22-04-ios-link.log
  </read_first>
  <what-built>
    Claude has linked the iOS simulator executable (.kexe). Per D-08 the user launches and observes — the binary may print metric values to stdout (no UIKit overlay on bare KMP demo per 22-PATTERNS.md).
  </what-built>
  <how-to-verify>
    On a Mac with Xcode installed, choose ONE option:

    **Option A — Direct execution** (fastest):
    ```
    # From repo root, replace path with what Task 2 captured:
    ./demos/ios/build/bin/iosSimulatorArm64/debugExecutable/demos-ios.kexe
    ```
    Direct execution may fail if the binary needs simulator runtime — if so, use Option B.

    **Option B — iOS Simulator runtime**:
    ```
    # List available simulators:
    xcrun simctl list devices | grep -E "iPhone.*Booted"   # check for already-booted sim
    # If none booted, boot one (replace UDID with one from `xcrun simctl list devices`):
    xcrun simctl boot <UDID>
    open -a Simulator
    # Run binary inside booted simulator:
    xcrun simctl spawn booted ./demos/ios/build/bin/iosSimulatorArm64/debugExecutable/demos-ios.kexe
    ```

    Observe for 30 seconds:
    1. Binary starts without immediate crash (no `Abort trap: 6` or `Bus error`)
    2. Stdout shows lines with metric values — typically `CpuInfo(usage=12.3%)`, `MemoryInfo(usedMb=...)`, etc.
    3. Values are NOT all `-1.0` / `INVALID`
    4. CPU and Memory values appear within ~2 seconds of start
    5. Binary either completes its 30s loop and exits cleanly, OR the user manually terminates it (Ctrl+C) without it hanging
    6. Phase 3 invariant (DEBT-01): on Ctrl+C, the shake-detection coroutine should exit cleanly — no zombie threads, no "Process didn't terminate" log

    **Reply format**:
    - `approved` — binary ran, stdout showed live non-INVALID metrics, clean exit
    - `failed: <description>` — describe (e.g. "failed: only INVALID printed", "failed: no stdout output")
    - `crashed: <description>` — paste signal/abort message
    - `no-mac` — host machine is not macOS / no Xcode; mark plan as BLOCKED
  </how-to-verify>
  <acceptance_criteria>
    - User reply is `approved`, `failed: <concrete>`, `crashed: <concrete>`, or `no-mac`
    - Failure descriptions name a specific stdout line, exit code, or signal
    - `no-mac` → SUMMARY records `outcome: BLOCKED`
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, `crashed: <description>`, or `no-mac`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 4: Diagnose iOS failure (only if Task 3 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-04-ios-link.log
    - demos/ios/src/iosMain/ (entry point — main function)
    - libs/modules/cpu/src/iosMain/ (CPU iOS actual)
    - libs/modules/memory/src/iosMain/ (Memory iOS actual)
    - libs/ui/kmm/src/iosMain/ (KamperUi iOS actual — for shake detection)
    - User's verbatim Task 3 reply
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-mac`.

    Classify:
    1. **Link failure**: missing iOS-specific dep, wrong target, or `:libs:ui:kmm` actual missing for iosMain.
    2. **Binary crash on launch**: signal (SIGABRT, SIGSEGV) — usually a missing `expect`/`actual` pair or a Darwin API call without try/catch.
    3. **All-INVALID output**: per CLAUDE.md D-06, the iOS watcher actual is throwing. CPU on iOS reads via `host_processor_info` / `mach_task_basic_info` — confirm the actual under `libs/modules/cpu/src/iosMain/` wraps in try/catch.
    4. **No output**: iosMain entry point does not call `Kamper.start()` or does not register stdout listeners.
    5. **Hang on Ctrl+C** (Phase 3 DEBT-01 regression): shake-detection coroutine using `while (true)` instead of `while (isActive)` — re-check `libs/ui/kmm/src/iosMain/` for the loop.

    Document classification + root cause + fix.
  </action>
  <verify>
    <automated>test -f /tmp/22-04-ios-link.log</automated>
  </verify>
  <acceptance_criteria>
    - One classification chosen, OR skipped
    - Root cause cites specific file:line
    - Fix is actionable
  </acceptance_criteria>
  <done>Failure classified or skipped.</done>
</task>

<task type="auto">
  <name>Task 5: Apply iOS fix and re-verify (only if Task 4 produced a fix)</name>
  <files>(varies — set by Task 4)</files>
  <read_first>
    - The file Task 4 named
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
    - CLAUDE.md (Safety Rule — D-06; Watcher loops use `while (isActive)`, never `while (true)`)
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-mac`.

    Apply the smallest fix:
    - Stale Gradle path: `:kamper:X` → `:libs:X`
    - Watcher throwing: wrap Darwin call in try/catch returning `-1.0` per D-06
    - `while (true)` → `while (isActive)` per Phase 3 DEBT-01
    - Missing actual: add platform actual mirroring iosArm64/iosSimulatorArm64 source set rules

    After edit:
    1. `./gradlew detekt` exits 0
    2. Re-run `./gradlew :demos:ios:linkDebugExecutableIosSimulatorArm64 --console=plain 2>&1 | tee /tmp/22-04-ios-link-retry.log`
    3. Re-prompt user via Task 3

    If still failing after one attempt, mark FAIL with residual cause.
  </action>
  <verify>
    <automated>./gradlew detekt --console=plain 2>&1 | tail -5; grep -c "BUILD SUCCESSFUL" /tmp/22-04-ios-link-retry.log 2>/dev/null || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Edited file matches Task 4 target
    - `./gradlew detekt` exits 0
    - Retry log contains BUILD SUCCESSFUL
    - User re-prompted, OR plan honestly FAIL
  </acceptance_criteria>
  <done>Fix applied AND retry passes; OR plan FAIL.</done>
</task>

<task type="auto">
  <name>Task 6: Write 22-04-SUMMARY.md with PASS/FAIL/BLOCKED outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-04-ios-link.log
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 04`, `platform: ios`, `outcome: PASS|FAIL|BLOCKED`, `architecture: <arm64|x64>`, `date: 2026-04-29`
    2. **Stale reference scan**: Task 1 grep output verbatim (including Xcode project search if performed)
    3. **Link outcome**: BUILD SUCCESSFUL + .kexe path + architecture used
    4. **Smoke test outcome**: PASS / FAIL / BLOCKED + user's Task 3 reply verbatim
    5. **Module health**: per-module status — CPU, FPS, Memory, Network, Issues, Jank, GC, Thermal — `live` / `INVALID` / `not observed`
    6. **Phase 3 DEBT-01 invariant**: confirmed (Ctrl+C clean exit) / regressed / not observed
    7. **Fixes applied**: file list, OR "none required"
    8. **Outcome rule**: PASS = BUILD SUCCESSFUL + `approved` + ≥CPU and Memory live + clean exit; FAIL = build/runtime fail after one fix; BLOCKED = `no-mac`
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md && grep -E "^outcome: (PASS|FAIL|BLOCKED)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Frontmatter contains exactly one of `outcome: PASS`, `FAIL`, or `BLOCKED`
    - User reply quoted verbatim
    - Module health table lists all 8 modules
    - Phase 3 DEBT-01 invariant status recorded
  </acceptance_criteria>
  <done>22-04-SUMMARY.md written with explicit outcome.</done>
</task>

</tasks>

<verification>
- Stale reference scan covers Gradle + Xcode project files
- linkDebugExecutableIosSimulatorArm64 produces a .kexe
- User runs binary, observes ≥30s
- 22-04-SUMMARY.md committed with explicit outcome
</verification>

<success_criteria>
- 22-04-SUMMARY.md with explicit outcome
- All 8 module statuses + Phase 3 DEBT-01 invariant status recorded
- Every fix is smallest viable edit
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md` with outcome, link log, .kexe path, user observation, module health, DEBT-01 invariant, fixes.
</output>

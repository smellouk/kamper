---
phase: 22
plan: 05
type: execute
wave: 5
depends_on: [22-04]
files_modified:
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/macos/ contains zero stale ':kamper:*' Gradle path references"
    - "./gradlew :demos:macos:linkDebugExecutableMacosArm64 (or X64) completes BUILD SUCCESSFUL"
    - "User executes the linked macOS binary and it runs ≥30 seconds without crashing"
    - "Stdout shows non-INVALID values for ≥CPU and Memory during 30s observation"
    - "Smoke test results recorded in 22-05-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md"
      provides: "macOS demo smoke test outcome"
      contains: "outcome:"
  key_links:
    - from: "demos/macos/build.gradle.kts"
      to: ":libs:engine, :libs:modules:*"
      via: "macosX64 + macosArm64 binary executable entryPoint"
      pattern: "implementation\\(project\\(\":libs:"
---

<objective>
Validate that the macOS demo (`demos/macos/`) builds via KMP `linkDebugExecutableMacos*` and passes a 30-second smoke test on the host Mac. Per D-06, this is the fifth demo. Per D-08, Claude links; user runs.

Purpose: Confirm the macOS-specific actuals for `:libs:modules:*` (Mach kernel APIs for CPU, `task_info` for Memory, IOKit for Thermal). Per 22-PATTERNS.md, the macOS demo does NOT depend on `:libs:ui:kmm` (no overlay), so smoke test signal comes from stdout only.

Output: A 22-05-SUMMARY.md recording link build, user binary observation, and per-module health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-04-SUMMARY.md
@CLAUDE.md
@demos/macos/build.gradle.kts

<interfaces>
<!-- macOS KMP binary contract -->

From demos/macos/build.gradle.kts (post-Phase-21):
```kotlin
kotlin {
    macosX64    { binaries { executable { entryPoint = "com.smellouk.kamper.macos.main" } } }
    macosArm64  { binaries { executable { entryPoint = "com.smellouk.kamper.macos.main" } } }
}

sourceSets {
    val commonMain by getting {
        dependencies {
            implementation(project(":libs:engine"))
            implementation(project(":libs:modules:cpu"))   // ... 8 modules
            // Note: NO :libs:ui:kmm dependency — macOS demo is stdout-only
        }
    }
}
```

Build output:
- macosArm64 → `demos/macos/build/bin/macosArm64/debugExecutable/demos-macos.kexe`
- macosX64   → `demos/macos/build/bin/macosX64/debugExecutable/demos-macos.kexe`

Choice depends on `uname -m`: arm64 → macosArm64; x86_64 → macosX64.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale ':kamper:' references in demos/macos/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/macos/build.gradle.kts
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
  </read_first>
  <action>
    Run and capture verbatim:

    1. `grep -rn ":kamper:" demos/macos/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/macos/src/ --include="*.kt" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/macos/ || echo "NO_MATCHES"`

    Per 22-PATTERNS.md, expect "NO_MATCHES" everywhere. Document any match.
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/macos/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All grep commands returned "NO_MATCHES" OR every match documented
    - Grep stdout captured verbatim
  </acceptance_criteria>
  <done>Grep results captured.</done>
</task>

<task type="auto">
  <name>Task 2: Detect host arch, run linkDebugExecutableMacos<Arch>, locate .kexe</name>
  <files>(builds binary, captures log)</files>
  <read_first>
    - demos/macos/build.gradle.kts
    - CLAUDE.md
  </read_first>
  <action>
    Detect host architecture and choose the matching task:

    ```
    ARCH=$(uname -m)
    if [ "$ARCH" = "arm64" ]; then
        TASK=":demos:macos:linkDebugExecutableMacosArm64"
        BIN_DIR="demos/macos/build/bin/macosArm64/debugExecutable"
    elif [ "$ARCH" = "x86_64" ]; then
        TASK=":demos:macos:linkDebugExecutableMacosX64"
        BIN_DIR="demos/macos/build/bin/macosX64/debugExecutable"
    else
        echo "Unsupported arch: $ARCH" && exit 1
    fi

    ./gradlew $TASK --console=plain 2>&1 | tee /tmp/22-05-macos-link.log

    find "$BIN_DIR" -type f -name "*.kexe" 2>/dev/null
    ```

    Capture:
    - The chosen task name and architecture
    - BUILD SUCCESSFUL line
    - The .kexe path

    Do NOT execute the .kexe here — that is Task 3 (user-driven).
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-05-macos-link.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-05-macos-link.log contains BUILD SUCCESSFUL
    - At least one .kexe file produced
    - Architecture and full .kexe path captured for Task 3
    - No `FAILED` lines before BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>macOS binary linked; path + architecture captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User executes macOS binary and observes 30 seconds of stdout</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (macOS row in Smoke Test Observation Guide)
    - /tmp/22-05-macos-link.log
  </read_first>
  <what-built>
    Claude has linked the macOS native executable. Per D-08 the user runs it directly.
  </what-built>
  <how-to-verify>
    From the repo root, in a NEW terminal:

    ```
    # Replace path with what Task 2 captured (architecture-dependent):
    ./demos/macos/build/bin/macosArm64/debugExecutable/demos-macos.kexe

    # OR for Intel Macs:
    ./demos/macos/build/bin/macosX64/debugExecutable/demos-macos.kexe
    ```

    Observe for 30 seconds:
    1. Binary starts without `Killed: 9`, `Bus error`, `Abort trap: 6`, or Gatekeeper rejection
    2. If macOS Gatekeeper blocks ("cannot be opened because the developer cannot be verified"), allow it via System Settings → Privacy & Security → "Allow Anyway", then re-run
    3. Stdout streams metric values — typically per-second prints like `CPU: 12.3%`, `Memory: 234 MB`, etc.
    4. Values are NOT all `-1.0` / `INVALID`
    5. CPU and Memory both produce live values within ~2 seconds of start
    6. At 30s, terminate with Ctrl+C — process exits cleanly (no hang, no zombie)

    **Reply format**:
    - `approved` — binary ran, stdout showed live non-INVALID metrics for 30s, clean exit
    - `failed: <description>` — describe (e.g. "failed: stdout shows CPU=-1.0 every second", "failed: Thermal: INVALID")
    - `crashed: <description>` — paste signal/abort message
    - `gatekeeper-blocked` — macOS refused to run unsigned binary even after Allow Anyway → describe and reply this
    - `no-mac` — host machine is not macOS; mark plan as BLOCKED
  </how-to-verify>
  <acceptance_criteria>
    - User reply is `approved`, `failed: <concrete>`, `crashed: <concrete>`, `gatekeeper-blocked`, or `no-mac`
    - Failure descriptions name a specific stdout line, exit signal, or Gatekeeper dialog
    - `no-mac` → SUMMARY records `outcome: BLOCKED`
    - `gatekeeper-blocked` → Task 4 includes "codesign --force --deep --sign -" workaround
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, `crashed: <description>`, `gatekeeper-blocked`, or `no-mac`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 4: Diagnose macOS failure (only if Task 3 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-05-macos-link.log
    - demos/macos/src/macosMain/ (entry point)
    - libs/modules/cpu/src/macosMain/ (or appleMain if shared with iOS)
    - libs/modules/memory/src/macosMain/
    - libs/modules/thermal/src/macosMain/ (Thermal is macOS-specific via IOKit)
    - User's verbatim Task 3 reply
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-mac`.

    Classify:
    1. **Link failure**: missing dep / wrong target. Most macOS failures here are shared with iOS (appleMain source set).
    2. **Gatekeeper-blocked**: ad-hoc sign workaround — `codesign --force --deep --sign - <kexe>` then retry; document in Task 5 fix.
    3. **Binary crash on start**: signal — usually missing `expect`/`actual` for macosMain, or Mach API call without try/catch.
    4. **All-INVALID**: per CLAUDE.md D-06 — watcher actual throwing. CPU on macOS reads via `host_processor_info`, Memory via `task_info`. Confirm try/catch wrap.
    5. **No stdout**: macosMain entry doesn't call `Kamper.start()` or doesn't register a stdout listener.
    6. **Thermal INVALID specifically**: IOKit access requires entitlements; on a development build this may always be INVALID. Document but don't block plan unless ALL modules INVALID.

    Document classification + root cause + fix.
  </action>
  <verify>
    <automated>test -f /tmp/22-05-macos-link.log</automated>
  </verify>
  <acceptance_criteria>
    - One classification chosen, OR skipped
    - Root cause cites specific file:line
    - Fix is actionable
  </acceptance_criteria>
  <done>Failure classified or skipped.</done>
</task>

<task type="auto">
  <name>Task 5: Apply macOS fix and re-verify (only if Task 4 produced a fix)</name>
  <files>(varies — set by Task 4)</files>
  <read_first>
    - The file Task 4 named
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
    - CLAUDE.md (Safety Rule — D-06)
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-mac`.

    Apply the smallest fix:
    - Stale Gradle path: `:kamper:X` → `:libs:X`
    - Watcher throwing: wrap Mach call in try/catch returning `-1.0` per D-06
    - Gatekeeper: `codesign --force --deep --sign - <kexe-path>` (no source edit needed; document in summary)
    - Missing actual: add platform actual under `libs/modules/<name>/src/macosMain/` (or `appleMain` if Apple-shared)

    After source edits:
    1. `./gradlew detekt` exits 0
    2. Re-run `./gradlew $TASK --console=plain 2>&1 | tee /tmp/22-05-macos-link-retry.log` with the same architecture-aware $TASK from Task 2
    3. Re-prompt user via Task 3

    If still failing after one attempt, mark FAIL.
  </action>
  <verify>
    <automated>./gradlew detekt --console=plain 2>&1 | tail -5; grep -c "BUILD SUCCESSFUL" /tmp/22-05-macos-link-retry.log 2>/dev/null || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Edited file matches Task 4 target (or codesign workaround applied for gatekeeper-blocked)
    - `./gradlew detekt` exits 0
    - Retry log contains BUILD SUCCESSFUL (if source edited)
    - User re-prompted, OR plan honestly FAIL
  </acceptance_criteria>
  <done>Fix applied AND retry passes; OR plan FAIL.</done>
</task>

<task type="auto">
  <name>Task 6: Write 22-05-SUMMARY.md with PASS/FAIL/BLOCKED outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-05-macos-link.log
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 05`, `platform: macos`, `outcome: PASS|FAIL|BLOCKED`, `architecture: <macosArm64|macosX64>`, `date: 2026-04-29`
    2. **Stale reference scan**: Task 1 grep output verbatim
    3. **Link outcome**: BUILD SUCCESSFUL + .kexe path + architecture
    4. **Smoke test outcome**: PASS / FAIL / BLOCKED + user's Task 3 reply verbatim
    5. **Module health**: per-module — CPU, FPS, Memory, Network, Issues, Jank, GC, Thermal — `live` / `INVALID` / `not observed`
    6. **Gatekeeper status**: passed / required-codesign / blocked
    7. **Fixes applied**: file list, OR "none required"
    8. **Outcome rule**: PASS = BUILD SUCCESSFUL + `approved` + ≥CPU and Memory live; FAIL = build/runtime fail after one fix; BLOCKED = `no-mac`. Note: Thermal INVALID alone (IOKit entitlement issue) does not fail the plan if other modules pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md && grep -E "^outcome: (PASS|FAIL|BLOCKED)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Frontmatter contains `outcome: PASS`, `FAIL`, or `BLOCKED`
    - User reply quoted verbatim
    - Module health table lists all 8 modules
    - Gatekeeper status recorded
  </acceptance_criteria>
  <done>22-05-SUMMARY.md written with explicit outcome.</done>
</task>

</tasks>

<verification>
- Stale reference scan complete
- linkDebugExecutableMacos<Arch> succeeds
- User runs binary, observes ≥30s
- 22-05-SUMMARY.md committed with explicit outcome
</verification>

<success_criteria>
- 22-05-SUMMARY.md with explicit outcome
- All 8 module statuses + Gatekeeper status recorded
- Every fix is smallest viable edit
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-05-SUMMARY.md` with outcome, link log, .kexe path, architecture, user observation, module health, Gatekeeper status, fixes.
</output>

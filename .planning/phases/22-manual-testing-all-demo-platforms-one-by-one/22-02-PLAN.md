---
phase: 22
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/android/ contains zero stale ':kamper:*' Gradle path references"
    - "./gradlew :demos:android:assembleDebug completes BUILD SUCCESSFUL"
    - "User installs the APK on a connected emulator/device and the app launches without crashing"
    - "addInfoListener callbacks deliver non-INVALID values for ≥CPU, Memory, FPS modules during 30s observation"
    - "KamperPanel overlay renders in debug build per ADR-001"
    - "Smoke test results recorded in 22-02-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md"
      provides: "Android demo smoke test outcome"
      contains: "outcome:"
  key_links:
    - from: "demos/android/build.gradle.kts"
      to: ":libs:engine, :libs:modules:*, :libs:ui:kmm"
      via: "implementation/debugImplementation(project(...)) declarations"
      pattern: "implementation\\(project\\(\":libs:"
---

<objective>
Validate that the Android demo (`demos/android/`) builds and passes a full 30-second on-device smoke test on the post-Phase-21 monorepo. Per CLAUDE.md WARNING, this plan REQUIRES a connected emulator or physical device — Claude builds the APK; the user installs and launches it.

Purpose: Confirm the Android-specific actuals for `:libs:modules:*` (e.g. `/proc/stat` reads, `ActivityManager` memory probes, Choreographer FPS) wire correctly through `:libs:engine` and that `KamperPanel` (debug-only via `:libs:ui:kmm`) renders the overlay per ADR-001.

Output: A 22-02-SUMMARY.md recording build status, user-installed APK observation, and per-module health (CPU/FPS/Memory/Network/Issues/Jank/GC/Thermal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md
@CLAUDE.md
@demos/android/build.gradle.kts
@demos/android/src/main/java/com/smellouk/kamper/android/MainActivity.kt

<interfaces>
<!-- Android-specific contracts the executor must verify -->

From CLAUDE.md WARNING:
> `./gradlew connectedAndroidTest` requires a connected Android device or running emulator.
> NEVER run autonomously. The Android demo smoke test requires the user to manually install
> the APK and observe.

From demos/android/build.gradle.kts (lines 41-57, post-Phase-21):
```kotlin
implementation(project(":libs:engine"))
implementation(project(":libs:modules:cpu"))
// ... full module set
debugImplementation(project(":libs:ui:kmm"))   // KamperPanel debug overlay
```

From ADR-001 (manual install pattern): The demo's MainActivity calls Kamper.install(...)
explicitly per module, then Kamper.start(). KamperPanel overlay only renders in debug builds
because :libs:ui:kmm is a debugImplementation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale ':kamper:' path references in demos/android/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/android/build.gradle.kts
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Stale Reference Findings section)
  </read_first>
  <action>
    Run the following grep commands and capture output verbatim:

    1. `grep -rn ":kamper:" demos/android/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/android/src/ --include="*.kt" --include="*.java" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/android/ || echo "NO_MATCHES"`

    Per 22-PATTERNS.md the file is already clean — expect "NO_MATCHES" on all three. Document any real match in task output before proceeding.
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/android/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All three grep commands returned "NO_MATCHES" (exit 1 / no output) OR every match documented in task output
    - Grep stdout captured exactly in task summary
  </acceptance_criteria>
  <done>Grep results captured.</done>
</task>

<task type="auto">
  <name>Task 2: Run ./gradlew :demos:android:assembleDebug and locate the APK</name>
  <files>(builds APK, captures log)</files>
  <read_first>
    - demos/android/build.gradle.kts
    - CLAUDE.md (Build & Test section, especially the WARNING about connectedAndroidTest)
  </read_first>
  <action>
    Execute the assemble task (NOT install/test — those need a device):

    ```
    ./gradlew :demos:android:assembleDebug --console=plain 2>&1 | tee /tmp/22-02-android-build.log
    ```

    Then locate the produced APK:
    ```
    find demos/android/build/outputs/apk -name "*.apk" -type f 2>/dev/null
    ```

    Capture the exact APK path. Expected location: `demos/android/build/outputs/apk/debug/android-debug.apk` (filename may vary per AGP version).

    Do NOT attempt `./gradlew :demos:android:installDebug` autonomously — per CLAUDE.md WARNING it requires a device. Defer install to Task 3 where the user runs it interactively.
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-02-android-build.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-02-android-build.log contains `BUILD SUCCESSFUL`
    - At least one .apk file found under demos/android/build/outputs/apk/debug/
    - No `FAILED` lines before BUILD SUCCESSFUL in the log
    - Zero invocations of `connectedAndroidTest`, `installDebug`, or `runDebug`
  </acceptance_criteria>
  <done>APK assembled; path captured for Task 3 instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User installs APK and observes 30-second smoke test on device/emulator</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Smoke Test Observation Guide section, "INVALID Sentinel Guard Pattern")
    - /tmp/22-02-android-build.log (from Task 2)
    - demos/android/src/main/java/com/smellouk/kamper/android/MainActivity.kt
  </read_first>
  <what-built>
    Claude has built the debug APK. Per CLAUDE.md WARNING, Claude cannot run `installDebug` or `connectedAndroidTest` autonomously — the user must install and observe manually.
  </what-built>
  <how-to-verify>
    From the repo root, with a connected emulator OR physical Android device (USB debugging enabled), run:

    ```
    adb devices            # confirm device shows "device" not "unauthorized" / "offline"
    ./gradlew :demos:android:installDebug
    adb shell am start -n com.smellouk.kamper.android/.MainActivity
    ```

    Then observe for 30 seconds:
    1. App launches without an immediate crash dialog ("App keeps stopping" = FAIL)
    2. Main UI shows metric tiles for at least: CPU, FPS, Memory, Network
    3. Each tile shows numeric values updating roughly every second — NOT all stuck on `-1.0` or `INVALID`
    4. Per `MainActivity.kt`: a non-INVALID `CpuInfo` should appear within ~2s of launch (the listener guards against INVALID)
    5. **KamperPanel overlay** (debug-only, per `debugImplementation(project(":libs:ui:kmm"))`) appears as a draggable chip/panel — confirm it renders. If not visible, check that the overlay permission is granted for the app (Settings → Apps → Kamper Demo → Display over other apps).
    6. Tap each tile / scroll the UI — no crashes, no ANRs ("App not responding" dialog)
    7. Optionally tail logcat in another terminal for crash signal:
       ```
       adb logcat -s AndroidRuntime:E *:S | head -50
       ```
       — if any FATAL EXCEPTION line appears for `com.smellouk.kamper.android` package, the smoke test FAILS.

    **Reply format**:
    - `approved` — APK installed, app ran ≥30s, all 4+ tiles showed live non-INVALID values, KamperPanel overlay visible, no crashes
    - `failed: <description>` — describe specifically what was wrong (e.g. "failed: CPU tile stuck at -1.0", "failed: KamperPanel did not render")
    - `crashed: <description>` — paste the FATAL EXCEPTION class + first stack frame
    - `no-device` — no device/emulator available; mark plan as BLOCKED in summary

    Confirm `adb devices` shows a device before starting. If no device, reply `no-device` so the plan completes honestly as BLOCKED rather than fabricating a PASS.
  </how-to-verify>
  <acceptance_criteria>
    - User reply is `approved`, `failed: <concrete>`, `crashed: <concrete>`, or `no-device`
    - If `failed:` or `crashed:`, the description names a specific tile, value, or exception (no vague "didn't work")
    - If `no-device`, plan SUMMARY records `outcome: BLOCKED` (not PASS, not FAIL)
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, `crashed: <description>`, or `no-device`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 4: Diagnose Android failure (only if Task 3 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-02-android-build.log
    - demos/android/src/main/java/com/smellouk/kamper/android/MainActivity.kt
    - demos/android/src/main/AndroidManifest.xml
    - libs/ui/kmm/src/androidMain/ (KamperPanel/KamperUi sources)
    - User's verbatim reply from Task 3
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-device`.

    Classify the failure:

    1. **Build failure** before APK produced: `error:` lines in build.log; usually a missing `:libs:*` path or AGP-incompatible API.
    2. **Install failure**: `INSTALL_FAILED_*` from `adb install`; usually min-SDK mismatch or duplicate APK.
    3. **App crash on launch** (`FATAL EXCEPTION`): match the throwing class to the stack frame the user pasted. Common cases:
       - `ClassNotFoundException` for KamperPanel/KamperUi → `:libs:ui:kmm` actual missing for androidMain
       - `RuntimeException` from CrashDetector → Phase 3 fix regression
       - `SecurityException` from overlay → manifest missing `SYSTEM_ALERT_WINDOW`
    4. **INVALID-only metrics**: per CLAUDE.md D-06, the watcher's `getValue()` is throwing internally. Identify which module by the pattern: CPU INVALID = `/proc/stat` read failure; Memory INVALID = `ActivityManager` access failure; FPS INVALID = Choreographer not registered.
    5. **No KamperPanel overlay**: check `debugImplementation(project(":libs:ui:kmm"))` is present in build.gradle.kts; check `KamperUi.attach(activity)` is called in MainActivity; check `SYSTEM_ALERT_WINDOW` permission was granted at install.

    Document classification + root cause (with file:line) + proposed fix in task output.
  </action>
  <verify>
    <automated>test -f /tmp/22-02-android-build.log</automated>
  </verify>
  <acceptance_criteria>
    - One classification chosen, OR task skipped (Task 3 approved or no-device)
    - Root cause cites a specific file:line OR a specific exception class from the user's reply
    - Proposed fix names the exact file to edit and the exact change required
  </acceptance_criteria>
  <done>Failure classified; OR task skipped.</done>
</task>

<task type="auto">
  <name>Task 5: Apply Android fix and re-verify (only if Task 4 produced a fix)</name>
  <files>(varies — set by Task 4)</files>
  <read_first>
    - The file Task 4 named as needing the fix (read it before editing)
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (canonical post-Phase-21 patterns)
    - CLAUDE.md (Safety Rule — D-06: try/catch wrap platform calls; never propagate)
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-device`.

    Apply the smallest fix Task 4 specified:
    - Stale Gradle path: change `project(":kamper:X")` → `project(":libs:X")`
    - Missing manifest permission: add `<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>` to `demos/android/src/main/AndroidManifest.xml`
    - Watcher throwing: wrap the platform call in try/catch returning `-1.0` per CLAUDE.md "Safety Rule (Hard — D-06)"
    - Missing `:libs:ui:kmm` actual: add the actual implementation under `libs/ui/kmm/src/androidMain/`

    Run `./gradlew detekt` after the edit — must exit 0 (zero issues per CLAUDE.md).

    Re-run `./gradlew :demos:android:assembleDebug --console=plain 2>&1 | tee /tmp/22-02-android-build-retry.log` and request user re-installs and observes via Task 3 protocol.

    If still failing after one fix attempt, mark plan FAIL with documented residual failure — do not silently abandon and do not loop indefinitely.
  </action>
  <verify>
    <automated>./gradlew detekt --console=plain 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Edited file is exactly Task 4's named target
    - `./gradlew detekt` exits 0 after the edit
    - `/tmp/22-02-android-build-retry.log` contains BUILD SUCCESSFUL
    - User has been re-prompted to observe, OR plan is explicitly marked FAIL with residual failure documented
  </acceptance_criteria>
  <done>Fix applied AND retry passes user observation; OR plan honestly FAIL with residual cause.</done>
</task>

<task type="auto">
  <name>Task 6: Write 22-02-SUMMARY.md with PASS/FAIL/BLOCKED outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-02-android-build.log
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 02`, `platform: android`, `outcome: PASS|FAIL|BLOCKED`, `date: 2026-04-29`
    2. **Stale reference scan**: Task 1 grep output verbatim or "NO_MATCHES on all 3 patterns"
    3. **Build outcome**: BUILD SUCCESSFUL/FAILED + APK path produced
    4. **Smoke test outcome**: PASS / FAIL / BLOCKED with the user's Task 3 reply verbatim
    5. **Module health**: per-module status — CPU, FPS, Memory, Network, Issues, Jank, GC, Thermal — each marked `live` / `INVALID` / `not observed`
    6. **KamperPanel overlay**: rendered / not rendered / not observed
    7. **Fixes applied**: every file modified with one-line rationale, OR "none required"
    8. **Outcome rule**:
       - PASS: BUILD SUCCESSFUL + user `approved` + ≥3 modules live + KamperPanel rendered
       - FAIL: build or runtime failure even after one fix attempt
       - BLOCKED: user replied `no-device` (no device available — not a Kamper failure)

    Use $HOME/.claude/get-shit-done/templates/summary.md as the structural skeleton.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md && grep -E "^outcome: (PASS|FAIL|BLOCKED)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path
    - Frontmatter contains exactly one of `outcome: PASS`, `outcome: FAIL`, or `outcome: BLOCKED`
    - User's verbatim Task 3 reply appears in the Smoke test outcome section
    - Module health table lists all 8 modules
    - KamperPanel overlay status recorded
  </acceptance_criteria>
  <done>22-02-SUMMARY.md written with explicit outcome and per-module health.</done>
</task>

</tasks>

<verification>
- Stale reference scan complete (zero matches expected)
- `./gradlew :demos:android:assembleDebug` exits BUILD SUCCESSFUL
- User installs APK on device, observes ≥30s, replies via Task 3 protocol
- 22-02-SUMMARY.md committed with PASS / FAIL / BLOCKED outcome
- Zero `connectedAndroidTest` invocations attempted
</verification>

<success_criteria>
- 22-02-SUMMARY.md exists with explicit outcome
- All 8 module statuses recorded
- KamperPanel overlay status recorded
- Every fix applied is the smallest viable edit and matches canonical patterns
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md` with PASS/FAIL/BLOCKED outcome, build log, user observation verbatim, module health table, KamperPanel overlay status, and applied fixes.
</output>

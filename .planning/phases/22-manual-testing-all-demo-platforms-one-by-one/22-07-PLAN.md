---
phase: 22
plan: 07
type: execute
wave: 7
depends_on: [22-06]
files_modified:
  - demos/react-native/metro.config.js
  - demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/react-native/metro.config.js line 17 references '..', '..', 'libs', 'ui', 'rn' (NOT 'kamper')"
    - "demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt has no broken imports — compiles cleanly"
    - "demos/react-native/ contains zero stale ':kamper:*' or kamper/ path references after fixes"
    - "npx react-native run-android (or equivalent) builds the RN demo"
    - "User installs the RN demo APK on emulator/device, the JS bridge initialises, and KamperModule appears in Metro logs"
    - "Smoke test results recorded in 22-07-SUMMARY.md"
  artifacts:
    - path: "demos/react-native/metro.config.js"
      provides: "Metro bundler config with corrected libs/ui/rn path"
      contains: "'libs', 'ui', 'rn'"
    - path: "demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt"
      provides: "RN entry point without broken import"
      contains: "KamperTurboPackage"
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md"
      provides: "React Native demo smoke test outcome"
      contains: "outcome:"
  key_links:
    - from: "demos/react-native/metro.config.js"
      to: "libs/ui/rn (the react-native-kamper JS source)"
      via: "watchFolders path.resolve"
      pattern: "'libs', 'ui', 'rn'"
    - from: "demos/react-native/android/settings.gradle"
      to: ":libs:engine, :libs:modules:*"
      via: "dependencySubstitution module->project mapping"
      pattern: "project\\(':libs:"
---

<objective>
Validate that the React Native demo (`demos/react-native/`) builds and passes a 30-second smoke test on the post-Phase-21 monorepo. This is the LAST and most complex demo (composite Gradle build + Metro bundler + RN autolinking + JS bridge), and 22-PATTERNS.md identified TWO known issues that must be fixed first:

1. **`demos/react-native/metro.config.js` line 17**: stale path `'..', '..', 'kamper', 'ui', 'rn'` must become `'..', '..', 'libs', 'ui', 'rn'`
2. **`demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt` (uncommitted in git status)**: has a broken import `import com.smellouk.kamper.rn` (truncated package import — invalid Kotlin) that must be removed; the existing `KamperTurboPackage` reference is correct.

Per D-08, Claude applies the fixes and runs builds; the user runs the RN demo on a device/emulator and reports observations.

Output: A 22-07-SUMMARY.md recording the two pre-fixes, build status, user RN observation, per-module health, and the final native bridge state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-06-SUMMARY.md
@CLAUDE.md
@demos/react-native/metro.config.js
@demos/react-native/package.json
@demos/react-native/android/settings.gradle
@demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt

<interfaces>
<!-- React Native composite build + autolinking -->

From demos/react-native/package.json (line 18 — already correct):
```json
"react-native-kamper": "file:../../libs/ui/rn"
```

From demos/react-native/metro.config.js (line 17 — STALE, must fix):
```js
// CURRENT (broken — path doesn't exist post-Phase-21):
watchFolders: [
    path.resolve(__dirname, '..', '..', 'kamper', 'ui', 'rn'),
],
// REQUIRED:
watchFolders: [
    path.resolve(__dirname, '..', '..', 'libs', 'ui', 'rn'),
],
```

From demos/react-native/android/settings.gradle (lines 29-42, already correct):
```groovy
includeBuild('../../..') {
    dependencySubstitution {
        substitute module('com.smellouk.kamper:engine') using project(':libs:engine')
        // ... full module set, all using :libs:*
    }
}
```

From git diff of MainApplication.kt (UNCOMMITTED — broken):
```kotlin
// CURRENT (broken — line +9 in diff):
import com.smellouk.kamper.rn       // Truncated package import — INVALID Kotlin

// REQUIRED: remove this line entirely
// The (existing) explicit registration call is fine:
add(KamperTurboPackage())
```

The broken `import com.smellouk.kamper.rn` line (without an identifier after the package)
will fail Kotlin compilation. It must be deleted before the Android RN build can succeed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix demos/react-native/metro.config.js line 17 (kamper → libs)</name>
  <files>demos/react-native/metro.config.js</files>
  <read_first>
    - demos/react-native/metro.config.js (current state)
    - demos/react-native/package.json (line 18 — confirms libs/ui/rn is correct)
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Issue 1 — CRITICAL)
  </read_first>
  <action>
    Edit `demos/react-native/metro.config.js`:

    Replace the line:
    ```
        path.resolve(__dirname, '..', '..', 'kamper', 'ui', 'rn'),
    ```
    with:
    ```
        path.resolve(__dirname, '..', '..', 'libs', 'ui', 'rn'),
    ```

    Also update the doc comment on line 8 that still references `kamper/ui/rn/`:
    ```
     * watchFolders: Metro must watch `kamper/ui/rn/` because it lives outside
    ```
    becomes:
    ```
     * watchFolders: Metro must watch `libs/ui/rn/` because it lives outside
    ```

    Do NOT touch any other line — preserve the resolver/nodeModulesPaths block exactly.

    Verify the path resolves to a real directory:
    ```
    ls -d libs/ui/rn 2>&1
    ```
    Must print `libs/ui/rn` (i.e., the directory exists). If it does not, this is a deeper bug — STOP and surface it before proceeding.
  </action>
  <verify>
    <automated>grep -c "'libs', 'ui', 'rn'" demos/react-native/metro.config.js; grep -c "'kamper', 'ui', 'rn'" demos/react-native/metro.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'libs', 'ui', 'rn'" demos/react-native/metro.config.js` returns ≥ 1
    - `grep -c "'kamper', 'ui', 'rn'" demos/react-native/metro.config.js` returns 0
    - `ls -d libs/ui/rn` exits 0 (target directory exists)
    - The doc comment update is consistent with the code change (no comment-vs-code drift)
  </acceptance_criteria>
  <done>metro.config.js watches the correct post-Phase-21 path; doc comment matches code.</done>
</task>

<task type="auto">
  <name>Task 2: Fix MainApplication.kt — remove broken truncated import</name>
  <files>demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt</files>
  <read_first>
    - demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt (current state, including the uncommitted edit)
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Issue 2 — modified file in git status)
  </read_first>
  <action>
    First inspect the current state of the file:
    ```
    git diff demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt
    ```

    The diff shows a broken import was added on what is line 9 in the working tree:
    ```kotlin
    import com.smellouk.kamper.rn
    ```
    This is invalid Kotlin — a package-only `import` with no identifier suffix. It must be deleted.

    Edit the file to remove ONLY that line. Do NOT revert the other change (`com.smellouk.kamper.rn.KamperTurboPackage()` → `KamperTurboPackage()`); that simplification is fine BECAUSE the file's package declaration is already `package com.smellouk.kamper.rn` so `KamperTurboPackage` resolves locally without import.

    Confirm the file is package `com.smellouk.kamper.rn`:
    ```
    head -3 demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt
    ```

    After the edit, the file's import block should not contain `import com.smellouk.kamper.rn` (without a trailing identifier). All remaining imports must end with an identifier (e.g. `.ReactApplication`, `.ReactHost`, etc.).
  </action>
  <verify>
    <automated>grep -c "^import com\.smellouk\.kamper\.rn$" demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt; grep -c "KamperTurboPackage" demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import com\.smellouk\.kamper\.rn$" demos/react-native/.../MainApplication.kt` returns 0 (no truncated import)
    - `grep -c "KamperTurboPackage" demos/react-native/.../MainApplication.kt` returns ≥ 1 (existing registration preserved)
    - File first line is `package com.smellouk.kamper.rn` (verified via `head -3`)
    - All import lines end with an identifier (no bare package imports)
  </acceptance_criteria>
  <done>MainApplication.kt compiles cleanly; broken import removed; KamperTurboPackage registration preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Grep for residual ':kamper:' / 'kamper/' references in demos/react-native/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/react-native/android/settings.gradle (already correct per 22-PATTERNS.md)
    - demos/react-native/android/app/build.gradle
    - demos/react-native/metro.config.js (after Task 1 fix)
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Stale Reference Findings + known package-lock.json reference)
  </read_first>
  <action>
    Run and capture verbatim:

    1. `grep -rn ":kamper:" demos/react-native/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" --include="*.groovy" 2>/dev/null | grep -v node_modules || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/react-native/android/app/src/ --include="*.kt" --include="*.java" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/react-native/ 2>/dev/null | grep -v node_modules || echo "NO_MATCHES"`
    4. `grep -rn "'kamper'" demos/react-native/ --include="*.js" --include="*.json" --include="*.ts" 2>/dev/null | grep -v node_modules || echo "NO_MATCHES"`

    NOTE: 22-PATTERNS.md identified `demos/react-native/package-lock.json` line 43 contains `"../../kamper/react-native":`. This is a npm-generated lockfile artifact — DO NOT hand-edit it. Document it in the SUMMARY but do not block on it; it will be regenerated next time `npm install` runs. If it surfaces in (1) or (3), note it as "documented exception (will regenerate via npm install)".

    Document any other match.
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/react-native/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" --include="*.groovy" 2>/dev/null | grep -v node_modules; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All four grep commands returned "NO_MATCHES" OR the only match is `package-lock.json` (documented as expected)
    - Any other match documented in task output with proposed fix
  </acceptance_criteria>
  <done>Grep results captured; only the documented package-lock.json artifact may remain.</done>
</task>

<task type="auto">
  <name>Task 4: Run RN Android Gradle build (composite include via includeBuild)</name>
  <files>(builds, captures log)</files>
  <read_first>
    - demos/react-native/android/settings.gradle
    - demos/react-native/android/app/build.gradle
    - CLAUDE.md (Build & Test — composite build / RN section)
  </read_first>
  <action>
    The RN Android project is its own composite Gradle build (`includeBuild('../../..')`). Build it from inside the RN android directory:

    ```
    cd demos/react-native/android
    ./gradlew :app:assembleDebug --console=plain 2>&1 | tee /tmp/22-07-rn-android-build.log
    cd -
    ```

    If `./gradlew` is missing in `demos/react-native/android/`, fall back to the root wrapper from the repo root:
    ```
    ./gradlew -p demos/react-native/android :app:assembleDebug --console=plain 2>&1 | tee /tmp/22-07-rn-android-build.log
    ```

    Confirm BUILD SUCCESSFUL and locate the APK:
    ```
    find demos/react-native/android/app/build/outputs/apk -name "*.apk" -type f 2>/dev/null
    ```

    Do NOT attempt `installDebug` or `connectedAndroidTest` autonomously (per CLAUDE.md WARNING). Defer install + Metro to Task 5.

    Codegen note: `demos/react-native/package.json` has a `postinstall` script that runs codegen. If the build fails with "no codegen artifacts", the user should run `npm install` (or `yarn install`) inside `demos/react-native/` first; document this in Task 5 instructions.
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-07-rn-android-build.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-07-rn-android-build.log contains BUILD SUCCESSFUL
    - At least one APK produced under demos/react-native/android/app/build/outputs/apk/debug/
    - No `FAILED` lines before BUILD SUCCESSFUL
    - Zero invocations of `installDebug` or `connectedAndroidTest`
  </acceptance_criteria>
  <done>RN Android composite build succeeds; APK located.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User starts Metro, installs APK, observes RN bridge for 30 seconds</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (React Native row in Smoke Test Observation Guide)
    - /tmp/22-07-rn-android-build.log
    - demos/react-native/package.json (scripts section)
  </read_first>
  <what-built>
    Claude has fixed the metro.config.js stale path AND removed the broken MainApplication.kt import, then assembled the RN Android debug APK via the composite Gradle build. The user must now: (1) install npm deps if needed, (2) start Metro, (3) install + launch the RN app on a device/emulator, (4) observe Metro logs and the app for 30 seconds.
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Connected Android emulator OR physical device (`adb devices` shows a device, status `device`)
    - Node.js + npm/yarn installed
    - JDK 17 (already required by project)

    From the repo root:

    **Step 1 — Install deps (if not already done)**:
    ```
    cd demos/react-native
    npm install        # OR: yarn install
    # The postinstall hook will run codegen automatically.
    cd -
    ```

    **Step 2 — Start Metro in one terminal** (keep this running for the 30-second observation):
    ```
    cd demos/react-native
    npm start          # equivalent to: npx react-native start
    ```
    Metro should print `Welcome to Metro` and a URL like `http://localhost:8081`.
    Watch for any red errors in Metro's output — if Metro errors out at startup with `Cannot find module 'react-native-kamper'`, the metro.config.js fix Task 1 applied did not take effect; reply `failed: metro cannot find react-native-kamper`.

    **Step 3 — Install + launch RN app in a SECOND terminal**:
    ```
    cd demos/react-native
    npx react-native run-android
    ```
    This installs the APK Task 4 built and starts the app on the connected device.

    **Step 4 — Observe for 30 seconds**:
    1. The RN app launches without an immediate red box overlay (`Cannot find module` / `Bridge call failed`)
    2. Metro logs (terminal 1) show `KamperModule` related output — typically a registration log, then per-second metric prints
    3. The RN app's main screen renders (no white-screen freeze, no infinite spinner)
    4. If the demo wires `Kamper.addInfoListener` from JS, the screen shows live values; if it logs to Metro, look for non-INVALID values
    5. KamperPanel native overlay (per Phase 14/15 RN bridge) appears as in the regular Android demo — confirm it renders OR confirm Metro shows the bridge accepting `attach` calls
    6. No FATAL EXCEPTION in `adb logcat -s ReactNativeJS:* AndroidRuntime:E *:S | head -50`

    **Reply format**:
    - `approved` — Metro started, app installed + launched, KamperModule visible in logs, no red-box for 30s, ≥CPU and Memory show live non-INVALID values
    - `failed: <description>` — describe (e.g. "failed: red-box says Cannot find module 'react-native-kamper'", "failed: KamperModule registered but all values are INVALID")
    - `crashed: <description>` — paste FATAL EXCEPTION stack frame OR red-box error message
    - `no-device` — no Android device/emulator available; mark plan as BLOCKED
    - `no-node` — no Node.js / npm available; mark plan as BLOCKED
  </how-to-verify>
  <acceptance_criteria>
    - User reply is `approved`, `failed: <concrete>`, `crashed: <concrete>`, `no-device`, or `no-node`
    - Failure descriptions reference Metro output, red-box message, or logcat exception
    - `no-device` or `no-node` → SUMMARY records `outcome: BLOCKED`
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, `crashed: <description>`, `no-device`, or `no-node`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 6: Diagnose RN failure (only if Task 5 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-07-rn-android-build.log
    - demos/react-native/metro.config.js (post-Task-1)
    - demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt (post-Task-2)
    - demos/react-native/android/settings.gradle
    - libs/ui/rn/ (the react-native-kamper JS source — confirm it's a valid npm package)
    - User's verbatim Task 5 reply
  </read_first>
  <action>
    SKIP if Task 5 returned `approved`, `no-device`, or `no-node`.

    Classify:
    1. **Metro can't find react-native-kamper**: re-check Task 1 fix landed and `libs/ui/rn/package.json` exists; if the `package.json` is missing or broken, the issue is upstream of this plan (Phase 14 regression).
    2. **Build failed before APK** (despite Task 4 BUILD SUCCESSFUL — i.e. user re-ran and got different result): codegen artifacts stale; document `cd demos/react-native && npm install` as the fix.
    3. **Red-box `Cannot find native module 'KamperModule'`**: TurboPackage registration broken; re-inspect MainApplication.kt for the `add(KamperTurboPackage())` line — Task 2 should have preserved it.
    4. **Red-box `KamperTurboPackage` reference unresolved**: missing import; the file's `package com.smellouk.kamper.rn` declaration should resolve `KamperTurboPackage` from the same package — verify the class exists at `demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/KamperTurboPackage.kt`.
    5. **All-INVALID metrics**: same diagnosis as Plan 22-02 (Android-side watcher actuals throwing); refer back to 22-02-SUMMARY.md.
    6. **App crashes immediately on launch**: paste the user's FATAL EXCEPTION class — usually a JNI / native lib loading issue. Check `react-native run-android` log for `couldNotLoadLibrary`.

    Document classification + root cause + fix.
  </action>
  <verify>
    <automated>test -f /tmp/22-07-rn-android-build.log && test -f demos/react-native/metro.config.js</automated>
  </verify>
  <acceptance_criteria>
    - One classification chosen, OR skipped
    - Root cause cites file:line, Metro log line, OR red-box message
    - Fix is actionable
  </acceptance_criteria>
  <done>Failure classified or skipped.</done>
</task>

<task type="auto">
  <name>Task 7: Apply RN fix and re-verify (only if Task 6 produced a fix)</name>
  <files>(varies — set by Task 6)</files>
  <read_first>
    - The file Task 6 named
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
    - CLAUDE.md (Safety Rule — D-06)
  </read_first>
  <action>
    SKIP if Task 5 returned `approved`, `no-device`, or `no-node`.

    Apply the smallest fix Task 6 specified.

    After edit:
    1. `./gradlew detekt` exits 0 (run from repo root)
    2. Re-run RN Android build:
       ```
       ./gradlew -p demos/react-native/android :app:assembleDebug --console=plain 2>&1 | tee /tmp/22-07-rn-android-build-retry.log
       ```
    3. Re-prompt user via Task 5 protocol (user must restart Metro and re-run `npx react-native run-android`)

    If still failing after one attempt, mark FAIL with residual cause documented.
  </action>
  <verify>
    <automated>./gradlew detekt --console=plain 2>&1 | tail -5; grep -c "BUILD SUCCESSFUL" /tmp/22-07-rn-android-build-retry.log 2>/dev/null || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Edited file matches Task 6 target
    - `./gradlew detekt` exits 0
    - Retry log contains BUILD SUCCESSFUL
    - User re-prompted, OR plan honestly FAIL
  </acceptance_criteria>
  <done>Fix applied AND retry passes; OR plan FAIL.</done>
</task>

<task type="auto">
  <name>Task 8: Write 22-07-SUMMARY.md with PASS/FAIL/BLOCKED outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-07-rn-android-build.log
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 07`, `platform: react-native-android`, `outcome: PASS|FAIL|BLOCKED`, `date: 2026-04-29`
    2. **Pre-fixes applied** (always, regardless of outcome):
       - `demos/react-native/metro.config.js` — kamper → libs (line 17 + doc comment line 8)
       - `demos/react-native/android/app/src/main/java/com/smellouk/kamper/rn/MainApplication.kt` — removed broken truncated import
    3. **Stale reference scan**: Task 3 grep output verbatim, including the documented `package-lock.json` exception
    4. **Build outcome**: BUILD SUCCESSFUL + APK path + composite build invocation method (`./gradlew -p demos/react-native/android` or `cd && ./gradlew`)
    5. **Smoke test outcome**: PASS / FAIL / BLOCKED + user's Task 5 reply verbatim
    6. **Module health**: per-module — CPU, FPS, Memory, Network, Issues, Jank, GC, Thermal — `live` / `INVALID` / `not observed`
    7. **Bridge state**: `KamperModule registered` / `red-box` / `not observed`
    8. **KamperPanel native overlay (RN)**: rendered / not rendered / not observed
    9. **Additional fixes applied** (beyond the two pre-fixes): file list, OR "none required"
    10. **Outcome rule**: PASS = both pre-fixes applied + BUILD SUCCESSFUL + `approved` + KamperModule registered + ≥CPU and Memory live; FAIL = build/runtime fail after one fix; BLOCKED = `no-device` or `no-node`.

    Use $HOME/.claude/get-shit-done/templates/summary.md as the structural skeleton.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md && grep -E "^outcome: (PASS|FAIL|BLOCKED)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md && grep -c "metro.config.js" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md && grep -c "MainApplication.kt" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Frontmatter contains `outcome: PASS`, `FAIL`, or `BLOCKED`
    - Both pre-fixes (metro.config.js + MainApplication.kt) explicitly listed in "Pre-fixes applied" section
    - User reply quoted verbatim
    - Module health table lists all 8 modules
    - Bridge state recorded
    - KamperPanel RN overlay status recorded
  </acceptance_criteria>
  <done>22-07-SUMMARY.md written with both pre-fixes documented and explicit outcome.</done>
</task>

</tasks>

<verification>
- metro.config.js fixed (kamper → libs)
- MainApplication.kt broken import removed
- Stale reference scan complete (residual package-lock.json acknowledged)
- Composite RN Android build succeeds
- User runs Metro + installs APK + observes ≥30s
- 22-07-SUMMARY.md committed with both pre-fixes documented and explicit outcome
- Zero `connectedAndroidTest` invocations
</verification>

<success_criteria>
- 22-07-SUMMARY.md with explicit outcome
- Both pre-fixes listed and verified by grep
- All 8 module statuses + bridge state + KamperPanel status recorded
- Every additional fix is smallest viable edit
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-07-SUMMARY.md` with both pre-fixes documented, build log, user observation, module health, bridge state, KamperPanel RN overlay status, and any additional fixes.
</output>

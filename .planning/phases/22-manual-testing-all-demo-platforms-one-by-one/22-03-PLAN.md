---
phase: 22
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/compose/ contains zero stale ':kamper:*' Gradle path references"
    - "./gradlew :demos:compose:run completes BUILD SUCCESSFUL and launches a desktop window"
    - "Compose desktop demo shows live non-INVALID values for ≥CPU, Memory modules during 30s observation"
    - "Smoke test results recorded in 22-03-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md"
      provides: "Compose Multiplatform desktop demo smoke test outcome"
      contains: "outcome:"
  key_links:
    - from: "demos/compose/build.gradle.kts"
      to: ":libs:engine, :libs:modules:*"
      via: "commonMain + desktopMain source set dependencies"
      pattern: "implementation\\(project\\(\":libs:"
---

<objective>
Validate that the Compose Multiplatform desktop demo (`demos/compose/`) builds and passes a 30-second smoke test on the post-Phase-21 monorepo. Per D-06, this is the third demo tested (after JVM and Android).

Purpose: Confirm the JVM target actuals (used by the desktop variant of CMP) install correctly through `:libs:engine` and that Compose recomposes metric sections per Phase 7 (KamperPanel Refactor — PERF-02).

Output: A 22-03-SUMMARY.md recording build status, user observation, and per-module health.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-02-SUMMARY.md
@CLAUDE.md
@demos/compose/build.gradle.kts

<interfaces>
<!-- Compose Multiplatform desktop target uses jvmMain actuals -->

From demos/compose/build.gradle.kts (multiplatform source sets, post-Phase-21):
```kotlin
sourceSets {
    val commonMain by getting {
        dependencies {
            implementation(compose.runtime/foundation/material3/ui)
            implementation(project(":libs:engine"))
            implementation(project(":libs:modules:cpu"))   // ... all 8 modules
        }
    }
    val desktopMain by getting {
        dependencies {
            implementation(compose.desktop.currentOs)
        }
    }
    val androidMain by getting { /* uses :libs:ui:kmm */ }
    val iosMain by creating  { /* uses :libs:ui:kmm */ }
}
```

The desktop run task launches a Swing/Compose-Desktop window using JVM target actuals.
This is the SAME JVM source set Plan 22-01 already validated, so most failures here would
be Compose-specific (recomposition bugs, theme issues) not engine-level.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale ':kamper:' path references in demos/compose/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/compose/build.gradle.kts
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
  </read_first>
  <action>
    Run and capture verbatim:

    1. `grep -rn ":kamper:" demos/compose/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/compose/src/ --include="*.kt" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/compose/ || echo "NO_MATCHES"`

    Per 22-PATTERNS.md, expect "NO_MATCHES" on all three. Document any real match.
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/compose/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All three grep commands returned "NO_MATCHES" OR every match documented
    - Grep stdout captured in task summary
  </acceptance_criteria>
  <done>Grep results captured.</done>
</task>

<task type="auto">
  <name>Task 2: Run ./gradlew :demos:compose:run with 60-second timeout</name>
  <files>(executes, captures log)</files>
  <read_first>
    - demos/compose/build.gradle.kts
    - CLAUDE.md (Build & Test section)
  </read_first>
  <action>
    Execute the desktop variant of the Compose demo:

    ```
    ./gradlew :demos:compose:run --console=plain 2>&1 | tee /tmp/22-03-compose-run.log &
    GRADLE_PID=$!
    sleep 60
    kill $GRADLE_PID 2>/dev/null || true
    wait $GRADLE_PID 2>/dev/null || true
    ```

    If `:demos:compose:run` does not exist as a task, the Compose Multiplatform plugin
    typically registers `:demos:compose:runDistributable` or similar. Discover candidates with:

    ```
    ./gradlew :demos:compose:tasks --group=application --console=plain | grep -E "^[a-z]+ -" | head -20
    ```

    Use the first task that explicitly launches the desktop variant. Avoid Android tasks
    (`installDebug`, `assembleDebug`) here — those are validated by Plan 22-02.

    Capture:
    - BUILD SUCCESSFUL line
    - Last 50 stdout lines
    - Any stack traces
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-03-compose-run.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-03-compose-run.log exists and contains `BUILD SUCCESSFUL`
    - No `FAILED` lines before BUILD SUCCESSFUL
    - Last 50 lines captured for Task 3
    - The actual gradle task name used is recorded (run / runDistributable / runDebug)
  </acceptance_criteria>
  <done>Build completed; runtime log captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User observes Compose desktop window for 30 seconds</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Smoke Test Observation Guide)
    - /tmp/22-03-compose-run.log
  </read_first>
  <what-built>
    Claude has launched the Compose Multiplatform desktop demo. The user must observe the window's metric sections for 30 seconds and confirm Compose recomposes them with non-INVALID values.
  </what-built>
  <how-to-verify>
    From the repo root in a NEW terminal, run:
    ```
    ./gradlew :demos:compose:run
    ```
    (or whichever task name Task 2 identified — re-use the same one).

    Observe for 30 seconds:
    1. A native desktop window appears (Compose for Desktop renders via Skia/Skiko)
    2. Window has Compose-styled metric sections — typically a list/grid for CPU, FPS, Memory, Network
    3. Each visible section shows numeric values updating roughly every second
    4. No `-1.0` / `INVALID` placeholder strings visible (per CpuInfo.INVALID = -1.0, MemoryInfo.INVALID, etc.)
    5. No stack traces in the terminal during the 30s
    6. Closing the window exits cleanly without a crash dump

    **Reply format**:
    - `approved` — desktop window appeared, ≥CPU and Memory showed live values for 30s, clean shutdown
    - `failed: <description>` — describe specifically (e.g. "failed: window appeared but CPU section blank")
    - `crashed: <description>` — paste exception class + first frame
    - `no-display` — running headless (CI / SSH); mark plan as BLOCKED
  </how-to-verify>
  <acceptance_criteria>
    - User reply is `approved`, `failed: <concrete>`, `crashed: <concrete>`, or `no-display`
    - Failure descriptions name a specific section, value, or exception
    - `no-display` → plan SUMMARY records `outcome: BLOCKED`
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, `crashed: <description>`, or `no-display`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 4: Diagnose Compose failure (only if Task 3 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-03-compose-run.log
    - demos/compose/src/desktopMain/ (entry point and Compose UI)
    - libs/ui/kmm/src/jvmMain/ (if it exists — KamperPanel JVM actual)
    - User's verbatim Task 3 reply
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-display`.

    Classify:
    1. **Build failure**: missing `:libs:*` path or Compose plugin mismatch. Grep build.log for `error:` and `cannot resolve`.
    2. **Window-launch crash**: Skiko / native window initialization failure (often `UnsatisfiedLinkError` on Linux without proper libs).
    3. **Empty/blank sections**: Compose state holders not collecting Kamper info flows — check the demo's ViewModel/state holder against Phase 7 patterns (each metric section recomposes only when its own data changes).
    4. **INVALID-only metrics**: same diagnosis as Plan 22-01 (JVM watcher actual throwing); refer back to 22-01-SUMMARY.md to see whether the same issue surfaced there.
    5. **Recomposition runaway** (window freezes): Phase 7 regression — check stable state holders.

    Document classification + root cause (file:line) + fix proposal.
  </action>
  <verify>
    <automated>test -f /tmp/22-03-compose-run.log</automated>
  </verify>
  <acceptance_criteria>
    - One classification chosen, OR task skipped
    - Root cause cites a specific file:line
    - Proposed fix is actionable
  </acceptance_criteria>
  <done>Failure classified or skipped.</done>
</task>

<task type="auto">
  <name>Task 5: Apply Compose fix and re-verify (only if Task 4 produced a fix)</name>
  <files>(varies — set by Task 4)</files>
  <read_first>
    - The file Task 4 named
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
    - CLAUDE.md (Safety Rule — D-06)
  </read_first>
  <action>
    SKIP if Task 3 returned `approved` or `no-display`.

    Apply the smallest fix Task 4 specified. After edit:
    1. `./gradlew detekt` must exit 0
    2. Re-run `./gradlew :demos:compose:run --console=plain 2>&1 | tee /tmp/22-03-compose-run-retry.log` with 60s timeout
    3. Re-prompt user via Task 3 protocol

    If still failing after one attempt, mark plan FAIL with residual cause documented.
  </action>
  <verify>
    <automated>./gradlew detekt --console=plain 2>&1 | tail -5; grep -c "BUILD SUCCESSFUL" /tmp/22-03-compose-run-retry.log 2>/dev/null || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Edited file is exactly Task 4's named target
    - `./gradlew detekt` exits 0
    - Retry log contains BUILD SUCCESSFUL
    - User re-prompted, OR plan honestly FAIL
  </acceptance_criteria>
  <done>Fix applied AND retry passes; OR plan FAIL with residual cause.</done>
</task>

<task type="auto">
  <name>Task 6: Write 22-03-SUMMARY.md with PASS/FAIL/BLOCKED outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-03-compose-run.log
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 03`, `platform: compose-desktop`, `outcome: PASS|FAIL|BLOCKED`, `date: 2026-04-29`
    2. **Stale reference scan**: Task 1 grep output verbatim
    3. **Build outcome**: BUILD SUCCESSFUL + actual gradle task name used
    4. **Smoke test outcome**: PASS / FAIL / BLOCKED + user's Task 3 reply verbatim
    5. **Module health**: per-module status for CPU, FPS, Memory, Network, Issues, Jank, GC, Thermal — each `live` / `INVALID` / `not observed`
    6. **Fixes applied**: file list with rationale, OR "none required"
    7. **Outcome rule**: PASS = BUILD SUCCESSFUL + `approved` + ≥2 modules live; FAIL = build/runtime fail after one fix; BLOCKED = `no-display`
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md && grep -E "^outcome: (PASS|FAIL|BLOCKED)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Frontmatter contains exactly one of `outcome: PASS`, `FAIL`, or `BLOCKED`
    - User reply quoted verbatim
    - Module health table lists all 8 modules
  </acceptance_criteria>
  <done>22-03-SUMMARY.md written with explicit outcome.</done>
</task>

</tasks>

<verification>
- Stale reference scan complete
- Compose desktop window launches successfully (BUILD SUCCESSFUL + window visible)
- User observes ≥30s with live metrics
- 22-03-SUMMARY.md committed
</verification>

<success_criteria>
- 22-03-SUMMARY.md with explicit outcome
- All 8 module statuses recorded
- Every fix is smallest viable edit
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-03-SUMMARY.md` with outcome, build log, user observation, module health, fixes.
</output>

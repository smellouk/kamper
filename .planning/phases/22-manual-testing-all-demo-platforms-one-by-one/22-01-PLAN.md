---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "demos/jvm/ contains zero stale ':kamper:*' Gradle path references"
    - "./gradlew :demos:jvm:run completes BUILD SUCCESSFUL"
    - "JVM demo launches without crashing and runs ≥30 seconds"
    - "addInfoListener callbacks fire with non-INVALID values for at least CPU and Memory modules"
    - "Smoke test results recorded in 22-01-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md"
      provides: "JVM demo smoke test outcome"
      contains: "PASS"
  key_links:
    - from: "demos/jvm/build.gradle.kts"
      to: ":libs:engine, :libs:modules:*"
      via: "implementation(project(...)) declarations"
      pattern: "implementation\\(project\\(\":libs:"
---

<objective>
Validate that the JVM demo (`demos/jvm/`) builds and passes a full 30-second smoke test on the post-Phase-21 monorepo. This is the fastest demo to test (no emulator, no Xcode, no browser) — running it first confirms the basic Kamper engine + module install chain is healthy before tackling platform-specific demos. Per D-06, JVM is the first platform tested.

Purpose: Gain fast confidence that `:libs:engine` + `:libs:modules:*` install correctly via `Kamper.install(...)` and that `addInfoListener<T>` callbacks fire with non-INVALID values. If JVM fails, every other demo will also fail.

Output: A 22-01-SUMMARY.md recording PASS/FAIL with build output excerpts and observed metric values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-CONTEXT.md
@.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md
@CLAUDE.md
@demos/jvm/build.gradle.kts
@demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt

<interfaces>
<!-- Smoke test signal contracts the executor must check for -->

From libs/api/src/commonMain/.../Watcher.kt and module Info classes:
```kotlin
// Each module Info has an INVALID sentinel companion (D-06 of CLAUDE.md):
// CpuInfo.INVALID, MemoryInfo.INVALID, FpsInfo.INVALID, etc.
// A passing smoke test means addInfoListener callbacks deliver Info instances
// where info != XxxInfo.INVALID within ~2 seconds of Kamper.start().
```

From demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt:
```kotlin
// Engine install pattern — these listeners must fire with valid data:
Kamper.install(CpuModule)
Kamper.install(MemoryModule())
Kamper.install(NetworkModule)
// ... etc.
Kamper.addInfoListener<CpuInfo>    { /* should fire with non-INVALID */ }
Kamper.addInfoListener<MemoryInfo> { /* should fire with non-INVALID */ }
Kamper.start()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep for stale ':kamper:' path references in demos/jvm/</name>
  <files>(read-only scan)</files>
  <read_first>
    - demos/jvm/build.gradle.kts
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Stale Reference Findings section)
  </read_first>
  <action>
    Run the following grep commands from the repo root and capture stdout/exit codes verbatim:

    1. `grep -rn ":kamper:" demos/jvm/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts" || echo "NO_MATCHES"`
    2. `grep -rn '":kamper' demos/jvm/src/ --include="*.kt" || echo "NO_MATCHES"`
    3. `grep -rn "kamper/engine\|kamper/modules\|kamper/api\|kamper/ui" demos/jvm/ || echo "NO_MATCHES"`

    Per 22-PATTERNS.md the JVM build file is already clean — expect "NO_MATCHES" on all three. If ANY grep returns a real match (i.e. not "NO_MATCHES"), STOP and surface the match in the task output before proceeding to Task 2; do not attempt fixes here, fold them into Task 5.

    Record the exact grep output in the task summary.
  </action>
  <verify>
    <automated>grep -rn ":kamper:" demos/jvm/ --include="*.gradle" --include="*.gradle.kts" --include="*.kts"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - All three grep commands returned "NO_MATCHES" (no output, exit 1) OR every match has been documented in task output
    - No silent skip — exact grep stdout captured in task summary
  </acceptance_criteria>
  <done>Grep results captured; either zero stale references found, or all findings documented for Task 5.</done>
</task>

<task type="auto">
  <name>Task 2: Run ./gradlew :demos:jvm:run and capture build + runtime output</name>
  <files>(executes build, captures logs)</files>
  <read_first>
    - demos/jvm/build.gradle.kts
    - demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt
    - CLAUDE.md (Build & Test section)
  </read_first>
  <action>
    Execute the JVM demo with a 60-second timeout so the smoke test has at least 30 seconds of runtime after build completes:

    ```
    ./gradlew :demos:jvm:run --console=plain 2>&1 | tee /tmp/22-01-jvm-run.log &
    GRADLE_PID=$!
    sleep 45
    kill $GRADLE_PID 2>/dev/null || true
    wait $GRADLE_PID 2>/dev/null || true
    ```

    Then inspect the log:
    1. Confirm `BUILD SUCCESSFUL` appears in /tmp/22-01-jvm-run.log
    2. Confirm no `FAILED` lines appear before BUILD SUCCESSFUL
    3. Capture the last 50 lines of stdout — these should show CPU/Memory/Network metric prints (the JVM Main launches a Swing UI; if running headless, the `runConsole` task in build.gradle.kts is the fallback)
    4. If `./gradlew :demos:jvm:run` fails with `java.awt.HeadlessException` or similar GUI-related error, retry with the console-only task: `./gradlew :demos:jvm:runConsole --console=plain` and the same 45s timeout

    Do NOT use `connectedAndroidTest` or any device-requiring command (per CLAUDE.md WARNING).
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-01-jvm-run.log</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/22-01-jvm-run.log exists and is non-empty
    - `grep -c "BUILD SUCCESSFUL" /tmp/22-01-jvm-run.log` returns ≥ 1
    - `grep -c "FAILED" /tmp/22-01-jvm-run.log` returns 0 (or zero matches before BUILD SUCCESSFUL)
    - Last 50 lines of /tmp/22-01-jvm-run.log captured for use in Task 3 observation
  </acceptance_criteria>
  <done>JVM demo build completed; runtime stdout captured for observation analysis.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User confirms JVM demo smoke test passes (30-second observation)</name>
  <read_first>
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Smoke Test Observation Guide section)
    - /tmp/22-01-jvm-run.log (captured in Task 2)
  </read_first>
  <what-built>
    Claude has executed `./gradlew :demos:jvm:run` and captured the runtime log. The user must now interpret the live behaviour the log alone cannot prove (Swing window rendered, metrics updating in real time).
  </what-built>
  <how-to-verify>
    Either:

    **Option A — Re-run interactively (recommended)**: From the repo root, in a NEW terminal window, run:
    ```
    ./gradlew :demos:jvm:run
    ```
    Wait up to 30 seconds for output, then observe:
    1. A Swing window titled "Kamper JVM Demo" (or similar) appears WITHOUT a Java/Kotlin stack trace in the terminal
    2. The window has tabs/sections for at least: CPU, Memory, FPS, Network
    3. The CPU section shows a numeric percentage (e.g. `12.3%`) updating roughly every second — NOT a static `-1.0` or `INVALID`
    4. The Memory section shows numeric values (heap used / max in MB) updating
    5. No "Exception" / "Error" / "FATAL" log lines appear in the terminal during the 30-second observation
    6. Closing the window or pressing Ctrl+C in the terminal exits cleanly without a crash

    **Option B — Inspect Task 2 log only (if no display available)**: Open `/tmp/22-01-jvm-run.log` and confirm:
    1. `BUILD SUCCESSFUL` appears
    2. No `Exception` or stack-trace lines after `BUILD SUCCESSFUL`
    3. If running `runConsole` was used, console prints show CPU and Memory metric values — non-zero, non-`-1.0`

    **Reply format**: Reply with one of:
    - `approved` — JVM demo passes (window/console showed live non-INVALID metrics for ≥30s)
    - `failed: <description>` — describe what you saw (e.g. "failed: window appeared but CPU stuck at -1.0")
    - `crashed: <description>` — describe the crash output
  </how-to-verify>
  <acceptance_criteria>
    - User reply is exactly `approved`, OR the failure description is concrete (mentions a value, screen state, or log line)
    - If user replies `failed:` or `crashed:`, Task 4 routes to Task 5 (apply fixes); if `approved`, skip Task 5 and proceed to Task 6
  </acceptance_criteria>
  <resume-signal>Reply: `approved`, `failed: <description>`, or `crashed: <description>`</resume-signal>
  <files>(human verification only — no file modifications)</files>
  <action>This is a checkpoint task. Claude does NOT execute commands here. Present the `<how-to-verify>` block to the user verbatim and WAIT for them to reply with the resume signal. Do not proceed to the next task until the user replies.</action>
  <verify>
    <automated>echo "checkpoint: awaiting user reply"</automated>
  </verify>
  <done>User has replied with the resume-signal value (`approved` / `failed: ...` / `crashed: ...` / etc.) and that reply is captured in the task summary.</done>
</task>

<task type="auto">
  <name>Task 4: Diagnose failure (only runs if Task 3 reported failure or crash)</name>
  <files>(read-only diagnosis)</files>
  <read_first>
    - /tmp/22-01-jvm-run.log
    - demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt
    - libs/modules/cpu/src/jvmMain/kotlin/com/smellouk/kamper/cpu/CpuInfoSource.jvm.kt (if exists)
    - libs/engine/src/jvmMain/ (sample one engine actual)
  </read_first>
  <action>
    SKIP this task if Task 3 returned `approved`.

    Otherwise, classify the failure into one of these categories and document the root cause in task output:

    1. **Build failure** (`BUILD FAILED` in log): grep for `error:` and `Cannot resolve` lines; root cause is usually a stale path reference Task 1 missed, or a transitive `:libs:*` module that does not exist. List the exact compile errors and the file:line they occurred at.

    2. **Runtime crash** (stack trace after BUILD SUCCESSFUL): grep for `Exception in thread`. Identify the throwing class — if it is in `com.smellouk.kamper.*`, this is a Kamper bug (rare post-Phase-21); if it is JVM/Swing/AWT, it may be environment-specific (headless system).

    3. **INVALID-only metrics** (window appears but values are -1.0): the watcher's `getValue()` is throwing per CLAUDE.md D-06. Identify which module — CPU is most likely on macOS (since CPU watcher reads `/proc/stat` which doesn't exist on Darwin). Document which modules are INVALID and which are healthy.

    4. **No metrics observable** (window appears empty): listener wiring broken. Inspect Main.kt's `addInfoListener` blocks against the install order.

    Write the classification + root cause + proposed fix into task output. Do NOT apply fixes here — that is Task 5.
  </action>
  <verify>
    <automated>test -f /tmp/22-01-jvm-run.log && echo "diagnosis input present"</automated>
  </verify>
  <acceptance_criteria>
    - One of the four failure categories selected, OR task is skipped because Task 3 approved
    - Root cause references a specific file:line OR a specific log line excerpt
    - Proposed fix is actionable (specific edit to a specific file)
  </acceptance_criteria>
  <done>Failure classified with root cause and proposed fix; OR task skipped because Task 3 approved.</done>
</task>

<task type="auto">
  <name>Task 5: Apply fix and re-verify (only runs if Task 4 produced a fix)</name>
  <files>(varies — set by Task 4 diagnosis)</files>
  <read_first>
    - The file Task 4 identified as needing the fix (read it first to see current state)
    - demos/jvm/build.gradle.kts (if a path fix)
    - .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-PATTERNS.md (Local project() Dependency Pattern — canonical post-Phase-21 form)
  </read_first>
  <action>
    SKIP this task if Task 3 returned `approved` (no fix needed).

    Otherwise, apply the fix proposed in Task 4. Constrain the fix to the smallest possible edit:
    - Stale path reference: change `project(":kamper:X")` → `project(":libs:X")`. Match exactly the canonical form in `demos/jvm/build.gradle.kts` lines 27–35.
    - Module-specific runtime fix: edit the platform actual under `libs/modules/<name>/src/jvmMain/`; preserve the try/catch + `-1.0` fallback per CLAUDE.md "Safety Rule (Hard — D-06)".
    - Demo-side wiring fix: edit `demos/jvm/src/main/kotlin/com/smellouk/kamper/jvm/Main.kt`.

    After the fix, re-run:
    ```
    ./gradlew :demos:jvm:run --console=plain 2>&1 | tee /tmp/22-01-jvm-run-retry.log &
    GRADLE_PID=$!
    sleep 45
    kill $GRADLE_PID 2>/dev/null || true
    wait $GRADLE_PID 2>/dev/null || true
    ```

    Then loop back to Task 3 verification — request the user re-observe. If still failing, document the residual failure in 22-01-SUMMARY.md and mark plan as FAIL (do NOT silently abandon).
  </action>
  <verify>
    <automated>grep -c "BUILD SUCCESSFUL" /tmp/22-01-jvm-run-retry.log 2>/dev/null || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - File modified is exactly the file Task 4 named
    - `./gradlew detekt` still passes after the edit (run it once after applying the fix): `./gradlew detekt` exits 0
    - Retry log shows `BUILD SUCCESSFUL`
    - User has been re-prompted to observe the rerun, OR plan is explicitly marked FAIL with residual failure documented
  </acceptance_criteria>
  <done>Fix applied AND retry passes user observation; OR plan is honestly marked FAIL with residual failure cause documented.</done>
</task>

<task type="auto">
  <name>Task 6: Write 22-01-SUMMARY.md with PASS/FAIL outcome</name>
  <files>.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/22-01-jvm-run.log (and /tmp/22-01-jvm-run-retry.log if it exists)
  </read_first>
  <action>
    Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md` containing:

    1. Frontmatter: `phase: 22`, `plan: 01`, `platform: jvm`, `outcome: PASS` (or `FAIL`), `date: 2026-04-29`
    2. **Stale reference scan**: paste the Task 1 grep output (or "NO_MATCHES on all 3 patterns")
    3. **Build outcome**: BUILD SUCCESSFUL/FAILED + the gradle command used (`:demos:jvm:run` or `:demos:jvm:runConsole`)
    4. **Smoke test outcome**: PASS / FAIL with the user's observation reply verbatim
    5. **Module health**: for each of CPU, Memory, FPS, Network, Issues, Jank, GC, Thermal — record `live` / `INVALID` / `not observed` based on user feedback
    6. **Fixes applied**: list every file modified during this plan with a one-line rationale, OR "none required"
    7. **Outcome**: PASS only if (a) BUILD SUCCESSFUL, (b) user observed live non-INVALID values, (c) no crash during 30s observation. Otherwise FAIL with explicit reason.

    Use the template at $HOME/.claude/get-shit-done/templates/summary.md as the structural skeleton.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md && grep -E "^outcome: (PASS|FAIL)$" .planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path
    - Frontmatter contains `outcome: PASS` or `outcome: FAIL` (one of the two, exactly)
    - The user observation quote from Task 3 appears verbatim in the Smoke test outcome section
    - Module health table lists all 8 modules with one of {live, INVALID, not observed}
  </acceptance_criteria>
  <done>22-01-SUMMARY.md written with the verbatim user observation and explicit PASS/FAIL outcome.</done>
</task>

</tasks>

<verification>
- Stale reference scan complete (zero matches expected per 22-PATTERNS.md)
- `./gradlew :demos:jvm:run` exits with BUILD SUCCESSFUL
- User confirms (via Task 3 reply) that the JVM demo runs ≥30 seconds without crashing and shows non-INVALID metric values
- 22-01-SUMMARY.md committed with explicit PASS/FAIL frontmatter
</verification>

<success_criteria>
- 22-01-SUMMARY.md exists with `outcome: PASS` (FAIL also valid completion if all tasks ran honestly and the failure is documented)
- Zero `connectedAndroidTest` invocations attempted (per CLAUDE.md WARNING)
- Every fix applied is the smallest viable edit and matches the canonical `:libs:*` path pattern
</success_criteria>

<output>
Create `.planning/phases/22-manual-testing-all-demo-platforms-one-by-one/22-01-SUMMARY.md` with PASS/FAIL outcome, build log excerpts, user observation verbatim, module health table, and applied fixes list.
</output>

---
phase: 25-rename-the-library-from-kamper-to-konitor
plan: 07
type: execute
wave: 6
depends_on: [25-01, 25-02, 25-03, 25-04, 25-05, 25-06]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Zero 'kamper' occurrences in shipped artifact source files (.kt, .kts, .gradle, .yml, .yaml, .md, .json, .podspec, .ts)"
    - "Zero kamper* source directory names remain outside /build/ and /.cxx/"
    - "./gradlew :libs:api:test passes after the rename"
    - "./gradlew :libs:engine:test passes after the rename"
    - "./gradlew detekt passes with zero issues"
  artifacts:
    - path: "libs/api/src/commonMain/kotlin/com/smellouk/konitor/api/Info.kt"
      provides: "API contracts in konitor package"
      contains: "package com.smellouk.konitor.api"
    - path: "libs/engine/src/commonMain/kotlin/com.smellouk.konitor/Engine.kt"
      provides: "Engine in konitor package"
      contains: "package com.smellouk.konitor"
    - path: "Konitor.podspec"
      provides: "CocoaPods spec"
      contains: "s.name = 'Konitor'"
  key_links:
    - from: "grep audit"
      to: "zero kamper occurrences"
      via: "grep -r kamper --include=*.kt --include=*.kts"
      pattern: "no matches in shipped artifacts"
---

<objective>
Perform a final grep audit to confirm zero "kamper" occurrences remain in shipped artifact surfaces (.kt, .kts, .gradle, .yml, .yaml, .md, .json, .podspec, .ts, .h, .mm, .c, .def, .xml), then run the full test suite and detekt to confirm the build passes after the rename.

This plan contains a human-verify checkpoint because a passing build confirms the rename is correct end-to-end. The grep audit runs autonomously first; only the build result requires user confirmation.

Purpose: Validate the complete rename is correct and the build is healthy.
Output: Confirmed clean audit + passing build.
</objective>

<execution_context>
@/Users/smellouk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/smellouk/Developer/git/kamper/.planning/ROADMAP.md
@/Users/smellouk/Developer/git/kamper/.planning/STATE.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive grep audit — confirm zero kamper occurrences in shipped artifact files</name>
  <read_first>
    - Run the grep command below and review results carefully
  </read_first>
  <files></files>
  <action>
Run the following comprehensive grep audit. For each file type, report any remaining occurrences. Expected result for all: 0 matches.

**Audit 1 — Kotlin source files (package declarations, class names, imports):**
```bash
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.kt" \
  /Users/smellouk/Developer/git/kamper/libs/ \
  /Users/smellouk/Developer/git/kamper/demos/ \
  | grep -v "/build/" | grep -v "/.cxx/" | grep -v "node_modules"
```

**Audit 2 — Gradle build files:**
```bash
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.kts" --include="*.gradle" \
  /Users/smellouk/Developer/git/kamper/ \
  | grep -v "/build/" | grep -v "node_modules" | grep -v "/.planning/"
```

**Audit 3 — CI workflows and YAML:**
```bash
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.yml" --include="*.yaml" \
  /Users/smellouk/Developer/git/kamper/ \
  | grep -v "/build/" | grep -v "/.planning/" | grep -v "node_modules"
```

**Audit 4 — Documentation and JSON:**
```bash
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.md" --include="*.json" --include="*.toml" \
  /Users/smellouk/Developer/git/kamper/ \
  | grep -v "/build/" | grep -v "/.planning/" | grep -v "node_modules" | grep -v "/.git/"
```

**Audit 5 — Podspecs, TypeScript, ObjC, C:**
```bash
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.podspec" --include="*.ts" --include="*.h" --include="*.mm" \
  --include="*.c" --include="*.def" --include="*.xml" \
  /Users/smellouk/Developer/git/kamper/ \
  | grep -v "/build/" | grep -v "/.cxx/" | grep -v "node_modules" | grep -v "/Pods/" | grep -v "/.planning/"
```

**Expected allowable remaining kamper occurrences (DO NOT flag as issues):**
- `github.com/smellouk/kamper` — repo URL preserved per D-15 (in README.md, CLAUDE.md, podspecs, CONTRIBUTING.md)
- `smellouk/kamper.git` — git remote URLs (same reason)
- `id("kamper.kmp.library")`, `id("kamper.publish")`, `id("kamper.android.config")` — Gradle plugin IDs preserved (Plan 01 decision)
- `.planning/` directory contents — explicitly out of scope per CONTEXT.md deferred section
- `node_modules/` — excluded from audit by grep filters above

**For any result NOT in the allowable list above:** fix before proceeding to Task 2.

Common fixes:
- Missing file: run `git mv` + content sed
- Missed class name: `sed -i '' 's/[[:<:]]Kamper[[:>:]]/Konitor/g' {file}` (BSD sed — use [[:<:]] not \b)
- Missed package: `sed -i '' 's/com\.smellouk\.kamper/com.smellouk.konitor/g' {file}`

Document all findings and actions taken in the SUMMARY.md output.
  </action>
  <verify>
    <automated>
grep -r "kamper\|Kamper\|KAMPER" \
  --include="*.kt" --include="*.kts" --include="*.gradle" \
  /Users/smellouk/Developer/git/kamper/libs/ \
  /Users/smellouk/Developer/git/kamper/demos/ \
  | grep -v "/build/" | grep -v "/.cxx/" | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - Audit 1 result: 0 matches (Kotlin source files)
    - Audit 2 result: only `id("kamper.kmp.library")`, `id("kamper.publish")`, `id("kamper.android.config")` matches allowed (plugin IDs)
    - Audit 3 result: only github.com/smellouk/kamper URL matches allowed (repo URL)
    - Audit 4 result: only github.com/smellouk/kamper URL and release-please bootstrap SHA context matches allowed
    - Audit 5 result: only github.com/smellouk/kamper URL matches allowed in podspecs; 0 matches in .ts, .h, .mm, .c, .def files
    - `find /Users/smellouk/Developer/git/kamper -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/" | grep -v "node_modules" | grep -v "/.planning/" | grep -v "/.git/"` returns 0
  </acceptance_criteria>
  <done>Comprehensive grep audit passes. Zero unexplained kamper occurrences remain in shipped artifact surfaces. All remaining kamper strings are plugin IDs or repo URLs per documented allowable list.</done>
</task>

<task type="auto">
  <name>Task 2: Run test suite and detekt to confirm build passes after rename</name>
  <read_first>
    - Run: ./gradlew :libs:api:test :libs:engine:test (as per CLAUDE.md smoke test)
  </read_first>
  <files></files>
  <action>
Run the standard Konitor (renamed from Kamper) test suite including all unit tests (as per CLAUDE.md smoke test command):

```bash
./gradlew :libs:api:test :libs:engine:test --rerun-tasks
```

Then run detekt to confirm zero static analysis violations:
```bash
./gradlew detekt
```

If tests fail, investigate and fix the root cause. Common issues after a rename:
- Missing import (class was renamed but import not updated): find the file via the error, apply sed
- Wrong class name in test (e.g., `Kamper.` still in a test file): fix with sed on specific file
- Missing file reference (e.g., `@xml/kamper_file_paths` not found because XML not renamed): verify Plan 04 completed
- Compilation error in a file where sed substitution was partial: read the file, fix manually

Do NOT run `./gradlew connectedAndroidTest` — requires device per CLAUDE.md WARNING.
Do NOT run any task that requires Xcode or a physical iOS/macOS device.
  </action>
  <verify>
    <automated>./gradlew :libs:api:test :libs:engine:test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :libs:api:test` exits 0 (BUILD SUCCESSFUL)
    - `./gradlew :libs:engine:test` exits 0 (BUILD SUCCESSFUL)
    - `./gradlew detekt` exits 0 with "0 violations found"
    - No "Unresolved reference" or "Cannot resolve symbol" errors in build output
    - No "kamper" string appears in any compilation error output
  </acceptance_criteria>
  <done>./gradlew :libs:api:test :libs:engine:test passes. ./gradlew detekt passes with zero issues. The Konitor library builds successfully after the full rename.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete rename from Kamper to Konitor:
- Plan 01: KonitorPublishPlugin.kt, updated settings.gradle.kts and build.gradle.kts
- Plan 02: 548 .kt files + 142+ source directories renamed to com.smellouk.konitor
- Plan 03: All Kamper* class files renamed to Konitor* equivalents
- Plan 04: All build.gradle.kts namespace/applicationId updated; GPU native lib renamed; XML resources renamed
- Plan 05: Demos renamed; RN TypeScript/podspec/CI/release-please updated
- Plan 06: 6 skill directories renamed; CLAUDE.md/README.md/CONTRIBUTING.md updated
- Plan 07: Grep audit passed; tests passed; detekt passed
  </what-built>
  <how-to-verify>
1. Run `./gradlew :libs:api:test :libs:engine:test` — expect BUILD SUCCESSFUL
2. Run `./gradlew detekt` — expect BUILD SUCCESSFUL, 0 violations
3. Spot-check a few key files:
   - Open `libs/engine/src/androidMain/kotlin/com/smellouk/konitor/Konitor.kt` — should have `object Konitor`
   - Open `libs/ui/kmm/src/commonMain/kotlin/com/smellouk/konitor/ui/KonitorUi.kt` — should have `expect object KonitorUi`
   - Open `libs/api/src/commonMain/kotlin/com/smellouk/konitor/api/KonitorDslMarker.kt` — should have `annotation class KonitorDslMarker`
   - Open `CLAUDE.md` — title should read "Konitor" and skills should be referenced as `/konitor-new-module` etc.
   - Open `README.md` — install instructions should show `com.smellouk.konitor:engine`
4. Verify skill directories: `ls .claude/skills/` should show 6 konitor-* directories, no kamper-* directories
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any remaining issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grep audit → human review | Audit results reviewed before declaring rename complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Tampering | All renamed files | accept | Verification only plan — no new code written. Grep audit confirms rename correctness. Test suite confirms build health. |
</threat_model>

<verification>
Final state verification:
1. `grep -r "^package com.smellouk.kamper" libs --include="*.kt" | grep -v "/build/"` returns 0
2. `find libs demos -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/" | grep -v "node_modules"` returns 0
3. `./gradlew :libs:api:test :libs:engine:test` exits 0
4. `./gradlew detekt` exits 0
5. `ls .claude/skills/ | grep "^kamper"` returns 0
6. `test -f Konitor.podspec` exits 0
7. `test ! -f Kamper.podspec` exits 0
</verification>

<success_criteria>
- Comprehensive grep audit: zero unexplained kamper occurrences in shipped artifacts
- Zero kamper source directories (outside excluded dirs)
- ./gradlew :libs:api:test :libs:engine:test passes (BUILD SUCCESSFUL)
- ./gradlew detekt passes (0 violations)
- Human verification confirms key spot-checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-07-SUMMARY.md`
</output>

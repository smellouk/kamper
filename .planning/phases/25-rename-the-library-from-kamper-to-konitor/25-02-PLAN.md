---
phase: 25-rename-the-library-from-kamper-to-konitor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/api/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/engine/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/engine/src/commonMain/kotlin/com.smellouk.kamper/
  - libs/modules/*/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/ui/kmm/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/ui/rn/android/src/main/java/com/smellouk/kamper/**/*.kt
  - libs/integrations/*/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/rn/src/**/kotlin/com/smellouk/kamper/**/*.kt
  - libs/apple-sdk/src/**/kotlin/com/smellouk/kamper/**/*.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every .kt file in libs/ has package declaration com.smellouk.konitor.* (not kamper)"
    - "All source directories renamed from com/smellouk/kamper/ to com/smellouk/konitor/ in libs/"
    - "Engine module dot-separated directory renamed from com.smellouk.kamper to com.smellouk.konitor"
    - "All import statements referencing com.smellouk.kamper.* in libs/ updated to com.smellouk.konitor.*"
  artifacts:
    - path: "libs/engine/src/commonMain/kotlin/com.smellouk.konitor"
      provides: "Engine commonMain source directory (dot-separated per existing convention)"
      contains: "Engine.kt"
    - path: "libs/api/src/commonMain/kotlin/com/smellouk/konitor/api"
      provides: "API contracts source directory"
      contains: "Info.kt"
    - path: "libs/modules/cpu/src/commonMain/kotlin/com/smellouk/konitor/cpu"
      provides: "CPU module canonical reference source directory"
      contains: "CpuInfo.kt"
  key_links:
    - from: "libs/engine/src/androidMain/kotlin/com/smellouk/konitor/Kamper.kt"
      to: "package com.smellouk.konitor"
      via: "package declaration"
      pattern: "^package com.smellouk.konitor$"
    - from: "libs/api/src/commonMain/kotlin/com/smellouk/konitor/api/"
      to: "package com.smellouk.konitor.api"
      via: "package declaration"
      pattern: "^package com.smellouk.konitor.api$"
---

<objective>
Rename all source directories from com/smellouk/kamper/ to com/smellouk/konitor/ across every module in libs/, and update every package declaration and import statement within those files from com.smellouk.kamper.* to com.smellouk.konitor.*.

This plan is PARALLEL with Plan 01 (they touch different files — Plan 01 touches build-logic/ and root scripts; this plan touches libs/ source). It is the atomically critical source rename — once directories and package declarations are renamed, Plans 03–06 can proceed.

Purpose: Rename the Kotlin package namespace in all 548 source files across 142 source directories in libs/.
Output: All libs/ .kt files using com.smellouk.konitor.* package declarations and imports. Source directories renamed.
</objective>

<execution_context>
@/Users/smellouk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/smellouk/Developer/git/kamper/.planning/ROADMAP.md
@/Users/smellouk/Developer/git/kamper/.planning/STATE.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-CONTEXT.md

<interfaces>
<!-- Directory structure context for executor. These are the source roots that need renaming. -->

Pattern: every source set in every lib follows com/smellouk/kamper/<module-name>/
Replace with:                                   com/smellouk/konitor/<module-name>/

libs/ source root list (all source sets under each):
  libs/api/src/          → 8 source sets (commonMain, commonTest, androidMain, jvmMain, iosMain, macosMain, tvosMain, jsMain, wasmJsMain)
  libs/engine/src/       → 9 source sets including androidInstrumentedTest
    SPECIAL: commonMain uses DOT-separated dir: kotlin/com.smellouk.kamper/ → kotlin/com.smellouk.konitor/
    All other engine source sets use slash: kotlin/com/smellouk/kamper/ → kotlin/com/smellouk/konitor/
  libs/modules/cpu/      → 10 source sets (all standard)
  libs/modules/fps/      → 10 source sets
  libs/modules/gc/       → 9 source sets
  libs/modules/gpu/      → 11 source sets (includes androidUnitTest, jvmTest)
  libs/modules/issues/   → 10 source sets
  libs/modules/jank/     → 9 source sets
  libs/modules/memory/   → 10 source sets
  libs/modules/network/  → 10 source sets
  libs/modules/thermal/  → 9 source sets
  libs/ui/kmm/src/       → 8 source sets (includes appleMain, androidUnitTest)
  libs/ui/rn/android/src/main/java/ → com/smellouk/kamper/rn/ (Java source tree)
  libs/integrations/sentry/src/     → 2 source sets (commonMain, commonTest)
  libs/integrations/firebase/src/   → 8 source sets
  libs/integrations/opentelemetry/  → 8 source sets
  libs/rn/src/           → 8 source sets
  libs/apple-sdk/src/    → 1 source set (commonMain)

GPU module special case for cinterop import:
  libs/modules/gpu/src/macosMain/.../MacosGpuInfoSource.kt has:
    import com.smellouk.kamper.gpu.cinterop.kamper_gpu_stats
  Update to: import com.smellouk.konitor.gpu.cinterop.kamper_gpu_stats
  (The C function name `kamper_gpu_stats` stays — only the Kotlin package prefix changes here)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename source directories from com/smellouk/kamper to com/smellouk/konitor in all libs</name>
  <read_first>
    - Run: find /Users/smellouk/Developer/git/kamper/libs -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/" | sort
    - libs/engine/src/commonMain/kotlin/ (to confirm the dot-separated directory name)
  </read_first>
  <files>
    libs/api/src/**/com/smellouk/kamper,
    libs/engine/src/commonMain/kotlin/com.smellouk.kamper,
    libs/engine/src/**/com/smellouk/kamper,
    libs/modules/*/src/**/com/smellouk/kamper,
    libs/ui/kmm/src/**/com/smellouk/kamper,
    libs/ui/rn/android/src/main/java/com/smellouk/kamper,
    libs/integrations/*/src/**/com/smellouk/kamper,
    libs/rn/src/**/com/smellouk/kamper,
    libs/apple-sdk/src/**/com/smellouk/kamper
  </files>
  <action>
Rename every source directory named `kamper` (at the `com/smellouk/kamper` level) to `konitor` across all libs/ source sets (per D-06).

Strategy: use `git mv` for each directory so git tracks the rename properly. Process module by module.

Step 1 — Rename standard slash-separated source directories. For each of these patterns, run git mv:
```
git mv libs/api/src/{sourceSet}/kotlin/com/smellouk/kamper libs/api/src/{sourceSet}/kotlin/com/smellouk/konitor
```
for sourceSet in: commonMain, commonTest, androidMain, jvmMain, iosMain, macosMain, tvosMain, jsMain, wasmJsMain

Repeat for every module's every source set. Specifically:
- libs/api/src/{commonMain,commonTest,androidMain,jvmMain,iosMain,macosMain,tvosMain,jsMain,wasmJsMain}/kotlin/com/smellouk/kamper → konitor
- libs/engine/src/{androidMain,androidInstrumentedTest,jvmMain,iosMain,macosMain,tvosMain,jsMain,wasmJsMain,commonTest}/kotlin/com/smellouk/kamper → konitor
- libs/modules/cpu/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/fps/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/gc/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/gpu/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/issues/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/jank/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/memory/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/network/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/modules/thermal/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/ui/kmm/src/{commonMain,commonTest,androidMain,androidInstrumentedTest,androidUnitTest,appleMain,iosMain,tvosMain}/kotlin/com/smellouk/kamper → konitor
- libs/ui/rn/android/src/main/java/com/smellouk/kamper → konitor
- libs/integrations/sentry/src/{commonMain,commonTest}/kotlin/com/smellouk/kamper → konitor
- libs/integrations/firebase/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/integrations/opentelemetry/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/rn/src/{all source sets}/kotlin/com/smellouk/kamper → konitor
- libs/apple-sdk/src/commonMain/kotlin/com/smellouk/kamper → konitor

Step 2 — Rename the engine commonMain DOT-SEPARATED directory (D-05 special case):
```
git mv libs/engine/src/commonMain/kotlin/com.smellouk.kamper libs/engine/src/commonMain/kotlin/com.smellouk.konitor
```
This is a flat directory rename (dot in name, not a path separator).

Do NOT rename the `smellouk` directory (only `kamper` → `konitor`).
  </action>
  <verify>
    <automated>find /Users/smellouk/Developer/git/kamper/libs -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find libs -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/"` returns 0 results
    - `find libs -type d -name "konitor" | grep -v "/build/" | grep -v "/.cxx/"` returns ≥ 140 results
    - `test -d libs/engine/src/commonMain/kotlin/com.smellouk.konitor` exits 0
    - `test ! -d libs/engine/src/commonMain/kotlin/com.smellouk.kamper` exits 0
    - `test -d libs/api/src/commonMain/kotlin/com/smellouk/konitor` exits 0
    - `test -d libs/modules/cpu/src/commonMain/kotlin/com/smellouk/konitor/cpu` exits 0
  </acceptance_criteria>
  <done>All source directories in libs/ renamed from `kamper` to `konitor`. Engine commonMain dot-separated directory renamed. No `kamper` directory names remain in libs/ source tree.</done>
</task>

<task type="auto">
  <name>Task 2: Update all package declarations and import statements in libs/ .kt files to com.smellouk.konitor</name>
  <read_first>
    - libs/modules/cpu/src/commonMain/kotlin/com/smellouk/konitor/cpu/CpuInfo.kt (verify dir renamed, check current package line)
    - libs/engine/src/commonMain/kotlin/com.smellouk.konitor/Engine.kt (verify dot-dir renamed)
    - libs/modules/gpu/src/macosMain/kotlin/com/smellouk/konitor/gpu/repository/source/MacosGpuInfoSource.kt
  </read_first>
  <files>
    libs/**/*.kt (all 548 Kotlin source files in libs/)
  </files>
  <action>
Update every `package com.smellouk.kamper` declaration and every `import com.smellouk.kamper` statement in all .kt files under libs/ to use `com.smellouk.konitor` (per D-04).

Use sed (or equivalent in-place replacement) across all .kt files:
```bash
find /Users/smellouk/Developer/git/kamper/libs -name "*.kt" | grep -v "/build/" | grep -v "/.cxx/" | xargs sed -i '' 's/com\.smellouk\.kamper/com.smellouk.konitor/g'
```

This single substitution handles:
- `package com.smellouk.kamper` → `package com.smellouk.konitor`
- `package com.smellouk.kamper.api` → `package com.smellouk.konitor.api`
- `package com.smellouk.kamper.cpu` → `package com.smellouk.konitor.cpu`
- `import com.smellouk.kamper.Kamper` → `import com.smellouk.konitor.Kamper`
- `import com.smellouk.kamper.api.Logger` → `import com.smellouk.konitor.api.Logger`
- `import com.smellouk.kamper.gpu.cinterop.kamper_gpu_stats` → `import com.smellouk.konitor.gpu.cinterop.kamper_gpu_stats`
- All other package/import combinations

NOTE: Class names (Kamper, KamperUi, KamperPanel, etc.) are NOT renamed here — that is Plan 03's scope. This task ONLY changes the package namespace prefix.

NOTE: The C symbol `kamper_gpu_stats` in the import for MacosGpuInfoSource.kt will correctly become `com.smellouk.konitor.gpu.cinterop.kamper_gpu_stats` via this substitution (only the package prefix changes, not the C symbol name).

After running sed, verify a few representative files manually:
- libs/engine/src/commonMain/kotlin/com.smellouk.konitor/Engine.kt — check package line
- libs/api/src/commonMain/kotlin/com/smellouk/konitor/api/Info.kt — check package line
- libs/modules/cpu/src/commonMain/kotlin/com/smellouk/konitor/cpu/CpuInfo.kt — check package line
  </action>
  <verify>
    <automated>grep -r "^package com\.smellouk\.kamper" /Users/smellouk/Developer/git/kamper/libs --include="*.kt" | grep -v "/build/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "^package com.smellouk.kamper" libs --include="*.kt" | grep -v "/build/"` returns 0 matches
    - `grep -r "^package com.smellouk.konitor" libs --include="*.kt" | grep -v "/build/" | wc -l` returns ≥ 540 (most of the 548 files)
    - `grep -r "import com.smellouk.kamper" libs --include="*.kt" | grep -v "/build/"` returns 0 matches
    - `grep "package com.smellouk.konitor" libs/engine/src/commonMain/kotlin/com.smellouk.konitor/Engine.kt` exits 0
    - `grep "package com.smellouk.konitor.api" libs/api/src/commonMain/kotlin/com/smellouk/konitor/api/Info.kt` exits 0
    - `grep "package com.smellouk.konitor.cpu" libs/modules/cpu/src/commonMain/kotlin/com/smellouk/konitor/cpu/CpuInfo.kt` exits 0
  </acceptance_criteria>
  <done>All 548 .kt files in libs/ use com.smellouk.konitor.* package declarations and imports. Zero occurrences of "com.smellouk.kamper" remain in package/import lines in libs/ source files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source files → Kotlin compiler | Package declarations in renamed source files are the only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | libs/**/*.kt | accept | Rename only — substituting com.smellouk.kamper → com.smellouk.konitor in package/import lines. No logic changes, no new code paths, no new permissions. |
</threat_model>

<verification>
After completing both tasks:
1. `find libs -type d -name "kamper" | grep -v "/build/" | grep -v "/.cxx/"` returns 0
2. `grep -r "^package com.smellouk.kamper" libs --include="*.kt" | grep -v "/build/"` returns 0
3. `grep -r "import com.smellouk.kamper" libs --include="*.kt" | grep -v "/build/"` returns 0
4. `test -d libs/engine/src/commonMain/kotlin/com.smellouk.konitor` exits 0
5. `test -d libs/api/src/commonMain/kotlin/com/smellouk/konitor/api` exits 0
</verification>

<success_criteria>
- Zero `kamper` source directories remain in libs/ (outside /build/ and /.cxx/)
- Zero `com.smellouk.kamper` package declarations remain in libs/ .kt files
- Zero `com.smellouk.kamper` import statements remain in libs/ .kt files
- Engine commonMain dot-separated directory is named `com.smellouk.konitor`
</success_criteria>

<output>
After completion, create `.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-02-SUMMARY.md`
</output>

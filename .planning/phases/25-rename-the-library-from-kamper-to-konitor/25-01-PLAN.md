---
phase: 25-rename-the-library-from-kamper-to-konitor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build-logic/src/main/kotlin/KamperPublishPlugin.kt
  - build-logic/build.gradle.kts
  - settings.gradle.kts
  - build.gradle.kts
autonomous: true
requirements: []

must_haves:
  truths:
    - "KamperPublishPlugin.kt file renamed to KonitorPublishPlugin.kt"
    - "class KamperPublishPlugin renamed to KonitorPublishPlugin"
    - "project.group set to com.smellouk.konitor in the plugin"
    - "build-logic/build.gradle.kts registers plugin with class KonitorPublishPlugin"
    - "rootProject.name is Konitor in settings.gradle.kts"
    - "root build.gradle.kts uses KONITOR_GH_USER / KONITOR_GH_PAT credential keys"
  artifacts:
    - path: "build-logic/src/main/kotlin/KonitorPublishPlugin.kt"
      provides: "Maven publish convention plugin with konitor group ID"
      contains: "com.smellouk.konitor"
    - path: "build-logic/build.gradle.kts"
      provides: "build-logic plugin registrations"
      contains: "KonitorPublishPlugin"
    - path: "settings.gradle.kts"
      provides: "root project name"
      contains: "Konitor"
  key_links:
    - from: "build-logic/build.gradle.kts"
      to: "build-logic/src/main/kotlin/KonitorPublishPlugin.kt"
      via: "implementationClass = \"KonitorPublishPlugin\""
      pattern: "KonitorPublishPlugin"
    - from: "build.gradle.kts"
      to: "KONITOR_GH_USER"
      via: "extra property key"
      pattern: "KONITOR_GH_USER"
---

<objective>
Rename the build-logic publish plugin from KamperPublishPlugin to KonitorPublishPlugin, update the Maven groupId from com.smellouk.kamper to com.smellouk.konitor, update build-logic plugin registration, update settings.gradle.kts rootProject.name to "Konitor", and rename credential keys in root build.gradle.kts.

This is the highest-risk atomic unit: KonitorPublishPlugin.kt sets project.group for every published module. If it reads the old value, all artifact coordinates remain kamper. This must complete before Wave 2 (source renames) to ensure the build resolves after the full rename.

Purpose: Establish the Maven groupId and plugin identity foundation for the konitor brand. All subsequent plans depend on this being correct.
Output: KonitorPublishPlugin.kt, updated build-logic/build.gradle.kts, updated settings.gradle.kts, updated build.gradle.kts credential keys.
</objective>

<execution_context>
@/Users/smellouk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smellouk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/smellouk/Developer/git/kamper/.planning/ROADMAP.md
@/Users/smellouk/Developer/git/kamper/.planning/STATE.md
@/Users/smellouk/Developer/git/kamper/.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-CONTEXT.md

<interfaces>
<!-- Current state of files being modified. Read these before touching anything. -->

From build-logic/src/main/kotlin/KamperPublishPlugin.kt:
```kotlin
class KamperPublishPlugin : Plugin<Project> {
    override fun apply(project: Project) {
        project.group = "com.smellouk.kamper"
        // KAMPER_GH_USER / KAMPER_GH_PAT used in afterEvaluate block
        credentials {
            username = project.rootProject.extra["KAMPER_GH_USER"].toString()
            password = project.rootProject.extra["KAMPER_GH_PAT"].toString()
        }
        url = project.uri("https://maven.pkg.github.com/smellouk/kamper")
        // pom fields reference "kamper" in name.set("kamper:$projectName")
        // and description.set("Kamper is small KMM/KMP library...")
    }
}
```

From build-logic/build.gradle.kts (plugin registration):
```kotlin
gradlePlugin {
    plugins {
        register("kamperKmpLibrary") {
            id = "kamper.kmp.library"
            implementationClass = "KmpLibraryPlugin"
        }
        register("kamperAndroidConfig") {
            id = "kamper.android.config"
            implementationClass = "AndroidConfigPlugin"
        }
        register("kamperPublish") {
            id = "kamper.publish"
            implementationClass = "KamperPublishPlugin"   // ← rename only implementationClass
        }
    }
}
```

From settings.gradle.kts:
```kotlin
rootProject.name = "Kamper"   // → "Konitor"
```

From build.gradle.kts (credential key names):
```kotlin
set("KAMPER_GH_USER", properties["kamperGhUser"])    // → KONITOR_GH_USER / konitorGhUser
set("KAMPER_GH_PAT", properties["kamperGhToken"])     // → KONITOR_GH_PAT / konitorGhToken
// and in env var branch:
Env().containsKey("KAMPER_GH_USER") && Env().containsKey("KAMPER_GH_PAT")
set("KAMPER_GH_USER", Env("KAMPER_GH_USER"))
set("KAMPER_GH_PAT", Env("KAMPER_GH_PAT"))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename KamperPublishPlugin.kt to KonitorPublishPlugin.kt and update contents</name>
  <read_first>
    - build-logic/src/main/kotlin/KamperPublishPlugin.kt (read the entire file — all class/method content)
  </read_first>
  <files>build-logic/src/main/kotlin/KonitorPublishPlugin.kt</files>
  <action>
Create a new file `build-logic/src/main/kotlin/KonitorPublishPlugin.kt` (per D-07) with the EXACT content of `KamperPublishPlugin.kt` but with these substitutions:

1. Class name: `KamperPublishPlugin` → `KonitorPublishPlugin`
2. Group ID: `project.group = "com.smellouk.kamper"` → `project.group = "com.smellouk.konitor"` (per D-02/D-03)
3. Credential extra keys: `"KAMPER_GH_USER"` → `"KONITOR_GH_USER"` and `"KAMPER_GH_PAT"` → `"KONITOR_GH_PAT"` (referenced in afterEvaluate block)
4. GitHub Packages URL: `https://maven.pkg.github.com/smellouk/kamper` — LEAVE UNCHANGED (per D-15, repo not renamed)
5. POM name string: `name.set("kamper:$projectName")` → `name.set("konitor:$projectName")`
6. POM description: `"Kamper is small KMM/KMP library that provides performance monitoring for your app."` → `"Konitor is small KMM/KMP library that provides performance monitoring for your app."`
7. KDoc comment referencing "Kamper": update "Kamper" occurrences in the class KDoc to "Konitor"
8. `KAMPER_GH_USER / KAMPER_GH_PAT` in KDoc → `KONITOR_GH_USER / KONITOR_GH_PAT`

Then DELETE `build-logic/src/main/kotlin/KamperPublishPlugin.kt`.

Note: Plugin ID `"kamper.publish"` stays as-is — it is referenced from 20+ build.gradle.kts files. Only the implementationClass changes.
  </action>
  <verify>
    <automated>grep -c "KonitorPublishPlugin" /Users/smellouk/Developer/git/kamper/build-logic/src/main/kotlin/KonitorPublishPlugin.kt && grep -c "com.smellouk.konitor" /Users/smellouk/Developer/git/kamper/build-logic/src/main/kotlin/KonitorPublishPlugin.kt && test ! -f /Users/smellouk/Developer/git/kamper/build-logic/src/main/kotlin/KamperPublishPlugin.kt && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `build-logic/src/main/kotlin/KonitorPublishPlugin.kt` exists
    - File `build-logic/src/main/kotlin/KamperPublishPlugin.kt` does NOT exist
    - `grep "KonitorPublishPlugin" build-logic/src/main/kotlin/KonitorPublishPlugin.kt` returns at least 1 match
    - `grep "com.smellouk.konitor" build-logic/src/main/kotlin/KonitorPublishPlugin.kt` returns at least 1 match (the project.group line)
    - `grep -v "^[ *]*//\|^ *\*" build-logic/src/main/kotlin/KonitorPublishPlugin.kt | grep "com.smellouk.kamper"` returns 0 matches (no kamper in production code)
    - `grep "KONITOR_GH_USER\|KONITOR_GH_PAT" build-logic/src/main/kotlin/KonitorPublishPlugin.kt` returns at least 2 matches
  </acceptance_criteria>
  <done>KonitorPublishPlugin.kt is the only publish plugin in build-logic/src/main/kotlin/, class and group ID use konitor naming, credential keys renamed, old file deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update build-logic/build.gradle.kts and settings.gradle.kts and root build.gradle.kts</name>
  <read_first>
    - build-logic/build.gradle.kts (read entirely)
    - settings.gradle.kts (read entirely)
    - build.gradle.kts (read entirely)
  </read_first>
  <files>build-logic/build.gradle.kts, settings.gradle.kts, build.gradle.kts</files>
  <action>
Make the following changes:

**build-logic/build.gradle.kts:**
- In the `gradlePlugin` block, change `implementationClass = "KamperPublishPlugin"` → `implementationClass = "KonitorPublishPlugin"` (per D-07)
- All other plugin registrations (`kamperKmpLibrary` using id `"kamper.kmp.library"`, `kamperAndroidConfig` using id `"kamper.android.config"`) — leave the plugin IDs unchanged; only the class name changes for kamperPublish
- NOTE: Plugin IDs (`kamper.kmp.library`, `kamper.android.config`, `kamper.publish`) are applied via `id("kamper.publish")` in 20+ module build.gradle.kts files. Do NOT change plugin IDs in this task — changing just implementationClass is sufficient and safer.

**settings.gradle.kts:**
- Change `rootProject.name = "Kamper"` → `rootProject.name = "Konitor"`
- All other content (include statements, repository config) remains unchanged

**build.gradle.kts:**
- In the `extra.apply {}` block, change credential key names:
  - `properties["kamperGhUser"]` → `properties["konitorGhUser"]`
  - `properties["kamperGhToken"]` → `properties["konitorGhToken"]`
  - `set("KAMPER_GH_USER", ...)` → `set("KONITOR_GH_USER", ...)`
  - `set("KAMPER_GH_PAT", ...)` → `set("KONITOR_GH_PAT", ...)`
  - `Env().containsKey("KAMPER_GH_USER")` → `Env().containsKey("KONITOR_GH_USER")`
  - `Env().containsKey("KAMPER_GH_PAT")` → `Env().containsKey("KONITOR_GH_PAT")`
  - `Env("KAMPER_GH_USER")` → `Env("KONITOR_GH_USER")`
  - `Env("KAMPER_GH_PAT")` → `Env("KONITOR_GH_PAT")`
  </action>
  <verify>
    <automated>grep "KonitorPublishPlugin" /Users/smellouk/Developer/git/kamper/build-logic/build.gradle.kts && grep "rootProject.name = \"Konitor\"" /Users/smellouk/Developer/git/kamper/settings.gradle.kts && grep "KONITOR_GH_USER" /Users/smellouk/Developer/git/kamper/build.gradle.kts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "KonitorPublishPlugin" build-logic/build.gradle.kts` → 1 match (implementationClass line)
    - `grep "KamperPublishPlugin" build-logic/build.gradle.kts` → 0 matches
    - `grep "rootProject.name" settings.gradle.kts` → output contains `"Konitor"` not `"Kamper"`
    - `grep "KONITOR_GH_USER" build.gradle.kts` → at least 3 matches
    - `grep "KAMPER_GH_USER\|KAMPER_GH_PAT" build.gradle.kts` → 0 matches
    - `grep "konitorGhUser\|konitorGhToken" build.gradle.kts` → 2 matches
  </acceptance_criteria>
  <done>build-logic/build.gradle.kts registers KonitorPublishPlugin; settings.gradle.kts names the project Konitor; root build.gradle.kts uses KONITOR_GH_USER/KONITOR_GH_PAT credential keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build scripts → plugin | build.gradle.kts files call id("kamper.publish") which routes to KonitorPublishPlugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | KonitorPublishPlugin.kt | accept | Rename only — no new code paths, no new permissions, no new network calls. File contents are equivalent to old KamperPublishPlugin.kt with string substitution only. |
</threat_model>

<verification>
After completing both tasks:
1. `grep -r "KamperPublishPlugin" build-logic/` returns 0 results
2. `grep "com.smellouk.konitor" build-logic/src/main/kotlin/KonitorPublishPlugin.kt` returns ≥1 result
3. `test -f build-logic/src/main/kotlin/KonitorPublishPlugin.kt` exits 0
4. `test ! -f build-logic/src/main/kotlin/KamperPublishPlugin.kt` exits 0
5. `grep "rootProject.name" settings.gradle.kts | grep -c "Konitor"` returns 1
</verification>

<success_criteria>
- KonitorPublishPlugin.kt exists and KamperPublishPlugin.kt does not
- Maven groupId is com.smellouk.konitor in the plugin
- build-logic/build.gradle.kts registers KonitorPublishPlugin
- rootProject.name = "Konitor" in settings.gradle.kts
- Credential keys are KONITOR_GH_USER / KONITOR_GH_PAT in build.gradle.kts
</success_criteria>

<output>
After completion, create `.planning/phases/25-rename-the-library-from-kamper-to-konitor/25-01-SUMMARY.md`
</output>

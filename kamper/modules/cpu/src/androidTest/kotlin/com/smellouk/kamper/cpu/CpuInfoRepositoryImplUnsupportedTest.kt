package com.smellouk.kamper.cpu

import org.junit.Test

/**
 * Wave 0 stub for FEAT-01 CpuInfoRepositoryImpl UNSUPPORTED capability-probe tests.
 *
 * Plan 07-03 Task 2 will replace this placeholder with the full set of
 * 3 instrumented tests (UNSUPPORTED return when both sources fail,
 * cache short-circuit on second call, no false-positive cache flip when
 * shell fallback succeeds).
 *
 * NB: The full test target package (per Plan 07-03) is
 * `com.smellouk.kamper.cpu.repository`. This stub lives at
 * `com.smellouk.kamper.cpu` so it remains compile-clean before the
 * `repository.CpuInfoMapper`/`repository.source.CpuInfoSource` references
 * are introduced in Plan 07-03.
 */
@Suppress("IllegalIdentifier")
class CpuInfoRepositoryImplUnsupportedTest {
    @Test
    fun `UNSUPPORTED capability-probe tests will be implemented in Plan 07-03`() {
        TODO("implement in Plan 07-03 Task 2")
    }
}
